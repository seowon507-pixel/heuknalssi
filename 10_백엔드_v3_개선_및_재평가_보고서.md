# 흙날씨.진단 Backend v3 개선 및 재평가 보고서

> 작성일: 2026-07-24  
> 구현 대상: `backend-v3/`  
> 권위 기준: `09_흙날씨진단_서비스로직_명세서.md`  
> 주의: 참여 역할은 실제 사람이나 채용된 전문가가 아니라 서로 분리된 AI 서브에이전트 시뮬레이션이다.

## 1. 최종 결론

**Revision 7의 내부 백엔드 코드는 `READY`다. 외부 운영 배포는 `HOLD`다.**

세 기존 평가자가 동일 코드에 대해 다시 독립 평가한 결과는 다음과 같다.

| 독립 평가 역할 | 점수 | P0 | P1 | P2 | 내부 코드 | 외부 배포 |
|---|---:|---:|---:|---:|---|---|
| 백엔드 로직 평가자 | 99/100 | 0 | 0 | 1 | `READY` | `HOLD` |
| 통합·신뢰성 평가자 | 98/100 | 0 | 0 | 2 | `READY` | `HOLD` |
| 보안·테스트 평가자 | 97/100 | 0 | 0 | 0 | `READY` | `HOLD` |
| **평균** | **98.0/100** | **0** | **0** | **중복 제외 3** | **`READY`** | **`HOLD`** |

`READY`는 현재 저장소의 결정론적 도메인 로직, API, 실패 차단, 보안 경계와 테스트 증거에 대한 판정이다. 실제 농업 규칙·지역 매핑·공급자 응답 계약이 검수됐다는 뜻은 아니다. 해당 자산이 없으므로 기본 서버가 `ready:false`, `serviceState:"HOLD"`를 반환하는 것이 올바른 동작이다.

저장소 밖의 기존 `v2` 참고본은 수정하지 않았고, 당시 프론트엔드에도 연결하지 않았다. `backend-v3` 안의 프론트엔드 파일은 0개다.

## 2. 역할 분리

| 구분 | AI 서브에이전트 역할 | 책임 | 코드 수정 |
|---|---|---|---|
| 개발 | 주 에이전트 | 요구사항 통합, 검증, 최종 문서화 | 예 |
| 신규 개발 | `domain_engine_dev` | 규칙·도메인·작기·preflight·stage 계약 | 예 |
| 신규 개발 | `adapter_dev` | 공급자 어댑터·계약·동시성·실패 격리 | 예 |
| 신규 개발 | `api_security_dev` | HTTP·세션·CSRF·소유권·멱등성·저장소 | 예 |
| 읽기 전용 분석 | `rule_context_explorer` | 작기 입력공간과 matcher 구조 검토 | 아니오 |
| 기존 평가 | `backend_logic_reviewer` | 명세·도메인·규칙·결정론 재평가 | **아니오** |
| 기존 평가 | `integration_reliability_reviewer` | 런타임·어댑터·저장소·HTTP 재평가 | **아니오** |
| 기존 평가 | `security_test_reviewer` | 보안 경계·테스트·정보노출 재평가 | **아니오** |

기존 평가자 세 명은 모든 재평가에서 읽기 전용으로 참여했으며 개발 작업에 참여하지 않았다. 발견된 P0/P1은 평가자가 아닌 개발자에게 돌려보낸 뒤 같은 평가자가 다시 확인했다.

## 3. 평가 이력

| Revision | 로직 | 통합·신뢰성 | 보안·테스트 | 판정 | 주요 이유 |
|---|---:|---:|---:|---|---|
| 초기 v2 | — | — | — | `CHANGES_REQUESTED` — 종합 약 22/100 | 합성점수, 결측 재가중, 대표값, 검증되지 않은 외부 계약 |
| R1 | 68 | 62 | 60 | `CHANGES_REQUESTED` | 실행 경계·저장·실패 격리·보안 검증 부족 |
| R2 | 78 | 80 | 74 | `CHANGES_REQUESTED` | 계약 drift, readiness, 소유권·운영 경계 잔여 |
| R3 | 88 | 92 | 84 | `CHANGES_REQUESTED` | 핵심 구조 개선 후에도 P1 잔여 |
| R4 | 93 | 95 | 미완료 | `CHANGES_REQUESTED` | 컨텍스트 교차검증 P1 |
| R5 | 95 | 98 | 96 | `CHANGES_REQUESTED` | 전체 작기 입력공간 커버리지 P1 |
| R6 | 96 | 98 | 97 | `CHANGES_REQUESTED` | preflight와 실제 Forecast stage 의미 불일치 P1 |
| **R7** | **99** | **98** | **97** | **내부 `READY`** | **P0/P1 0, 평균 98.0** |

높은 평균점수만으로 승인하지 않았다. R5와 R6은 평균이 높아도 P1이 한 건씩 존재했으므로 `CHANGES_REQUESTED`로 판정했고 새 revision을 진행했다.

## 4. 핵심 개선 결과

### 4.1 계산 정직성

- 기후·토양·최근 관측·예보를 하나의 대표점수로 합치지 않는다.
- 결측을 0으로 바꾸지 않고 원래 계획 가중치의 누락분을 보존한다.
- 토양 구간별 면적분포를 필지 대표값이나 임의 평균으로 만들지 않는다.
- `stale`, `sample`, 부분 공급자 실패로 현재 무위험을 확정하지 않는다.
- 자유형 LLM이 수치·원인·진단·처방·행동을 새로 만들지 못한다.

### 4.2 요청·작기·규칙

- 요청 정규화와 작물·재배형태·작기·생육단계 조합을 서버에서 검증한다.
- 노지 `UNKNOWN`은 기후만 `HOLD`하고 계절과 독립적인 Soil·Forecast 부분분석을 유지한다.
- 시설토경은 Soil+Forecast, 시설수경은 Forecast를 작기 종류와 무관하게 일관되게 적용한다.
- `CUSTOM`은 1~12월 단일월 대표 요청을 모두 실제 정규화·규칙 해석 경로로 검사한다.
- `11→2` 같은 연도 순환 기간을 `[11,12,1,2]`로 처리한다.
- Soil 규칙은 작기와 독립적으로 활성화하며, 동일 작물·재배형태·겹치는 stage·metric의 중복 계상을 차단한다.

### 4.3 Revision 7 구조 수정

R6에서 같은 본질의 “preflight와 실제 실행 matcher 불일치”가 다시 확인돼 preflight만 국소 수정하지 않았다.

- `src/rules/registry.js`에 공통 `ruleStageMatchesRequestContext`를 두었다.
- Forecast의 중복 로컬 matcher를 제거했다.
- registry, preflight, 실제 Forecast 실행이 같은 stage 계약을 사용한다.
- baseline `UNSPECIFIED` 요청에서는 Forecast `ANY` 규칙만 활성화한다.
- 구체 stage 요청에서는 Forecast `ANY + 동일 stage`만 활성화한다.
- Climate·Soil의 기존 `UNSPECIFIED` 정확 일치 의미는 보존했다.

회귀 테스트는 registry와 실제 Forecast의 활성 규칙 ID가 `ANY`, `UNSPECIFIED`, `FLOWERING`에서 동일한지 직접 비교한다. 모든 Forecast 규칙이 `stage:"UNSPECIFIED"`인 전역 구성은 더 이상 preflight를 `READY`로 만들지 못한다.

### 4.4 어댑터·신뢰성

- Kakao 후보 0·1·다건을 분리하고 사용자가 후보를 확인하기 전 임의 선택하지 않는다.
- 넓은 행정구역의 provider centroid를 필지 점 좌표로 사용하지 않는다.
- KMA 단기·중기는 날짜·공간수준·발표시각을 보존하고 중기 부분 공급자 실패를 비캐시 상태로 격리한다.
- Soil V2는 코드에 동결된 canonical 계약 전체가 정확히 일치해야만 외부 호출한다.
- 같은 버전 문자열이어도 단위·필드·경계·면적·오차·요청 의미·지표·연도가 바뀌면 외부 호출 전에 `UNSUPPORTED`로 닫힌다.
- SingleFlight는 개별 waiter 취소가 다른 waiter의 업스트림 작업을 중단하지 않게 한다.
- 공급자 동시성·대기열·circuit·deadline·제한된 retry를 적용한다.

### 4.5 API·보안·저장

- 서명 익명 세션, Origin/CSRF, 운영 Secure cookie/HSTS를 적용한다.
- 분석 소유권 불일치와 존재하지 않는 ID는 같은 404 계약으로 반환한다.
- 신뢰 프록시는 명시적 IP/CIDR allowlist만 인정한다.
- 멱등성은 원문 JSON이 아니라 서비스가 정규화한 의미론적 요청을 해시한다.
- IP·세션 rate limit, 본문 크기 제한, 안전한 오류 응답과 최소 로그를 적용한다.
- 후보·분석·세션·멱등 저장소는 포화 시 활성 레코드를 축출하지 않고 fail-closed한다.
- 후보 batch 저장 실패 시 해당 batch에서 먼저 넣은 토큰만 롤백한다.

## 5. 최종 검증 증거

| 검사 | 명령 또는 절차 | 최종 결과 |
|---|---|---|
| 전체 자동 테스트 | `npm test` | **191/191 통과** |
| 정적 프로젝트 검사 | `npm run check` | **61개 파일 통과** |
| 전체 커버리지 | `node --test --experimental-test-coverage` | line **89.55%**, branch **77.29%**, function **91.57%** |
| 실제 HTTP E2E | `node --test test/backend-e2e.test.js` | **1/1 통과** |
| 공식 실행 경로 | `PORT=31995 npm start` | 정상 listen |
| 실제 preflight | `GET /api/health/preflight` | HTTP **200**, `ready:false`, `serviceState:"HOLD"`, blocker 10 |
| 프론트엔드 혼입 검사 | HTML/CSS/JSX/TSX/Vue/Svelte 검색 | **0개** |
| 대표 secret prefix 검사 | 저장소 내 key·private-key·Bearer 패턴 검색 | **일치 0개** |
| Git HEAD | `git rev-parse --verify HEAD` | **없음** |

제한된 샌드박스에서 처음 실행한 전체 테스트는 로컬 `listen` 권한 때문에 HTTP 테스트 23개가 `EPERM`으로 중단됐다. 같은 `npm test`를 로컬 포트 허용 상태에서 다시 실행해 191개 전부 통과했다. 이는 코드 실패로 집계하지 않았으며 원인과 재실행 결과를 모두 기록했다.

현재 저장소는 unborn 상태로 `HEAD`가 없다. 사용자가 commit을 요청하지 않았으므로 임의 commit을 만들지 않았고, 보고할 commit SHA는 `NONE`이다.

## 6. 남은 P2와 운영 한계

### P2 — 내부 코드 승인을 막지는 않지만 추적할 항목

1. **`FAILED_INPUT` 수명주기 기록**
   - enum에는 존재하지만 분석 전이 순서와 영속 기록에는 연결되지 않았다.
   - 현재 입력 오류는 안전한 HTTP 400으로 처리되므로 보안·데이터 오류는 아니지만 감사 가능한 수명주기 계약의 부채다.

2. **공식 시작 경로 자동 E2E**
   - `npm start`와 신뢰 런타임 모듈 조립은 수동 스모크로 검증했다.
   - 자식 프로세스를 시작·호출·종료하는 자동 E2E는 아직 없다.

3. **전체 HTTP 매트릭스**
   - 실제 HTTP E2E는 오이 노지의 세션→위치→분석→멱등 재생→소유권→보고 흐름 중심이다.
   - 다섯 작물, 시설토경·시설수경, 넓은 행정구역, 각 공급자 부분 실패의 전체 조합은 도메인·애플리케이션 테스트에는 있으나 같은 깊이의 HTTP E2E는 아니다.

### 운영 구조 한계

- 세션·후보·분석·멱등·rate limit·circuit 상태는 단일 프로세스 메모리 기반이다.
- 다중 인스턴스 전환 전 공유 저장소, 분산 rate limit, 암호화, 보관·삭제정책이 필요하다.
- 알 수 없는 경로와 비정상 트래픽의 대규모 차단은 운영 edge/WAF와 함께 설계해야 한다.

## 7. 외부 배포 `HOLD` 해제 조건

다음 자산을 실제 응답과 공식 문서로 동결하기 전에는 feature flag나 임의 문자열만으로 `READY`를 만들면 안 된다.

1. 출처·단위·문서 위치·검토일·버전이 있는 5개 작물 규칙팩
2. 출처·버전·유효기간이 있는 최소 6개 지역 위치 매핑
3. checksum과 규칙 버전이 일치하는 기후평년 스냅샷
4. ASOS 운영지점·기간 자료존재·거리·월평년 계약
5. Soil V2 XML 필드·단위·경계·허용오차·연도 계약과 실제 fixture
6. Kakao·KMA 단기·중기 실제 응답 fixture와 호출 계약
7. 운영 API key, quota, 이용조건·라이선스, 보관정책 확인

## 8. 권고 구현 순서

1. 5작물 규칙팩을 별도 검수 산출물로 동결하고 preflight에 주입한다.
2. 6개 시연지역 매핑을 공식 코드·관측지점·예보구역 계약과 함께 동결한다.
3. 실제 공급자 응답 fixture로 adapter contract를 확정한다.
4. `FAILED_INPUT` 감사 수명주기와 공식 시작 경로 자동 E2E를 추가한다.
5. 명세 §26.3의 다작물·시설·실패 HTTP E2E 매트릭스를 확장한다.
6. 다중 인스턴스가 필요해질 때 공유 저장소와 분산 제한 구조로 전환한다.

## 9. 범위 확인

- `backend-v3/`: 새 백엔드 구현 및 테스트
- `08_흙날씨진단_UI_샘플.html`: 수정하지 않음
- 현재 프론트엔드: 연결하지 않음
- 저장소 밖의 기존 `v2` 참고본: 수정하지 않음
- 외부 서비스·배포·DB: 변경하지 않음
