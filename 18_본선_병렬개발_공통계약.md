# 흙날씨 본선 병렬개발 공통 계약

> 기준 커밋: `e4a4086`
>
> 통합 브랜치: `codex/finalist-product-upgrade`
>
> 목적: 투두·사진/시즌·인공위성·챗봇을 동시에 개발해도 같은 농장과 같은 근거를 가리키도록 제품 계약을 먼저 고정한다.

## 1. 제품 원칙

1. 기후·토양·관측·예보·사진·위성은 서로 다른 근거 축이다. 하나의 종합점수로 합치지 않는다.
2. 없는 값은 `null`, 확인되지 않은 값은 `HOLD | UNAVAILABLE | UNSUPPORTED`로 유지한다.
3. AI는 근거에 없는 수치·원인·진단·농약/비료 처방·행동을 만들지 않는다.
4. 사용자가 보게 될 기본 화면은 `오늘 할 일 → 이유 → 완료 → 재확인 시점` 순서다. 원자료와 기술 상태는 상세 화면으로 분리한다.
5. 생성·수정·완료·삭제는 사용자의 명시적 확인 뒤에만 수행한다. 챗봇은 먼저 변경 제안을 보여준다.
6. 사진과 위성은 정밀도를 높이는 선택 근거다. 없다는 이유만으로 매번 전체 판정을 `HOLD`로 만들지 않는다.

## 2. 공통 식별자와 시간

- `accountId`: 계정키 원문이 아닌 서버 내부 해시 기반 식별자
- `farmId`: 농장 단위 UUID
- `cropId`: 농장에 등록된 작물 재배 단위 UUID. 같은 작물도 구역/시즌이 다르면 별도 ID
- `seasonId`: 파종/정식부터 수확 종료까지의 재배 시즌 UUID
- `analysisId`: 기존 분석 결과의 불투명 식별자
- `parcelId`: 사용자가 지도에서 저장한 필지 경계 UUID
- `actionId`, `photoId`, `observationId`: 기능별 UUID
- 모든 서버 시간은 ISO 8601 UTC로 저장하고, UI에서만 `Asia/Seoul`로 표시한다.
- 사용자 제공 촬영일·재배일은 `observedAt`, 서버 수신 시각은 `createdAt`으로 분리한다.

## 3. 공통 상태와 출처

```js
EvidenceState = "READY" | "PARTIAL" | "HOLD" | "UNAVAILABLE" | "UNSUPPORTED"
SourceKind = "USER" | "PUBLIC_API" | "PHOTO" | "SATELLITE" | "DERIVED_RULE"

EvidenceRef = {
  sourceKind,
  sourceId,
  observedAt: string | null,
  fetchedAt: string | null,
  spatialLevel: string,
  state: EvidenceState,
  limitationCodes: string[],
}
```

- 사용자 화면에는 내부 `limitationCodes`를 그대로 노출하지 않는다.
- `PARTIAL`은 계산값을 임의 보정한다는 뜻이 아니라, 확인된 축만 보여준다는 뜻이다.

## 4. 핵심 엔티티

### Farm

```js
{
  farmId, accountId, name, regionLabel, coordinates,
  cultivationMode, cropIds, activeSeasonIds,
  createdAt, updatedAt
}
```

### ActionItem

```js
{
  actionId, farmId, cropId, seasonId,
  title, instruction, reason,
  horizon: "TODAY" | "UPCOMING",
  status: "OPEN" | "DONE" | "SKIPPED",
  dueAt, recheckAt,
  evidenceRefs: EvidenceRef[],
  origin: "RULE" | "USER" | "ASSISTANT_PROPOSAL",
  completedAt, createdAt, updatedAt
}
```

- 규칙이 만든 행동은 같은 `farmId + cropId + ruleId + dueAt`에서 중복 생성하지 않는다.
- 완료 후 같은 위험이 다시 발생하면 기존 항목을 덮어쓰지 않고 새 항목을 만든다.

### PhotoRecord

```js
{
  photoId, farmId, cropId, seasonId,
  objectPath, thumbnailPath,
  observedAt, growthStage,
  note, consentState,
  analysisState, comparison,
  evidenceRefs: EvidenceRef[],
  createdAt, deletedAt
}
```

- 원본 파일과 AI/규칙 결과를 분리한다.
- 사진 비교는 색·면적·형태 변화 같은 관찰만 말하며 병해를 확정하지 않는다.
- 삭제 시 DB 메타데이터와 Storage 객체를 함께 삭제하는 서버 경로를 둔다.

### SeasonSummary

```js
{
  seasonId, farmId, cropId,
  startedAt, endedAt, status: "ACTIVE" | "COMPLETED",
  completedActionCount, skippedActionCount,
  riskTimeline, photoTimeline,
  generatedAt, limitations
}
```

- 시즌 회고는 저장된 사건만 시간순으로 요약한다. 수확량·성과를 추정하지 않는다.

### Parcel / SatelliteObservation

```js
Parcel = { parcelId, farmId, geometry, areaSquareMeters, createdAt, updatedAt }
SatelliteObservation = {
  observationId, parcelId, provider, collection,
  acquiredAt, cloudCover, resolutionMeters,
  indexName, indexValue, state,
  evidenceRefs: EvidenceRef[], limitations
}
```

- 유효한 GeoJSON Polygon/MultiPolygon만 허용하고 대한민국 경계 밖·과대 면적을 거부한다.
- 위성 촬영일·구름량·해상도·공간 범위를 항상 함께 보여준다.
- NDVI 등 지수는 같은 필지의 시계열 변화로만 해석하며 토양/작물 상태의 단독 진단으로 사용하지 않는다.

## 5. 변경 제안 계약

챗봇과 자동화는 바로 쓰지 않고 아래 제안을 반환한다.

```js
MutationProposal = {
  proposalId,
  type: "CREATE_ACTION" | "UPDATE_ACTION" | "COMPLETE_ACTION",
  preview,
  expiresAt,
  confirmationRequired: true
}
```

사용자가 확인 버튼을 누른 요청만 `proposalId`와 멱등성 키를 함께 보내 적용한다. 삭제는 채팅에서 지원하지 않는다.

## 6. API 경계

- `GET/POST /api/farms/:farmId/actions`
- `PATCH /api/farms/:farmId/actions/:actionId`
- `POST /api/farms/:farmId/photos` (업로드 준비/메타데이터 등록은 분리 가능)
- `GET /api/farms/:farmId/seasons/:seasonId/timeline`
- `POST /api/farms/:farmId/seasons/:seasonId/complete`
- `GET/PUT /api/farms/:farmId/parcel`
- `POST /api/farms/:farmId/satellite/observations`
- `POST /api/assistant/proposals/:proposalId/confirm`

모든 쓰기 API는 세션 소유권·CSRF·입력 제한·멱등성 검증을 통과해야 한다.

## 7. 저장과 배포 계약

- 서버리스 배포에서 프로세스 메모리는 캐시일 뿐 진실의 원천이 아니다.
- Supabase secret/service-role 키는 서버에서만 사용한다.
- Data API 객체는 2026년 공개 정책에 맞춰 명시적으로 `GRANT`하고, 노출 스키마 테이블에는 RLS를 켠다.
- 브라우저가 Supabase 비밀 키나 원본 Storage 경로를 직접 받지 않는다.
- 공유 저장소가 실제 읽기/쓰기/원자 연산 검증을 통과할 때만 `deploymentState="READY"`를 반환한다.

## 8. 병합 수용 조건

1. 기능별 단위·계약 테스트가 존재하고 전체 기존 테스트를 깨지 않는다.
2. 한 농장에 여러 작물, 여러 농장, 사진 없음, 위성 없음, 부분 API 실패가 모두 자연스럽게 동작한다.
3. 새 기능 때문에 기후·토양·예보의 결측값을 0이나 추정값으로 바꾸지 않는다.
4. 모바일 390px와 데스크톱 1440px 실제 화면에서 주요 행동과 상태가 3초 안에 보인다.
5. 챗봇은 확인 전에는 어떤 영구 변경도 하지 않는다.
6. P0/P1 버그가 없고, 독립 감사와 자연 사용자 흐름 검증을 통과한다.
