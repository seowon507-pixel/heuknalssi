# 흙날씨 다이어리·출석체크 기능 및 데이터 명세 V2

> 문서 성격: 구현 계약
> 전제: 현재 날씨·토양·예보·작물 규칙·행동·사진·수확 기능을 제거하지 않고 연결한다.
> 주의: 아래 API와 테이블명은 신규 구현 제안이며 현재 운영 완료를 뜻하지 않는다.

## 1. 도메인 경계

| 도메인 | 소유 데이터 | 다른 도메인과의 관계 |
| --- | --- | --- |
| 출석 | 농장별 날짜, 확인 방식, 확인 시각 | 일기 초안을 시작하지만 작물 점수에 참여하지 않음 |
| 다이어리 | 사진, 메모, 태그, 자동 초안, 확정 본문 | 작업·날씨·토양은 스냅샷 또는 참조로 연결 |
| 행동 계획 | 원인, 위험, 행동, 기한, 완료 | 완료 시 일기 초안의 재료가 됨 |
| 농장 분석 | 날씨·토양·예보·작물 규칙, 점수 | 출석과 무관하게 독립 계산 |
| 작물 시즌 | 파종·정식·생육·수확 단계 | 일기를 시즌 타임라인으로 묶음 |
| 사진 관찰 | 원본, 촬영일, 품질, 비교 결과 | 병해 확정 없이 일기에 연결 |

## 2. 출석 데이터 계약

### 2.1 `daily_checkins`

| 필드 | 형식 | 규칙 |
| --- | --- | --- |
| `id` | UUID | 서버 생성 |
| `user_id` | UUID | 인증 사용자 소유 |
| `farm_id` | UUID | 사용자에게 접근 권한이 있는 농장 |
| `local_date` | `YYYY-MM-DD` | 농장 시간대 기준 |
| `checkin_type` | `FIELD_VISIT \| REMOTE_CHECK` | 현장 방문과 원격 확인 구분 |
| `checked_at` | timestamp | 서버 기준 시각 |
| `status` | `ACTIVE \| CORRECTED \| DELETED` | 물리 삭제 대신 이력 보존 가능 |
| `note` | string nullable | 선택 입력, 최대 300자 |
| `client_request_id` | string | 재시도 중복 방지 키 |
| `created_at`, `updated_at` | timestamp | 감사와 동기화 |

필수 제약:

- `UNIQUE(user_id, farm_id, local_date)`
- `farm_id`는 서버에서 사용자 소유권 검증
- 동일 `client_request_id` 재요청은 동일 결과 반환
- 미래 날짜 출석 금지
- 과거 날짜 일기 작성은 허용하되 출석 생성 금지

### 2.2 출석 상태 계산

```text
NOT_CHECKED -> ACTIVE(FIELD_VISIT | REMOTE_CHECK)
ACTIVE -> CORRECTED
ACTIVE | CORRECTED -> DELETED
```

`DELETED`는 UI에서 미출석처럼 보이지만 감사 이력을 보존한다. 출석 수정·삭제는 작물 점수, 위험도, 과거 분석을 재계산하지 않는다.

### 2.3 복수 농장

- 농장별로 독립 출석을 기록한다.
- 계정 홈은 `오늘 확인한 농장 수 / 활성 농장 수`를 표시할 수 있다.
- 한 농장의 출석을 다른 농장에 복사하지 않는다.
- 연속 기록은 농장별로 계산하고 전체 계정에는 이번 달 총 기록일만 우선 표시한다.

## 3. 다이어리 데이터 계약

### 3.1 `diary_entries`

| 필드 | 형식 | 규칙 |
| --- | --- | --- |
| `id` | UUID | 서버 생성 |
| `user_id` | UUID | 소유자 |
| `farm_id` | UUID | 필수 |
| `season_id` | UUID nullable | 재배 시즌 연결 |
| `entry_date` | date | 기록 대상 날짜 |
| `title` | string | 최대 60자 |
| `body` | string | 최대 5,000자 |
| `authoring_mode` | `MANUAL \| AI_DRAFT_EDITED` | AI 초안 사용 여부 |
| `emotion_tag` | enum nullable | 사용자의 감정 태그이며 작물 상태가 아님 |
| `visibility` | `PRIVATE` 기본 | 향후 공유 전 명시적 동의 필요 |
| `draft_status` | `DRAFT \| SAVED` | 자동 초안은 `DRAFT` |
| `created_at`, `updated_at` | timestamp | 수정 추적 |

### 3.2 연결 테이블

- `diary_entry_crops(entry_id, crop_cycle_id)`
- `diary_entry_actions(entry_id, action_id)`
- `diary_entry_photos(entry_id, photo_id, display_order)`
- `diary_entry_sources(entry_id, source_type, source_id, snapshot_json, captured_at)`

날씨·경고는 `snapshot_json`으로 저장 시점 값을 고정한다. 원본 공급자 데이터의 전체 응답이나 비밀 키는 저장하지 않는다.

### 3.3 사진

- 원본 파일과 화면용 파생 이미지를 분리한다.
- 위치 메타데이터(EXIF)는 기본 제거한다.
- 비공개 버킷과 사용자·농장 단위 접근제어를 사용한다.
- 삭제 시 일기와 원본 파일의 참조 수를 확인한다.
- 업로드 실패 시 로컬 초안을 잃지 않고 재시도한다.

## 4. 자동 일기 초안

### 4.1 입력 허용 목록

- 오늘 출석 유형과 시각
- 사용자가 완료한 행동의 제목과 완료 시각
- 사용자가 직접 작성한 메모
- 사용자가 선택한 사진의 촬영일과 작물
- 저장 시점의 검증된 기상 요약과 경고
- 사용자에게 명확히 표시된 지역 토양 참고 또는 필지 검사 결과

### 4.2 생성 금지

- 사용자가 보지 않은 잎·열매·토양 상태
- 병해충 확진
- 작업 완료 추정
- 농약·비료·관수량의 신규 처방
- 필지값으로 확인되지 않은 지역 통계의 실측 표현
- 출석하지 않았는데 현장에 방문했다는 문장

### 4.3 생성 과정

1. 서버가 허용 목록 데이터만 구조화한다.
2. 결정론적 템플릿으로 최소 초안을 만든다.
3. AI가 활성화된 경우 사실을 추가하지 않는 범위에서 문장만 다듬는다.
4. 원문 데이터 키와 생성 문장을 함께 추적한다.
5. 사용자에게 `자동으로 정리한 초안`으로 표시한다.
6. 사용자가 저장해야 `SAVED`가 된다.

AI 실패 시 템플릿 초안을 사용하며 일기 작성 흐름을 막지 않는다.

## 5. 제안 API 계약

| 메서드·경로 | 목적 | 핵심 응답 |
| --- | --- | --- |
| `GET /api/farms/:farmId/checkins/today` | 오늘 출석 조회 | 상태, 유형, 시각, 이번 달 기록일 |
| `POST /api/farms/:farmId/checkins` | 출석 생성 | idempotent 출석 결과 |
| `PATCH /api/farms/:farmId/checkins/:id` | 유형·메모 수정 | 수정된 출석 |
| `DELETE /api/farms/:farmId/checkins/:id` | 출석 취소 | 삭제 상태와 이력 |
| `GET /api/farms/:farmId/diary?month=YYYY-MM` | 달력·피드 조회 | 날짜별 기록·사진·출석 표시 |
| `POST /api/farms/:farmId/diary/drafts` | 오늘 일기 초안 생성 | 근거가 연결된 초안 |
| `POST /api/farms/:farmId/diary` | 일기 저장 | 저장된 일기와 연결 정보 |
| `PATCH /api/farms/:farmId/diary/:id` | 일기 수정 | 버전이 증가한 일기 |
| `DELETE /api/farms/:farmId/diary/:id` | 일기 삭제 | 일기만 삭제, 출석·행동 유지 |
| `GET /api/farms/:farmId/seasons/:id/recap` | 시즌 회고 | 기록일, 사진, 행동, 수확 요약 |

모든 쓰기 API는 인증, 농장 소유권, CSRF 또는 동등한 보호, 요청 크기 제한, 속도 제한을 적용한다.

## 6. 기존 기능과 연결

### 6.1 오늘 할 일

- 행동 완료 후 `오늘 기록에 추가`를 기본 제안한다.
- 완료 작업은 일기에서 제거해도 행동 도메인에는 남는다.
- 건너뛴 작업을 완료로 표현하지 않는다.
- 사용자가 만든 작업과 분석 규칙 작업을 식별할 수 있어야 한다.

### 6.2 날씨·토양·예보

- 홈에서는 오늘의 행동에 필요한 한 줄만 표시한다.
- 일기에는 저장 시점 스냅샷을 사용한다.
- 토양 지역 통계는 `주변 토양 참고`, 필지 검정은 `내 농장 검사값`으로 구분한다.
- API 연결 실패가 출석과 수동 일기 저장을 차단하지 않는다.

### 6.3 사진 비교

- 같은 농장·작물·시즌의 사진만 기본 비교 후보로 제시한다.
- 촬영 간격, 구도, 밝기가 크게 다르면 `비교 참고`로 낮춘다.
- 결과는 `변화 관찰`, `재촬영 권장`, `비교 어려움` 중 하나이며 질병명이 아니다.

### 6.4 수확과 시즌

- 작물별 파종·정식일, 예상 수확 범위, 사진 보조 결과를 유지한다.
- 수확 완료 시 시즌 회고 초안을 만든다.
- 날씨로 변동된 예상 수확일과 실제 수확일을 모두 보존한다.

### 6.5 챗봇

- `오늘 기록에 메모 추가`, `내일 할 일 제안`, `지난주 사진 기록 요약`을 지원한다.
- 쓰기 요청은 제안 미리보기 후 사용자의 확인을 받아 저장한다.
- 챗봇이 출석을 대신 생성하거나 작업을 몰래 완료하지 않는다.

## 7. 오프라인과 동기화

- 출석과 일기는 네트워크가 없어도 로컬 초안으로 남긴다.
- 서버 시각과 농장 현지 날짜를 확인한 뒤 동기화한다.
- 동일 `client_request_id`로 재전송해 중복을 방지한다.
- 양쪽에서 수정된 일기는 사용자에게 버전 선택 또는 병합 화면을 제공한다.
- 사진 업로드 전까지 로컬 파일 삭제를 막고 업로드 상태를 표시한다.
- Supabase 공유 저장소가 미구성된 환경에서는 기기 간 동기화를 완료 기능으로 표시하지 않는다.

## 8. 개인정보와 보안

- 사진·일기·농장 위치는 비공개가 기본이다.
- AI에는 필요한 텍스트와 파생 정보만 전달하고 원본 위치·비밀키·계정 정보를 보내지 않는다.
- 사용자는 농장별 기록 내보내기와 삭제를 수행할 수 있어야 한다.
- 서비스 역할 키를 브라우저에 노출하지 않는다.
- Supabase 사용 시 사용자·농장·일기·사진 모두 RLS와 소유권 검증을 적용한다.
- 공유 기능을 추가할 때 공개 범위와 위치 흐림 처리를 별도로 설계한다.

## 9. 최소 테스트 계약

1. 같은 농장·날짜의 동시 출석 요청이 한 건만 생성된다.
2. 두 농장의 출석은 서로 영향을 주지 않는다.
3. 원격 확인이 현장 방문으로 렌더링되지 않는다.
4. 출석 생성·삭제 전후 작물 점수 결과가 동일하다.
5. 일기 삭제 후 출석과 완료 작업이 유지된다.
6. 과거 일기의 날씨 스냅샷이 API 갱신 후에도 동일하다.
7. AI 입력에 없는 관찰 문장이 초안에 추가되지 않는다.
8. AI 장애 시 템플릿 초안과 수동 저장이 동작한다.
9. 다른 사용자의 농장·일기·사진 접근이 거부된다.
10. 오프라인 재시도와 동시 요청에서 중복 일기·출석이 생성되지 않는다.
