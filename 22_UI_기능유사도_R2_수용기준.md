# UI 기능 유사도 R2 수용 기준

## 목적

목표 시안과 비슷하게 보이는 것만으로는 통과하지 않는다. 사용자가 누를 수 있는 모든 핵심 조작은 실제 데이터 조회·저장·상태 변경 또는 명확한 이용 불가 상태로 이어져야 한다.

## 기능 유사도 산정

- 기능 항목 20개를 같은 가중치로 평가한다.
- `동작함`: 실제 API 또는 기기 저장소에 연결되고 성공·실패 상태가 화면에 표시된다.
- `정직한 보류`: 외부 API가 비활성일 때 가짜 결과를 만들지 않고 필요한 설정과 다음 행동을 표시한다.
- `실패`: 클릭해도 변화가 없거나, 단순 스크롤만 하거나, 고정 숫자·샘플값을 실제 결과처럼 표시한다.
- 통과: 20개 중 19개 이상(`95%`) 동작하며 P0/P1 결함이 0개다.

## 검증 항목

| # | 사용자 기능 | 통과 조건 |
|---:|---|---|
| 1 | 저장 농장 복원 | 농장 설정 후 새로고침해도 같은 농장이 자동 복원된다. |
| 2 | 농장 추가/조건 변경 | 기존 농장을 잃지 않고 새 농장을 추가하거나 조건을 수정할 수 있다. |
| 3 | 복수 작물 전환 | 작물 탭을 누르면 해당 분석·할 일·위험·토양 해석으로 바뀐다. |
| 4 | 분석 새로고침 | 최신 공공자료로 분석을 다시 요청하고 갱신 시각이 바뀐다. |
| 5 | 현재 상태 상세 | 토양·날씨의 원인, 위험, 행동, 재확인 시점을 실제 분석값으로 보여준다. |
| 6 | 오늘 할 일 생성 | 분석 규칙에서 나온 행동을 농장·작물·작기 범위로 저장한다. |
| 7 | 개별 완료 | 완료 후 서버 상태가 `DONE`으로 바뀌고 재조회 뒤에도 유지된다. |
| 8 | 개별 건너뛰기 | 사용자 확인 후 서버 상태가 `SKIPPED`로 바뀌고 유지된다. |
| 9 | 일괄 완료 | 열린 행동을 순차 저장하고 일부 실패 시 성공한 항목과 실패를 구분한다. |
| 10 | 전체 작업 보기 | 오늘/당분간/완료 기록의 표시 범위를 실제로 전환한다. |
| 11 | 7일 위험 그래프 | 실제 예보 배열로 최고·최저기온, 강수 가능성, 작물 기준을 표시한다. |
| 12 | 주의 항목 가이드 | 날짜별 주의 항목에서 원인·위험·행동·재확인 가이드를 연다. |
| 13 | 위험 알림 수 | 고정 배지가 아니라 현재 분석에서 확인된 위험 개수를 표시한다. |
| 14 | 토양 상세 | 필지 검사값과 지역 통계를 구분하고 실제 토양 가이드를 연다. |
| 15 | 무료 토양검정 연결 | 최근 필지 검정 이력이 없을 때만 무료 검사 안내로 이동한다. |
| 16 | 사진 기록 | 촬영 조건 확인 후 작물·작기별 사진을 저장하고 삭제·비교할 수 있다. |
| 17 | 시즌 마감 | 저장한 사진과 완료 행동 수를 기준으로 시즌 회고를 만든다. |
| 18 | 위성 필지 | 위성 기능이 준비된 경우 필지 저장·관측 새로고침을 수행하고, 아니면 가짜값 없이 보류한다. |
| 19 | AI 도우미 | 현재 분석 ID로 질문하고 근거 기반 답변과 사용자 확인형 할 일 제안을 제공한다. |
| 20 | 메뉴 이동 | 오늘 할 일·작물·위험·위성·기록·설정 메뉴가 각 실제 기능으로 이동한다. |

## 금지 요소

- 고정 알림 개수, 하드코딩 점수, 샘플 토양·예보값
- API 호출 없이 성공한 것처럼 바뀌는 체크 상태
- 기능처럼 보이지만 같은 위치로만 스크롤되는 중복 버튼
- 외부 API가 꺼져 있을 때 임의 데이터로 채우는 폴백
- 사용자가 볼 필요 없는 내부 규칙 ID·오류 코드의 대시보드 노출

## 실제 화면 수용 조건

1. 데스크톱과 390px 모바일에서 핵심 기능을 키보드와 터치로 조작할 수 있다.
2. 완료·건너뛰기·사진 저장은 새로고침 후에도 결과가 유지된다.
3. 위험 가이드와 토양 상세는 기술 코드가 아니라 `원인 → 위험 → 행동 → 재확인` 순서로 읽힌다.
4. API 비활성·실패·빈 결과가 각각 구분되어 표시된다.
5. 자동 테스트와 실제 자연스러운 사용자 흐름 검증을 모두 통과한다.

## 2026-08-01 실행 결과

- 기능 수용 검사: **19/20 통과(95%)**
- 실제 성공 흐름: 인천 남동구 위치 검색 → 사과·오이 복수 분석 → 7일 예보·토양 표시 → AI 도우미에서 사용자 할 일 추가 → 일괄 완료 저장 → 사진 비공개 저장 → 시즌 마감·복원 → 가이드·메뉴 이동
- API 응답: 세션·사전점검·위치·분석·할 일 조회/생성/수정에서 확인된 HTTP 실패 0건
- 자동 검사: UI 105/105, 백엔드 332/332, 백엔드 정적 검사 109개 파일, 배포 빌드 통과
- 남은 1개: 위성 화면과 안전한 비활성 상태는 동작하지만 현재 실행 환경의 외부 위성 제공자 연결이 준비되지 않아 실제 NDVI 새로고침은 보류
- 화면 증거:
  - `output/UI_FUNCTIONAL_PARITY_DASHBOARD.png`
  - `output/UI_FUNCTIONAL_PARITY_GUIDE.png`
  - `output/UI_FUNCTIONAL_PARITY_COMPLETED.png`
