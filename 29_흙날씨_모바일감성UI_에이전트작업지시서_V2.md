# 흙날씨 모바일 감성 UI 에이전트 작업지시서 V2

> 기준 문서: `23_`, `26_`, `28_` V2 문서
> 작업 방식: 역할별 소유 범위를 분리하고 구현자와 승인자를 분리한다.

## 1. 공통 목표

- 모바일에서 가장 먼저 보이는 화면을 따뜻한 농장 다이어리로 전환한다.
- 하단 내비게이션은 `대시보드·할 일·다이어리·설정` 4개로 고정한다.
- 대시보드 중앙에 선택 작물의 생육 5단계와 환경을 합성한 원형 장면을 표시한다.
- 화면 감성은 썸원의 하루 한 번의 의식, 성장 기대, 파스텔 미니어처 세계를 참고하되 자산·캐릭터·레이아웃을 복제하지 않는다.
- 기존 날씨·토양·예보·작물 분석과 할 일·다이어리 기능을 유지한다.

## 2. 비주얼 아티스트

### 책임

- 5작물×5생육단계의 일관된 작물 자산
- 맑음·흐림·비·강수·바람·태풍·고온·가뭄·눈 환경 레이어
- 크림·살구·세이지·버터 옐로 중심의 독자적 아트 시스템
- 투명 합성과 작은 모바일 크기에서의 식별성 검수

### 소유 범위

- `ui-integration/diary-scene-assets/`
- 해당 폴더의 자산 README

### 금지

- 썸원 반려몽·방·아이콘의 형태 복제
- 얼굴이 있는 작물 캐릭터
- 작물별 실제 형태를 무시한 공통 새싹 자산
- 배경과 작물을 한 장으로 합쳐 환경 전환을 막는 자산

### 제출물

- 앱용 자산과 마스터 자산
- 파일명·기준점·합성 순서
- 최종 이미지 생성 프롬프트
- 256px·128px·64px 식별성 검수 화면

## 3. 프론트엔드·모션 개발자

### 책임

- 모바일 하단 4탭
- 원형 생육·환경 레이어 컴포넌트
- 백엔드 상태·원인·행동과 시각 장면의 단일 매핑
- CSS transform·opacity 기반 모션
- 모션 감소·오프라인·자산 오류 대체 화면
- 기존 기능의 보존과 재배치

### 소유 범위

- `08_흙날씨진단_UI_샘플.html`
- `ui-integration/ui-shell.mjs`
- `ui-integration/backend-client.mjs`
- `ui-integration/dashboard-workspace.css`
- 직접 관련된 UI 테스트

### 금지

- 프론트에서 새로운 농업 위험 기준 계산
- 날씨만으로 식물을 실제 시듦·병해 상태로 변경
- API 결측을 맑음·양호로 대체
- 모바일에서 다섯 번째 하단 탭 추가
- 기존 분석·할 일·다이어리 데이터를 더미로 교체

### 제출물

- 모바일·데스크톱 실제 화면
- 5작물·9환경 상태 전환 검증
- reduced-motion 화면
- 전체 UI 테스트와 빌드 결과

## 4. 독립 UI 감사관

### 책임

- 구현에 참여하지 않고 실제 실행 화면만 평가
- 360×800, 390×844, 430×932 모바일 검증
- 감성 품질과 농업 정보 정확성을 별도 평가
- 기능 존재 여부보다 3초 이해와 5초 출석 과업 평가

### 평가 항목

| 항목 | 최소 기준 |
| --- | ---: |
| 첫 화면 목적 이해 | 9.0/10 |
| 썸원형 따뜻한 감성의 독자적 번역 | 9.0/10 |
| 원형 장면 시각 완성도 | 9.0/10 |
| 상태·원인·행동 가독성 | 9.0/10 |
| 4탭 조작 직관성 | 9.0/10 |
| 생육·환경 데이터 정합성 | 9.0/10 |
| 접근성·모션 안전 | 9.0/10 |

### 불합격 조건

- P0/P1 결함 1건 이상
- 작물·환경 장면과 큰 상태 문구 불일치
- 출석이 작물 상태점수에 영향을 줌
- 식물 실제 상태와 예보 기반 연출을 혼동하게 함
- 모션 감소 설정에서 정보 손실
- 기존 기능 또는 실제 API 흐름 회귀

## 5. 통합 담당

- 작업 전후 `git status`와 파일 소유 범위를 확인한다.
- 아티스트 자산을 프론트 컴포넌트에 연결한다.
- 자산 누락·경로·용량·저작권·비밀정보를 검토한다.
- 실제 앱의 자연스러운 흐름으로 출석, 작물 전환, 할 일, 다이어리, 설정을 검증한다.
- 구현자가 자신의 화면을 최종 승인하지 않도록 독립 감사 결과를 반영한다.

## 6. 최종 완료 조건

1. V2 문서와 실제 화면 구조가 일치한다.
2. 5작물의 현재 생육단계가 각각 식별된다.
3. 환경 상태와 상호작용이 부드럽고 텍스트를 가리지 않는다.
4. 썸원의 감성 원리는 느껴지되 화면 복제로 보이지 않는다.
5. 기존 분석 기능과 실제 데이터 계약의 회귀가 없다.
6. 전체 테스트·빌드·실제 모바일 화면 검증이 통과한다.
7. 독립 감사 모든 항목 9.0 이상, P0/P1 0건이다.
