# 팀 변경분 비교 검토

## 비교 기준

- 사용자 최신 기준: `bb1922f1853ce91cdf473b47efbba6d77c894bb1`
  (`origin/codex/audit-remediation`)
- 팀 제출 기준: `a936f2810cfa4a91b7dce346d9698a90dea5f5d8`
  (`seowon507-pixel/heuknalssi`의 `main`)
- 비교 방법: 저장소 전체가 아니라 두 Git 트리의 파일 차이만 비교

두 저장소는 공통 Git 조상이 없다. 팀 저장소가 기존 프로젝트를 새 루트
커밋으로 가져간 형태이므로 커밋 수나 전체 파일 수를 팀 성과로 세지 않았다.
`git diff` 기준 순수 차이는 45파일, 25,345줄 추가, 248줄 삭제이며, 큰
추가량 대부분은 기후평년 JSON과 원본 엑셀이다.

## 사용자 코드로 분류한 것

다음은 두 저장소에 공통으로 존재하므로 팀 추가로 평가하지 않았다.

- 세션·CSRF·소유권·멱등성·요청 크기 제한
- 5개 작물 규칙 엔진과 `HOLD/PARTIAL/READY` 상태 모델
- 카카오 위치 후보 확인
- 기상청 단기·중기예보, ASOS, 토양 V2/V3 어댑터 기반
- 원인 → 위험 → 행동 → 재확인 구조
- Google AI 기반 근거 제한 도우미와 결정론적 대체 답변
- 복수 작물 온보딩과 날짜 기준 생육단계 추천
- 대시보드·마이페이지의 기본 제품 구조

## 팀이 순수하게 추가·변경한 부분

| 팀 변경 | 판단 | 이유 |
|---|---|---|
| 1991–2020 기후평년 엑셀·내장 JSON·변환기 | 좋음 | 변하지 않는 30년 평년값을 매번 외부 호출하지 않아 장애와 호출량을 줄임 |
| 사과·배 연간 기후 집계 | 좋음 | 월별 데이터만 있던 어댑터와 연간 규칙의 계약 불일치를 해결 |
| 평창군을 대관령 ASOS 지점에 매핑 | 좋음 | 이름이 같은 AWS 지점을 잘못 쓰는 위험을 피함 |
| 토양도 V3 태그·빈 응답 처리 수정 | 좋음 | 필드 계약을 맞추고 자료 없음과 스키마 변경을 구분 |
| 사용자 토양검정 결과 등록 | 매우 좋음 | 지역 평균과 내 밭 실측을 분리하려는 문제 정의에 직접 부합 |
| 토지이용별 토양화학성 보조 규칙 | 조건부 좋음 | 작물별 pH와 분리한 점은 좋지만 일반 토지이용 기준이므로 보조 근거만 사용해야 함 |
| PWA·앱 실행 중 로컬 알림 | 보통 | 설치성과 재방문성은 좋아지지만 완전 종료 상태 푸시는 아님 |
| Vercel 배포 래퍼 | 보통 | 시연은 쉬워졌지만 메모리 세션이라 서버리스 인스턴스 전환에 취약 |
| Supabase 기기 이관 코드 | 미완성 | 호출 코드는 있으나 테이블·RLS·RPC 마이그레이션과 외부 통합 검증이 없음 |
| SmartFarm 전면 비활성화 | 회귀 | 공급자 오류를 피했지만 키가 정상화돼도 절대 사용할 수 없어 원래 제품 방향과 어긋남 |
| 구현 기획서 | 수정 필요 | 실제 구현보다 강한 “전수 대조”, “전부 라이브”, “보안 백업 완성” 표현이 있었음 |

## 팀 기여 평가

| 항목 | 팀 변경분 점수 | 평가 |
|---|---:|---|
| 문제 정의 적합성 | 8.5/10 | 내 밭 토양검정과 기후평년 보강은 핵심 과제에 정확히 맞음 |
| 도메인 안전성 | 8.0/10 | 지역 평균·실측 분리와 결측 보존은 좋음 |
| 기술 재현성 | 5.5/10 | Supabase DB 구조가 없어 새 환경에서 백업 기능을 재현할 수 없음 |
| 운영 안정성 | 5.5/10 | 서버리스에서 메모리 세션·분석 저장을 그대로 사용 |
| 사용자 가치 | 7.5/10 | 토양검정과 알림은 유용하지만 저장·알림 한계를 더 명확히 해야 함 |
| 문서 신뢰성 | 5.0/10 | 검증하지 않은 범위를 완료처럼 기록한 부분이 감점 요인 |

팀 작업이 전체 프로젝트를 망치거나 “비전공자 티가 나는 코드”로 만든 것은
아니다. 특히 기후평년·토양검정·필지 토양 수정은 실제로 수준을 높였다.
감점 위험은 기능 수준보다 **완료 범위를 과장한 문서와 운영 구조의 미완성**에
있었다.

## 이번 검토에서 수정한 팀 추가분

1. SmartFarm을 전면 비활성화하지 않고, 사전점검이 `READY`이며 백엔드 지원
   작물·재배환경일 때만 별도 참고자료로 요청하도록 변경했다.
2. Supabase 비밀 키를 공개 키라고 잘못 설명한 환경변수 주석을 수정했다.
3. DB 마이그레이션이 없는 기기 이관 기능은 `CONFIGURED_UNVERIFIED`로
   표시하고 UI에서 비활성화했다.
4. 계정키 정규화·해시·허용 필드·크기 제한·RPC 호출·오류 축약 테스트를
   추가했다.
5. 알림 미지원 브라우저에서 `Notification` 참조 오류가 나지 않도록 모든
   저장·시험 제어를 함께 비활성화했다.
6. Vercel 빌드에 분리된 UI 셸 파일을 포함하고 CSP·권한·프레임 방지 헤더를
   추가했다.
7. 토양화학성 출처를 흙토람의 실제 표 페이지로 구체화했다.
8. 검정값이 기준 안이라는 이유만으로 처방서 시용량을 무조건 따르라는
   문구를 제거하고 검정기관·농업기술센터 확인으로 바꿨다.
9. “사진은 기기에 저장된다”는 오해를 막고 미리보기만 하며 저장·전송하지
   않는다고 정확히 표시했다.
10. 실제 검증보다 강했던 기획서 표현과 Vercel·PWA·DB 한계를 수정했다.

## 남은 필수 작업

### 배포 전

- Vercel의 세션·위치 후보·분석 저장을 공유 저장소로 교체
- Supabase를 계속 쓸 경우 공식 마이그레이션으로 테이블·RLS·함수 권한을
  저장소에 포함하고 새 프로젝트에서 처음부터 재현
- 실제 배포 환경에서 모든 외부 API별 성공·결측·인증 실패 상태 재검증
- SmartFarm 공급자 키와 지원 코호트를 실제 응답으로 다시 확인

### 검증 상태

- UI 테스트: 40개 통과
- 백엔드 테스트: 251개 통과
- 프로젝트 검사: 83파일 통과
- Vercel 정적 빌드: 통과
- 기후평년 변환: 219지점, 16메트릭, 평균기온 12개월 완전 지점 219개
- 샘플 실제 HTTP 흐름: 세션 → 위치 → `LAND_SEARCH` 분석 → 근거 제한
  도우미 응답 통과
- 실제 브라우저 시각 검증: 로컬 주소에 대한 브라우저 보안 정책 때문에
  이번 실행에서는 수행하지 못함
