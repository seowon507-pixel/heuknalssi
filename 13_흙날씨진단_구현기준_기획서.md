# 흙날씨 농지 진단 — 구현 기준 기획서

> 이 문서는 **실제로 구현·검증된 것만** 적는다.
> 계획 단계 문서는 [`07_흙날씨진단_100점_최종기획서.md`](./07_흙날씨진단_100점_최종기획서.md),
> 제품 요구사항은 [`PRODUCT_FIRST_PRD.md`](./PRODUCT_FIRST_PRD.md)를 본다.
>
> 기준일 2026-07-27 · 배포 https://heuknalssi.vercel.app

---

## 0. 한 문장

**농장 주소만 넣으면, 기상청·농촌진흥청 공개 데이터로 오늘 무엇을 먼저 해야 하는지 근거와 함께 알려 주는 서비스.**

### 현재 제공 범위

- 제품은 목적이 다른 두 흐름을 분리한다. `재배 준비 진단`은 심기 전
  농지·작물·재배환경을 비교하고 준비할 일을 안내하며, `재배 관리`는
  이미 심은 작물의 현재 위험과 오늘 할 일을 안내한다.
- 재배 관리는 작물별 파종·정식·개화 등 사용자가 아는 기준일을 받아
  현재 단계, 다음 단계, 수확 예상 **기간**, 수확 준비 시작일을 표시한다.
  날짜를 모르면 작물·작형의 검수 범위로 예상하되 실제 생체 상태나 확정
  수확일로 표현하지 않는다.
- 상세 주소를 선택하면 필지 토양검정·토양도와 주소 지점 기상을 우선하고,
  시·군·구까지만 선택하면 지역 참고 분석으로 계속 진행한다. 두 범위를
  같은 정밀도의 결과처럼 표시하지 않는다.

---

## 1. 왜 만들었나

농민이 기상·토양 정보를 얻기 어려워서가 아니다. **정보는 많은데 "그래서 오늘 뭘 해야 하나"로 번역되지 않아서**다.

기상청 예보는 시·군 단위로 나오고, 토양 통계는 법정동 평균이며, 작물별 적정범위는 별도 문서에 있다. 이 셋을 맞춰 보는 일을 농민에게 시키면 아무도 쓰지 않는다.

이 서비스는 그 대조를 대신한다. 그리고 **대조할 수 없는 것은 대조할 수 없다고 말한다.**

---

## 2. 설계 원칙 — 이것이 제품의 정체성

이 서비스의 핵심 경쟁력은 기능 개수가 아니라 **틀린 말을 하지 않는 것**이다.

| 원칙 | 구현 |
|---|---|
| **없는 값을 지어내지 않는다** | 결측은 결측으로 표시. 평균·보간으로 채우지 않음 |
| **모든 판정에 출처가 있다** | 규칙 103개 전부 `sourceUrl`·`sourcePageOrTable`·`reviewedAt` 보유. 없으면 활성화 자체가 안 됨 |
| **계약이 바뀌면 멈춘다** | 어댑터마다 계약 버전 고정. 응답 형식이 바뀌면 `SCHEMA_CHANGED`로 멈추고 옛 해석을 강행하지 않음 |
| **지역 평균과 내 밭 실측을 섞지 않는다** | `measurementBasis`로 근거를 구분. 지역 통계는 `usedForDecision: false`로 별도 기록 |
| **신선도를 부풀리지 않는다** | 관측 7일이 안 차면 `PARTIAL`. 창을 옮겨 채우지 않음 |

### 이 원칙 때문에 제한한 것

- 종합 점수 100점 만점 — 근거 없는 가중치라서 만들지 않음
- 스마트팜 벤치마크 — 핵심 판정과 분리하고, 사전점검에서 실제 연결이 확인된 지원 조합에만 요청
- pH 외 양분의 작물별 기준 — 검수된 출처를 찾기 전까지 표시만 하고 판정하지 않았음 (이후 흙토람 표 확보 후 반영)

---

## 3. 대상 작물 5종

**사과 · 배 · 오이 · 감자 · 상추**

재배형태별로 판정 범위가 다르다.

| 작물 | 노지 | 시설(토경) | 시설(수경) |
|---|---|---|---|
| 사과 · 배 | ✅ 과수 기준 | — | — |
| 감자 | ✅ 밭 기준 | — | — |
| 오이 · 상추 | ✅ 밭 기준 | ✅ 시설 기준 | 토양 판정 제외 |

수경재배는 토양이 판정 대상이 아니므로 `NOT_APPLICABLE`로 명시한다. 억지로 점수를 매기지 않는다.

---

## 4. 데이터 소스 — 구현·로컬 계약 검증 상태

| 소스 | 제공 | 역할 | 상태 |
|---|---|---|---|
| 기상청 기후평년 1991–2020 | 기상청 배포 엑셀 → 저장소 내장 | 작물 적합 기후 대조 | ✅ |
| 기상청 ASOS 일관측 | 공공데이터 `1360000` | 최근 7일 실황 | 어댑터·계약 테스트 완료, 실제 연결은 키·기관 응답에 따름 |
| 기상청 단기예보 | 공공데이터 `1360000` | 3일 위험 신호 | 어댑터·계약 테스트 완료, 실제 연결은 키·기관 응답에 따름 |
| 기상청 중기예보 | 공공데이터 `15059468` | 10일 위험 신호 | 어댑터·계약 테스트 완료, 실제 연결은 키·기관 응답에 따름 |
| 농경지화학성 통계 V2 | 공공데이터 `15144685` | 법정동 pH 면적 분포 | 어댑터·계약 테스트 완료, 실제 연결은 키·기관 응답에 따름 |
| 토양도 기반 토양특성 V3 | 공공데이터 `15144225` | 필지 배수·토심·토성 | 어댑터·계약 테스트 완료, 빈 응답은 `NO_DATA` 처리 |
| 사용자 등록 토양검정 | 사용자 입력 | **내 밭 실측 pH·양분** | ✅ |

### 평년값을 API에서 정적 파일로 바꾼 이유

평년값은 **30년 고정값**이다. 실시간 호출은 불필요한 의존성이고, 기상청 API 허브 승인·레이트리밋·장애에 서비스가 묶인다.

기상청이 배포한 「우리나라 기후평년값(월별) 1991–2020」 엑셀을 저장소에 넣고 빌드 시 JSON으로 변환한다. 의존성 0개 원칙을 지키려고 xlsx 파서를 설치하지 않고 zlib만으로 ZIP+XML을 직접 읽는다.

**219개 지점 × 16개 항목**을 변환했다. 변환기는 헤더 형식과 필수
평균기온 완전성을 검사한다. 원본과 결과를 서로 다른 구현으로 재계산하는
독립 교차검증은 아직 없으므로 “17,484셀 불일치 0”으로 표현하지 않는다.

---

## 5. 판정 규칙 103개

| 모듈 | 개수 | 근거 |
|---|---|---|
| CLIMATE | 38 | 농사로 작목별 재배 기준 |
| SOIL | 49 | 농사로(pH 7) + 흙토람 적정범위 표(양분 42) |
| FORECAST | 16 | 농촌진흥청 고온기 관리요령 등 |

규칙마다 출처 URL·표 위치·검수일이 붙어 있고, 하나라도 빠지면 활성화되지 않는다. 이 게이트는 테스트로 강제한다.

### 토양 판정이 실측으로 바뀌는 순간

기본값은 **지역 통계**다. 농경지화학성 V2는 법정동의 pH를 구간별 면적으로 준다. 작물 최적범위가 구간 경계에 걸치면 "경계 면적"이 커지고, 30%를 넘으면 판정을 보류한다.

사용자가 **비료 사용 처방서 값을 등록**하면 판정 근거가 바뀐다.

```
등록 전  → 지역 pH 기준 밖 면적 67%   (근거: REGIONAL_STATISTICS)
등록 후  → 내 밭 pH 6.1 · 7개 항목 모두 기준 안  (근거: USER_SOIL_TEST)
```

구현상 실측값은 **폭이 없는 점 구간**(`lower === upper`)으로 표현한다. 도메인이 이미 지원하던 형태라 판정 로직은 한 줄도 바꾸지 않았다. 면적 비율을 꾸며내지 않으므로 경계 불확실이 0이 되고 적합/이탈이 분명히 갈린다.

---

## 6. 사용자 흐름

```
① 주소 입력          "경상북도 안동시 퇴계로 115"  ← 번지까지 넣어야 필지 확정
② 작물·재배조건 선택
③ 분석               기상·토양·예보 동시 조회
④ 대시보드           오늘 먼저 할 일 + 근거 + 출처
⑤ 검사 결과지 등록    마이페이지 → 값 입력 → 자동 재분석
⑥ 위험 알림          최신 자료에서 새 주의 신호가 확인된 경우에만 발송
```

### 왜 번지까지 필요한가

시·군까지만 넣으면 **필지가 특정되지 않는다.**

| | 시·군만 | 번지까지 |
|---|---|---|
| 단기예보 | ✅ 대표 격자 | ✅ |
| 토양도 V3 | ❌ PNU 불가 | ✅ |
| ASOS 관측 | ❌ 지점 불확정 | ✅ |

이건 결함이 아니라 **정확도의 대가**다. 다만 화면에서 그 이유를 알려 줘야 한다.

---

## 7. 접근성 — 어르신이 실제로 쓸 수 있는가

주 사용자가 고령 농민이라는 전제에서 설계했다.

- **흙 검사 무료 안내** — 무료·15일·5군데 채취 500g("우유팩 반 정도")을 번호 단계로 안내. 주 사용 지역을 읽어 "안동시 농업기술센터"처럼 표시
- **전화 우선** — "인터넷이 어려우시면 시청·군청에 전화해서 '농업기술검정실'을 물어보세요"
- **글자 크게 / 고대비** 설정이 모든 화면에 적용
- **다시 설정할 필요 없음** — 검사 결과·지역·분석 조건이 기기에 저장되어 새로고침·재방문에도 유지
- **휴대폰을 바꿔도** 계정 열쇠 12자리(`ZAGP-7YCW-FZFA`)로 농장 위치·작물·재배 기준일·농장별 토양검정 입력·알림 설정을 이관. 분석 결과·행동 이력·사진·리포트는 현재 기기/세션 범위이므로 이관된다고 안내하지 않는다. `0/O`, `1/I/L`처럼 헷갈리는 글자는 제외

---

## 8. 알림

주의사항은 고정 문구가 아니다. 공공 API의 새 발표자료 또는 사용자의 새로고침으로 분석이 갱신됐을 때, 현재 농장·품목에 적용되는 행동 가능한 위험이 확인된 경우에만 대시보드와 알림에 표시한다.

- 위험이 없으면 `주의사항` 영역과 알림을 표시하지 않는다.
- 동일 품목·위험·유효기간의 알림은 반복하지 않는다.
- 위험이 새로 생기거나 날짜·심각도·행동이 바뀐 경우에만 새 알림으로 본다.
- 알림에는 대상 품목, 위험 날짜, 원인, 필요한 행동, 재확인 시점을 포함한다.
- 결측·오래된 자료·부분 실패로는 위험 또는 안전을 확정하지 않는다.

PWA 알림 본문은 **최신 분석에서 새로 확인된 주의사항의 첫 행동**이다.

```
[시험] 오늘 먼저 할 일 — 주의 근거 우선 확인
경북 안동시
```

분석이 없거나 새 위험이 없으면 알림을 보내지 않는다.

**한계를 명확히 한다.** 앱을 완전히 종료한 상태의 정시 발송은 서버 푸시(Web Push)가 있어야 한다. 현재는 앱이 실행 중이거나 앱을 열 때 밀린 알림을 하루 한 번 보낸다. 화면에도 그렇게 적어 두었다.

---

## 9. 시스템 구성

```
브라우저(PWA)
  │  정적 UI + 서비스워커
  ▼
Vercel 서버리스 함수  ──→  backend-v3 (Node 22, 외부 의존성 0개)
  │                          ├ 어댑터: 카카오 · 기상청×3 · 토양×2
  │                          ├ 도메인: 규칙 103개 판정
  │                          └ 평년값: 내장 JSON
  ▼
선택형 외부 저장소  ──  기기 이관 백업 인터페이스(현재 DB 스키마 미포함)
```

**런타임 외부 패키지 의존성 0개.** xlsx 변환, XML 파싱, ZIP 해제는
Node 내장 모듈로 처리한다. Web Push는 아직 구현하지 않았다.

Vercel 배포는 현재 세션·위치 후보·분석 결과를 프로세스 메모리에 둔다.
따라서 인스턴스가 바뀌거나 재시작되면 다단계 흐름이 끊길 수 있는
**해커톤 시연용 구조**이며, 상용 운영 전 공유 저장소로 교체해야 한다.

### 계정 열쇠 보안

열쇠 **원문을 백엔드가 저장소에 전달하지 않고** SHA-256 해시만 전달하며,
복원은 분당 5회로 제한한다. 다만 저장소 테이블·RLS·RPC 함수의 마이그레이션은
현재 저장소에 포함되어 있지 않다. 따라서 설정값이 있더라도
`CONFIGURED_UNVERIFIED`로 표시하고 화면 기능을 비활성화한다. RLS와 함수 권한을
검증한 마이그레이션이 추가되기 전에는 “보안 백업 완성”으로 판정하지 않는다.

---

## 10. 검증

| 항목 | 결과 |
|---|---|
| 백엔드 테스트 | 전체 테스트 명령으로 재검증 |
| UI 통합 테스트 | 전체 테스트 명령으로 재검증 |
| 프로젝트 검사 | `npm run verify`로 재검증 |
| 평년값 변환 | 헤더·지점 수·평균기온 완전성 검사 |
| 외부 API | 실행 시점의 키·기관 응답별 상태를 별도 보고 |
| 브라우저 E2E | 실제 화면 자연 흐름으로 별도 검증 |

브라우저 검증은 헤드리스 Chrome을 DevTools 프로토콜로 직접 몰아 수행한다. 서비스워커 등록, 알림 권한, 10초 시험 알림 실제 표시, 새로고침 후 세션 복원까지 확인한다.

---

## 11. 알려진 한계

정직하게 적는다. 심사에서 물어볼 것들이다.

| 한계 | 성격 | 대응 |
|---|---|---|
| **검수 지역 7곳** (이천·안동·부여·영주·문경·의성·평창) | 확장 필요 | 중기예보 구역코드표 241개 확보 완료. 시·군별 관측지점 매칭만 남음. 한 곳당 약 10분 |
| **ASOS 발표 지연** | 기관 특성 | 기상청이 전날 관측을 당일 새벽엔 올리지 않음. 낮에 자동 해소. 억지로 채우지 않고 `PARTIAL`로 표시 |
| **앱 종료 시 알림 미발송** | 웹 제약 | Web Push 연동 필요 |
| **세션·분석 결과 영속성 없음** | 서버리스 | 인메모리 저장. 다단계 흐름까지 공유 스토어 필요 |
| **기기 이관 DB 미포함** | 저장소 재현성 | 검증된 스키마·RLS·RPC 마이그레이션 전까지 UI 비활성화 |
| **스마트팜 조건부 연동** | 외부 데이터 | 사전점검 `READY`이고 지원 작물·재배환경일 때만 별도 참고자료로 요청 |
| **양분 판정 가중치** | 설계 판단 | 흙토람 표는 토지이용 구분 기준이라 작목별 pH보다 보조적. `SUPPORTING`으로 설정 |

---

## 12. 이번 구현에서 실제로 해결한 것

계획서에 없던, 구현 중 발견하고 고친 문제들이다.

1. **카카오 키 종류 오류** — JavaScript 키로는 서버 REST 호출이 401. `KA Header is required` 오류로 판별해 REST API 키로 교체
2. **토양도 V3 파라미터** — 요청은 `PNU_CD`, 응답은 `PNU_Cd`. 코드는 `PNU_Code`를 쓰고 있었다. 승인 문제로 오진했다가 명세를 직접 확인해 교정
3. **사과·배 기후 판정 불가** — 두 작물의 기후 규칙은 연간 집계인데 어댑터는 월별만 제공. 도메인 계층에서 월별 평년값으로 연평균을 파생 (안동 12.23℃ vs 사과 최적 8–11℃)
4. **평창군 관측지점** — 지점명이 같은 `526 평창`은 방재(AWS)라 ASOS 자료가 없다. 평창군 안에 있는 `100 대관령`(종관)을 사용
5. **토양도 빈 응답** — 임야·하천 필지는 값이 비어 온다. `SCHEMA_CHANGED`가 아니라 `NO_DATA`로 구분

---

## 13. 시연 시나리오 (3분)

```
0:00  주소 입력 "경상북도 안동시 퇴계로 115" → 사과 선택 → 분석
0:30  대시보드: 오늘 먼저 할 일 + 근거 + 출처 7개 공개
1:00  토양 카드 = "지역 pH 기준 밖 67%" · 아직 내 밭 값이 아님
1:15  마이페이지 → 흙 검사 무료 안내 (무료·15일·5군데)
1:40  검사 결과지 등록 → ddt.png 첨부 → 값 입력 → 저장
2:00  자동 재분석 → 토양 카드 "내 밭 pH 6.1 · 7개 항목 모두 기준 안"
2:20  알림 설정 → 시간 저장 → 10초 시험 알림 수신
2:40  자료 출처·판단 한계 확인
```

시연용 예시 처방서 `ddt.png`는 흙토람 과수 적정범위를 그대로 쓰되, **워터마크와 "실제 발급 문서가 아님" 문구**를 넣어 오인을 막았다.

---

## 14. 다음 단계

우선순위 순.

1. **검수 지역 전국 확대** — 기상청 지점 좌표 목록을 확보하면 최근접 매칭으로 자동화 가능
2. **토양검정 API 연동** (`15073569`) — 주소만으로 실측값 자동 조회. 검정 이력이 있는 필지에 한함
3. **Web Push** — 앱 종료 상태 정시 알림
4. **공유 저장소 도입** — 세션·후보·분석 결과 영속화
5. **기기 이관 마이그레이션** — 테이블·RLS·함수 권한·복원 테스트를 저장소에 포함

---

## 15. 근거 자료

- 기상청 우리나라 기후평년값(월별) 1991–2020
- 기상청 중기예보 구역코드 (2025.12 공식 표)
- 기상청 기상자료개방포털 관측지점 메타데이터
- 농촌진흥청 흙토람 토양검정 적정범위 (논·밭·과수·시설)
- 농사로 작목별 농작업일정 (사과·배·감자·오이·상추)
- 공공데이터포털 `15144685` `15144225` `1360000` `15059468`
