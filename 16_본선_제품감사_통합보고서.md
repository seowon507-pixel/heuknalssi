# 본선 제품감사 통합보고서

- 감사 기준: `demo1` / `127dfb3`
- 감사 일자: 2026-07-31
- 감사 방식: 백엔드·보안, 계산·농업 로직, UX·소비자, 챗봇·AI, 기능 QA, 해커톤 심사 6개 역할의 독립 평가
- 현재 판정: **CHANGES_REQUESTED**

## 1. 경영진 요약

현재 제품은 실제 공공데이터를 작물별 기준과 연결하고, 결과를 `원인 → 위험 → 행동 → 재확인 시점`으로 설명한다는 점에서 본선 진출 제품으로서 분명한 강점이 있다. 그러나 제품의 안전 원칙과 화면이 어긋나는 P0 결함이 확인됐다. 프론트엔드는 서로 다른 공간·시점·확실성을 가진 날씨와 지역 토양을 `60:40 생육점수`로 다시 합치며, 일부 작물은 재배기간을 확인하지 않은 채 현재 월 하나를 확정값처럼 분석한다. 이 두 문제는 자동 테스트 통과와 무관하게 심사 신뢰를 훼손한다.

본선 전 개선의 목적은 기능 수를 늘리는 것이 아니라 다음 세 가지를 증명하는 것이다.

1. 확인된 자료와 확인되지 않은 자료를 섞지 않는다.
2. 사용자가 3초 안에 오늘의 위험, 이유, 행동, 다시 볼 시점을 이해한다.
3. 외부 API가 일부 실패해도 성공한 작물과 자료는 보존한다.

## 2. 독립 감사 결과

| 역할 | 점수 | 판정 | 핵심 발견 |
| --- | ---: | --- | --- |
| 백엔드·보안 | 6.2/10 | CHANGES_REQUESTED | 서버리스 다중 인스턴스에서 세션·분석 상태가 공유되지 않음, 지속 저장소 없이 READY 과대표시 |
| 계산·농업 로직 | 5.6/10 | CHANGES_REQUESTED | 금지된 종합점수, 미확인 재배기간, 일반 생육단계를 확정 추천처럼 표시 |
| UX·소비자 | 6.4/10 | CHANGES_REQUESTED | 모바일 다중 농장 접근 부재, 저장 동의 불일치, 복원 로딩 잔류, 반복 텍스트 |
| 챗봇·AI | 6.0/10 | CHANGES_REQUESTED | 안전 분류·작물 불일치·질문 날짜 보존·대화 연속성의 명시적 처리 부족 |
| 기능 QA | 5.4/10 | CHANGES_REQUESTED | 다작물 직렬 처리와 부분 성공 폐기, 공급자별 상태를 전역 라벨이 가림 |
| 해커톤 심사 | 57/100 | CHANGES_REQUESTED | 핵심 기술은 좋으나 신뢰 위반 P0와 재배 전 문제 정의 공백 때문에 시연 승인 불가 |

소비자 모의평가 5종 평균은 6.1/10이었다. 초보 농업인 7.3, 고령 사용자 5.6, 다중 농장 사용자 4.8, 시설재배 사용자 6.2, 농업기술센터 상담사 6.8이었다. 실제 사람 평가가 아닌 시뮬레이션 결과다.

개별 근거는 `audit/agents/2026-07-31/`의 6개 보고서에 보존한다.

## 3. P0/P1 통합 결함 목록

### P0-1. 서로 다른 자료를 단일 생육점수로 합산

- 현상: 화면이 날씨 60%, 토양 40%로 `생육점수`를 산출한다.
- 위험: 지역 통계 토양, 필지 실측, 단기예보의 확실성이 다른데도 하나의 농장 상태 확정치로 오해된다.
- 조치: 단일 점수·등급·가중치 문구를 제거하고 `날씨 위험`, `토양 적합`, `가까운 예보`, `자료 상태`를 독립 축으로 표시한다.

### P0-2. 미확인 재배기간을 현재 월로 확정

- 현상: 오이·감자·상추 등에서 `current`가 현재 월 하나의 사용자 확인값으로 변환된다.
- 위험: 작기의 전체 기후 적합성을 현재 월 하나로 계산해 편향된 결론을 낸다.
- 조치: 검토된 작기 선택, 직접 월 범위, `잘 모름`을 명시한다. `잘 모름`이면 가까운 예보는 계속 제공하되 장기 기후 판단은 HOLD한다.

### P0-3. 서버리스 다중 인스턴스 상태 비공유

- 현상: 세션·후보·분석·멱등성 상태가 프로세스 메모리에 있다.
- 위험: 요청이 다른 인스턴스로 가면 정상 사용자가 세션·분석을 잃는다.
- 조치: 외부 공유 저장소 도입 전에는 배포 준비 상태를 READY로 표시하지 않는다. 이번 코드 개선에는 게이트웨이 완료 대기와 정직한 준비상태를 반영하며, 공유 DB 구축은 별도 인프라 작업으로 남긴다.

### P1-1. 다작물 요청의 전체 실패

- 현상: 작물을 직렬 분석하고 하나가 실패하면 앞서 성공한 결과도 화면에 표시하지 않는다.
- 조치: 작물별 독립 실행, 부분 성공 보존, 실패 작물명과 재시도 안내를 제공한다.

### P1-2. 공급자별 상태 은폐

- 현상: 기후평년·ASOS·단기·중기·토양의 개별 상태와 무관하게 전역 `실시간 분석`으로 표시된다.
- 조치: 전역 문구를 `자료별 확인`으로 바꾸고 결과 화면에서 사용된 자료의 LIVE/STALE/HOLD/ERROR와 기준 시각을 사용자 언어로 표시한다.

### P1-3. 저장 동의와 실제 동작 불일치

- 현상: 서버 저장 기능이 없으면 동의 UI를 숨기지만 브라우저 로컬 저장은 무조건 수행한다.
- 조치: `이 기기에 저장` 동의를 항상 표시하고 동의한 경우에만 저장한다. 저장 농장 삭제 경로도 같은 영역에서 제공한다.

### P1-4. 챗봇의 안전·문맥 결과 상태 부재

- 현상: 농약·비료·응급·사진 진단·다른 작물 질문과 날짜가 있는 질문을 명시적으로 분류하지 않는다.
- 조치: 자유 생성은 확대하지 않는다. 서버의 검증된 근거 목록 앞에 결정론적 안전 분류를 두고 `답변`, `추가 확인`, `안전상 제한`, `지원하지 않음` 결과를 반환한다.

## 4. 이번 개선 범위

### 즉시 구현

- 단일 생육점수와 60:40 가중치 제거
- 독립 축 상태 및 사용자용 자료상태 요약
- 재배기간 명시 입력과 미확인 HOLD
- 일반 생육단계의 `AI 예상` 과대표현 제거
- 다작물 부분 성공 보존
- 로컬 저장 동의 일치, 복원 로딩 상태 종료
- 모바일 농장 전환·추가 진입점과 작물별 접근성 이름
- 미구현 사진·센서 기능을 존재하는 기능처럼 보이는 문구 제거
- 챗봇 안전 분류, 작물 문맥 불일치, 후속질문 한계, 결과 상태
- 관련 회귀 테스트와 실제 화면 검증

### 외부 인프라가 필요한 후속 작업

- 서버리스 공유 세션·분석 저장소
- 계정 기반 다기기 농장·투두·사진 동기화
- 보존기간·삭제·소유권·감사로그가 포함된 사진 저장소

### 이번에 구현하지 않는 신규 기능

- 인공위성 필지 분석
- 사진 비교 및 시즌 회고
- 서버 동기화 투두리스트

이 기능들은 현 제품의 신뢰 결함을 닫은 다음 단계로 둔다. 특히 위성은 새로운 가중치가 아니라 `관측 시점·해상도·구름·필지 경계`가 명시된 별도 근거 축이어야 한다.

## 5. 측정 가능한 수용 조건

1. 화면·코드·테스트 어디에도 `생육점수`, `60:40`, 기후·토양 합산 함수가 없다.
2. 날씨·토양·예보는 각각 READY/PARTIAL/HOLD/ERROR 상태와 근거 범위를 유지한다.
3. 작기가 확인되지 않으면 장기 기후 결과는 HOLD이며 가까운 예보와 오늘 행동은 계속 제공된다.
4. 다작물 중 하나가 실패해도 성공 결과는 즉시 열리고 실패 작물만 식별된다.
5. 저장 동의가 꺼져 있으면 새 로컬 농장 설정을 저장하지 않는다.
6. 복원 성공 뒤 로딩 문구가 남지 않는다.
7. 챗봇이 농약·비료·응급·사진 진단에 확정 처방을 하지 않고 안전상 제한과 다음 확인 경로를 제공한다.
8. 데스크톱 1440px와 모바일 390px에서 핵심 행동, 근거, 자료 상태, 농장 전환, 챗봇이 잘리지 않는다.
9. 전체 자동 테스트, 실제 자연 흐름, 브라우저 콘솔 검사가 통과한다.
10. 서버 공유 저장소가 없는 배포는 `완전한 운영 준비`로 표시하지 않는다.

## 6. 본선 심사 관점의 우선순위

1. 신뢰 위반 P0 제거
2. 대표지역·5작물의 부분 실패 회복성
3. 시연 첫 화면의 행동 우선 위계
4. 챗봇 안전성과 검증된 근거 설명
5. 공유 저장소 구축
6. 투두 → 사진 기록 → 위성 순서의 신규 기능 확장

본선 시연은 `안동 사과` 한 농장을 기준으로 예보 위험, 작물별 임계값, 오늘 행동, 재확인 시점, 자료 한계를 보여준 뒤 같은 위치에서 다른 작물로 전환해 동일 날씨가 작물에 따라 다르게 해석되는 장면을 보여주는 것이 가장 설득력이 높다.
