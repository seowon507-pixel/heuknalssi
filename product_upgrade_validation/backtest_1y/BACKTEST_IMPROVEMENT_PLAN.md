# 흙날씨 백테스트 기반 개선 계획

- 작성 기준일: 2026-08-04
- 기준 백테스트: 2025-08-04~2026-08-03, 365일
- 대상: 검수 ASOS 8개 지점, 사과·배·오이·감자·상추
- 기준 산출물: `one-year-backtest-2025-08-04-2026-08-03.json`

## 1. 결론

현재 구현은 과거 관측을 이용한 규칙 재생과 결측 방어를 기계적으로 수행할 수 있다. 다만 아래 이유로 제품의 예측 정확도가 입증된 상태는 아니다.

1. 과거 격자 API `nph-dfs_shrt_grd` 활용 권한을 확인했고, 2025-08-04~2026-08-03의 1일·3일 선행 최저·최고기온 예보를 8개 검수 지역에서 수집·검증했다. 하루 24개 시간별 POP를 빠짐없이 수집해 최대 POP로 일 확률을 만드는 검증기도 추가했으며 실호출 표본에서 Brier 계산까지 확인했다. 5일 선행은 과거 중기예보 어댑터를 구현했지만 현재 키의 1.3 중기 육상·1.4 중기 기온 세부 권한이 403이라 전체 실행은 대기한다.
2. 실제 파종·생육단계·피해·수확일 정답이 없어 실제 수치는 아직 계산할 수 없다. 대신 결과·예측·알림 CSV 계약과 수확 범위 오차, 피해 이벤트 재현율, 알림 정밀도 평가기를 구현했다.
3. 기존 수확 일정 민감도 검사는 실제 작기 없이 임의의 90일 구간을 사용했다.
4. 수확 일정 보정 적용 334건 중 241건(72.16%)이 ±14일 상한에 도달했고, 지연 보정은 0건이었다.
5. 과거 시점의 필지 토양 스냅샷 자체는 아직 없지만, 검정일이 분석일 이전인 최신 스냅샷만 선택하고 3년을 넘긴 값은 점수에서 제외하는 시점 일치 평가기를 구현했다.

따라서 현재 판정은 `FORECAST_TEMPERATURE_AND_POP_PIPELINE_READY / FARM_OUTCOME_COLLECTION_READY_ACCURACY_NOT_PROVEN`이다. 예보 기온 오차와 기온 기반 위험 신호 정확도는 측정했고 강수·5일·실제 결과·토양 이력 평가 경로는 준비했지만, 아직 없는 정답이나 승인되지 않은 원천의 정확도 주장은 보류한다.

## 2. 개선 원칙

1. 현재 예보나 현재 토양값을 과거 날짜에 소급 적용하지 않는다.
2. 결측값은 0이나 평균으로 대체하지 않고 제외 사유와 함께 보존한다.
3. 작기와 생육단계가 없는 연중 임계값 발생률을 실제 사용자 알림률로 해석하지 않는다.
4. 날씨는 생육 속도에 대한 보조 근거이며 사진·센서 없이 생물학적 성숙을 확정하지 않는다.
5. 사용자가 확인한 작기·수확 범위가 자동 추정보다 우선한다.
6. 점수와 일정은 근거 강도 및 신뢰도를 별도로 노출한다.

## 3. P0 구현 범위

### 3.1 작물주기 기반 수확 일정 보정 v2

기존 방식은 관측기간 전체의 적산온도 차이를 일수로 환산해 ±14일로 자르는 방식이었다. v2는 다음과 같이 변경한다.

- 작물주기에 저장된 기준 수확 범위가 있을 때만 실제 일정 보정을 적용한다.
- 기준 수확 범위의 중간일까지 남은 기간과 관측/평년 적산온도 비율을 이용한다.
- 보정값으로 기준 수확 범위의 시작일과 종료일을 함께 이동한다.
- 최대 보정폭은 14일과 기준 수확 범위 폭의 25% 중 작은 값으로 제한한다.
- 이미 수확 범위가 끝났거나 작기가 `PLANNING`·`COMPLETED`이면 자동 보정을 적용하지 않는다.
- 결과에 기준 범위, 조정 범위, 적용 모드, 최대 보정폭, 신뢰도를 함께 기록한다.

이 제한은 정확도 보정값이 아니라 정답 데이터가 없는 상태에서 과도한 변동을 막는 제품 안전장치다.

일정 보정식은 다음과 같다.

```text
생육속도비 = 관측 적산온도 / 평년 적산온도
기준중간일 = 기준 수확 시작일 + (기준 수확 범위 폭 / 2)
남은일수 = max(0, 기준중간일 - 기준일)
원시보정일 = 남은일수 × (1 / 생육속도비 - 1)
최대보정일 = min(14, ceil(기준 수확 범위 폭 × 0.25))
최종보정일 = clamp(round(원시보정일 × 자료포함률), -최대보정일, +최대보정일)
조정 수확 범위 = 기준 수확 범위의 시작일·종료일 + 최종보정일
```

양수는 수확 예상 지연, 음수는 수확 예상 앞당김을 뜻한다. 이 값은 사진이나 실제 생체 측정 없이 성숙 여부를 확정하지 않는다.

### 3.2 작기·생육단계 필터

- 서비스의 위험 규칙은 요청의 현재 생육단계와 일치하는 규칙만 실행한다.
- 백테스트에서 생육단계 정답이 없는 규칙은 `stage_scoped_potential`로 분리하고 실제 알림으로 계산하지 않는다.
- 실제 작기 레코드가 확보되면 날짜별 CSV에 `season_id`, `cycle_status`, `growth_stage`, `rule_applicable`을 추가한다.
- 작기 종료 이후에는 신규 위험 행동과 수확 일정 보정을 만들지 않는다.

## 4. P1 데이터 검증 범위

### 4.1 과거 발행 예보

필요 데이터는 발행시각, 유효시각, 격자, 최저·최고기온, 강수확률이다. 과거 발행 예보 권한을 확인했으며 다음 중 기온 항목을 우선 계산했다.

사용할 공식 원천은 기상청 API허브의 `동네예보 격자자료 > 2.1 단기예보` (`nph-dfs_shrt_grd`)다. 최근 지점예보용 `getVilageFcst`는 운영 화면에는 계속 사용하지만 1년 백테스트의 과거 원천으로 사용하지 않는다.

- 1일·3일 선행 최고/최저기온 MAE·RMSE 완료
- 5일 선행은 단기예보 공식 범위 밖이므로 중기 과거 발행본 원천 확보 전까지 보류
- 강수 여부 정확도와 Brier score는 24개 시간별 POP가 모두 있는 날만 최대 POP로 일 단위 집계하여 계산. 일부 시간 결측일은 점수에서 제외
- 작물 위험 경보 정밀도·재현율
- 위험 발생 전 확보한 선행시간

예보 발행 이후에 생성된 관측값은 예측 입력으로 사용할 수 없다.

### 4.2 실제 농사 정답 데이터

최소 수집 항목은 다음과 같다.

- 농장·작물·작기 식별자
- 파종일 또는 정식일
- 생육단계 확인일
- 첫 수확일과 수확 종료일
- 피해 발생 여부와 확인일
- 사용자가 완료한 행동과 확인 사진

개인정보와 필지 원문 식별자는 분석용 내보내기에서 제외한다.

구현된 빈 CSV 계약과 실행 방법은 `product_upgrade_validation/backtest_1y/input_templates/README.md`를 따른다. 피해 기록이 없는 날을 자동으로 정상으로 취급하지 않으므로, 실제 데이터가 없는 상태에서 정확도를 만들어내지 않는다.

### 4.3 토양 전향 검증

- 필지 토양검정, 토양도 물리성, 지역 통계를 서로 다른 출처 등급으로 저장한다.
- 토양값마다 측정일, 조회일, 공간 범위, 출처를 보존한다.
- 토양검정 이력이 없는 경우 `토양검정 이력 없음`으로 표시하고 무료 토양검정 서비스로 연결한다.
- 현재 토양값을 과거 날짜의 정답으로 사용하지 않는다.
- 분석일 이후에 채취된 스냅샷은 선택 대상에서 제외하고, 선택된 값의 나이와 사용 가능 여부를 결과에 남긴다.

## 5. 재검증 수용 조건

| 구분 | 수용 조건 |
| --- | --- |
| 코드 안전성 | 전체 테스트 및 정적 검사 통과, 결측 임의 대체 0건 |
| 작기 게이트 | `PLANNING`·`COMPLETED` 작기의 수확 일정 자동 보정 0건 |
| 생육단계 | 단계 전용 규칙의 단계 밖 실제 알림 0건 |
| 수확 범위 | 단일 날짜 대신 조정된 시작·종료 범위 제공 |
| 보정 안정성 | 실제 작기 코호트에서 최대 보정폭 도달률 20% 이하 |
| 수확 정확도 | 실제 수확일 확보 후 작물별 MAE 7일 이하를 목표로 검증 |
| 예보 결합 | 과거 발행 예보와 관측 매칭률 95% 이상 — 현재 기온 99.83% |
| 설명 가능성 | 원인 → 위험 → 필요한 행동 → 재확인 시점 제공 |

`보정폭 도달률 20% 이하`와 `수확일 MAE 7일 이하`는 정답 작기 데이터가 확보된 뒤에만 통과 판정할 수 있다. 임의 90일 민감도 검사로 대체하지 않는다.

## 6. 실행 순서

1. 작물주기 수확 범위를 이용하는 보정 v2 구현
2. 작기 상태 게이트 및 조정된 수확 범위 API 투영
3. 회귀 테스트와 기존 1년 관측 민감도 검사 재실행
4. 과거 발행 예보 권한 해결 및 예보 정확도 백테스트 — 1일·3일 기온 완료
5. 실제 작기 기록을 이용한 수확일·위험 경보 검증
6. 토양 전향 검증과 전국 동적 지역 코호트 확대

## 7. 이번 리비전 완료 기록

### 7.1 구현 결과

- 규칙 버전: `harvest-weather-pace-v1` → `harvest-weather-pace-v2`
- 기준 수확 범위가 없으면 적산온도 추이만 계산하고 날짜 보정은 적용하지 않는다.
- 기준 범위가 있으면 단일 날짜가 아니라 시작일·종료일을 함께 이동한다.
- 결과에 `adjustmentMode`, 기준/조정 범위, 최대 보정일, 원시 보정일, 신뢰도를 추가했다.
- `PLANNING`과 `COMPLETED` 작기는 외부 날씨 조회와 자동 보정을 실행하지 않는다.
- 관측 결측과 평년 적산온도 비활성 기간을 서로 다른 제한 사유로 분리했다.

### 7.2 1년 관측 재생 전후

| 항목 | 기존 v1 | 개선 v2 |
| --- | ---: | ---: |
| 90일 지점·작물 평가 | 400건 | 400건 |
| 적산온도 속도 계산 가능 | 334건 | 334건 |
| 실제 일정 보정으로 잘못 집계 | 334건 | 0건 |
| 최대 보정폭 도달 | 241건 | 계산하지 않음 |
| 최대 보정폭 도달률 | 72.16% | `null` |
| 결과 해석 | 임의 구간을 일정 민감도로 표시 | 실제 작기·기준 수확 범위 부재로 일정 평가 차단 |

v2의 0건은 수확 예측 정확도가 좋아졌다는 뜻이 아니다. 과거 작기 정답이 없을 때 날짜를 만드는 잘못된 평가를 제거한 결과다. 현재 백테스트 상태는 `PACE_READY_SCHEDULE_BLOCKED`다.

### 7.3 실행한 검증

- 수확 날씨 도메인·응용 회귀 테스트: 10/10 통과
- 전체 백엔드 테스트: 429/429 통과
- 정적 프로젝트 검사: 142개 파일 통과
- 2025-08-04~2026-08-03 실제 ASOS 호출: 8/8 지점 성공
- 관측 행 포함률: 2,920/2,920, 100%
- 기온 3종 완전 행: 2,914/2,920, 99.79%
- 과거 발행 단기예보 격자: 365일 × 8개 지역 × 2개 선행기간 = 5,840행, API 실패 0건
- 일별 CSV: 365행, 날짜×지점×작물 CSV: 14,600행, 90일 열량 창 CSV: 400행

### 7.4 재검증 수용 조건 판정

| 구분 | 현재 판정 | 근거 |
| --- | --- | --- |
| 코드 안전성 | 통과 | 전체 테스트·정적 검사 통과, 결측 임의 대체 없음 |
| 작기 게이트 | 통과 | `PLANNING`·`COMPLETED` 무조회·무보정 회귀 테스트 |
| 생육단계 | 통과 | 단계 밖 규칙 미실행 테스트 및 백테스트 잠재 조건 분리 |
| 수확 범위 | 통과 | 기준/조정 시작일·종료일 제공 |
| 보정 안정성 | 보류 | 실제 작기 코호트가 없어 상한 도달률을 계산하지 않음 |
| 수확 정확도 | 보류 | 실제 첫 수확일·종료일 정답 없음 |
| 예보 결합 | 기온 통과·강수 파이프라인 통과·5일 권한 대기 | 과거 격자 5,840/5,840행, 기온-관측 쌍 5,830/5,840(99.83%). 하루 24개 POP 완전 집계와 Brier 계산 실호출 확인. 중기 과거자료는 별도 권한 403 |
| 설명 가능성 | 부분 통과 | 수확 속도 요약과 제한 사유 제공, 실제 결과 검증은 정답 데이터 필요 |

### 7.5 남은 위험과 다음 입력

1. 하루 24개 POP 집계와 Brier 계산은 구현·실호출 검증했다. 1년 전체 재생은 17,520회의 격자 호출이 필요하므로 재개 가능한 추출 캐시로 장시간 실행한다.
2. 5일 선행은 APIHub `fct_afs_wc.php`와 `fct_afs_wl.php` 어댑터를 구현했다. 현재 인증키는 두 세부 API에 403을 반환하므로 활용신청 활성화 후 전체 실행한다.
3. `farm_id`, `crop_id`, `season_id`, 첫 수확일, 수확 종료일, 확정 피해일이 포함된 실제 기록을 빈 CSV 계약에 입력해야 실제 정확도가 산출된다.
4. 실제 작기 기록으로 작물별 수확일 MAE와 최대 보정폭 도달률을 재계산하기 전까지 v2는 안전한 일정 추정 로직이지 검증된 수확 예측 모델은 아니다.
5. 과거 토양 스냅샷 입력·시점 선택·노후도 차단은 구현했다. 실제 검정 이력을 입력하기 전까지 토양 기여도는 1년 재생에서 제외된다.

### 7.6 과거 발행 예보 정확도 결과

| 선행기간 | 최저기온 MAE | 최저기온 RMSE | 최고기온 MAE | 최고기온 RMSE |
| --- | ---: | ---: | ---: | ---: |
| 1일 | 1.2828°C | 1.7396°C | 1.1605°C | 1.5210°C |
| 3일 | 1.4386°C | 1.9112°C | 1.4172°C | 1.8454°C |

일반 생육단계 기온 규칙의 1일 선행 정밀도/재현율은 사과 0.8717/0.8950, 배 0.8500/0.7351, 오이 0.9562/0.9529, 감자 0.8763/0.9346, 상추 0.9212/0.9548이었다. 3일 선행 배 고온 규칙의 재현율은 0.5243으로 가장 낮아 장기 선행 경보 보정이 우선 개선 대상이다. 이 결과는 기온 임계값 초과 예측 성능이며 실제 작물 피해 진단 정확도가 아니다.

최종 판정은 `FORECAST_TEMPERATURE_AND_POP_PIPELINE_READY / FIVE_DAY_PERMISSION_PENDING / OUTCOME_AND_SOIL_COLLECTION_READY`다. 과거 예보 기온 검증은 완료했고 강수확률은 완전한 24시간 자료만 평가하는 경로와 실호출 표본을 검증했다. 5일 선행은 구현됐으나 중기 과거자료 세부 권한 활성화가 필요하며, 실제 피해·수확일·과거 필지 토양 정확도는 정답 레코드가 들어오기 전까지 보류한다.
