# 흙날씨 UI 시각개편 Revision 1

작성일: 2026-08-01
상태: `PASS`
범위: 대시보드 첫 화면의 시각 구조와 행동 전달 방식

## 1. 초기 판정

직전 업그레이드는 농업 로직과 기능을 개선했지만, 첫 화면의 레이아웃·타이포그래피·정보 위계는 거의 바뀌지 않았다. 따라서 이를 UI 업그레이드 완료로 인정하지 않는다.

- 독립 PM 시각 평가: `7.6/10`
- 고령·초보 농업인 관점: `7.3/10`
- SIMULATED_CONSUMER_REVIEW 5회 평균: `7.3/10`
- 상용 완성도: `6.9/10`
- 최종 판정: `CHANGES_REQUESTED`

현재 화면:

![현재 실제 대시보드](output/finalist_upgrade_after_dashboard.png)

## 2. 핵심 결함의 근본 원인

### 제품 구조

1. 가장 중요한 `오늘 할 일`보다 7일 예보가 더 큰 면적을 차지한다.
2. 큰 예보 차트와 7개 날짜 카드가 같은 정보를 반복한다.
3. 우측의 긴 어두운 카드 안에 상태·이유·행동·버튼을 모두 넣어 읽기 부담이 크다.
4. 좌우 카드 높이를 맞추는 2열 구조 때문에 첫 화면 하단에 큰 공백이 생긴다.
5. 상태, 행동, 근거가 여러 렌더러에서 반복 생성돼 기능을 추가할수록 화면이 길어진다.

### 코드 구조

- `08_흙날씨진단_UI_샘플.html`의 대시보드에는 `#live-outlook`, `#action-plan-panel`, `.decision-flow`, `.action-workspace`, 숨김 레거시 영역이 공존한다.
- `ui-integration/backend-client.mjs`의 `renderAnalysis`가 여러 상태·행동 렌더러를 병렬 호출한다.
- CSS가 기본, v5, finish, r1 계층으로 누적되어 같은 선택자가 반복 재정의된다.
- `ui-integration/action-plan.mjs`는 별도의 하드코딩 스타일을 다시 삽입한다.

따라서 새 CSS를 마지막에 덧붙이는 방식은 금지한다. 전역 셸과 브랜드 토큰은 유지하되, 대시보드 결과 영역과 병렬 렌더 경로를 단일 워크스페이스로 교체한다.

## 3. 목표 화면

아래 이미지는 구성·크기·위계 목표를 확인하기 위한 시안이다. production asset으로 복사하지 않고 HTML/CSS 컴포넌트로 다시 구현한다.

![UI 목표 시안 Revision 1](output/UI_TARGET_REVISION_1.png)

목표 위계는 다음과 같다.

1. `오늘의 농장 상태`: 환경 상태와 날씨·토양·예보의 개별 상태
2. `오늘 할 일`: 최우선 행동, 이유, 기한, 완료 체크
3. `7일 위험 변화`: 날짜별 위험을 한 번만 시각화
4. `이번 주 주의`: 가까운 위험과 대응 시점
5. `토양 상태`: 필지 실측과 지역 참고값을 명확히 구분

## 4. 금지 요소

- 예보 차트와 7개 날짜 카드의 정보 중복
- 첫 화면을 차지하는 긴 설명문과 원자료 목록
- API 코드, 규칙 버전, HOLD 사유 등 개발자용 문구의 기본 노출
- 큰 빈 공간을 만드는 강제 동일 높이·최소 높이
- 같은 행동을 여러 카드에서 반복 표시
- 14px 미만의 필수 정보
- 과도한 pill, 둥근 카드, 그림자, 그래디언트
- 색상만으로 주의·긴급·완료를 구분하는 방식
- 환경 상태와 자료 신뢰도를 하나의 숫자로 혼합
- 새로운 다섯 번째 CSS 덮어쓰기 계층

## 5. 측정 가능한 수용 조건

### 3초 이해

- 첫 화면 3초 노출 후 테스트 사용자의 80% 이상이 농장, 작물, 최우선 행동을 말할 수 있어야 한다.
- 10초 안에 90% 이상이 첫 행동과 완료 기한을 찾을 수 있어야 한다.
- 최우선 행동은 정확히 1개, 첫 화면 전체 행동은 최대 3개만 기본 노출한다.

### 가독성·접근성

- 최우선 행동 제목 24px 이상
- 행동 문장 18px 이상
- 일반 설명 16px 이상
- 필수 보조 정보 14px 이상
- 버튼·입력 글자 16px 이상
- 모든 주요 버튼과 체크 영역 48×48px 이상
- 일반 텍스트 명암비 4.5:1 이상
- 360px 모바일과 200% 확대에서 가로 스크롤과 핵심 행동 잘림이 없어야 한다.
- 키보드 포커스, 접근 가능한 이름, `prefers-reduced-motion`을 지원한다.

### 제품 표현

- 모든 위험 안내는 `원인 → 위험 → 행동 → 기한 → 재확인` 구조를 충족한다.
- 지역 토양 자료에는 `내 밭 검사값 아님`을 같은 정보 블록 안에 표시한다.
- 환경 상태와 자료 신뢰도를 서로 다른 개념으로 표현한다.
- 사용자의 80% 이상이 10초 안에 가장 위험한 날을 찾을 수 있어야 한다.
- 상단 뷰포트에서 전체 면적의 15%를 넘는 의도 없는 빈 공간이 없어야 한다.
- 동일 의미의 정보가 첫 화면에 두 번 이상 반복되지 않아야 한다.

### 완료 게이트

- 독립 PM 종합 평균 8.5/10 이상
- 모든 평가 항목 8.0 이상
- SIMULATED_CONSUMER_REVIEW 5회 평균 8.5 이상
- 소비자별 종합점수 8.0 미만 없음
- P0/P1, 진행 차단, 확인된 주요 UI 결함 0
- 전체 자동 테스트와 실제 화면·자연스러운 사용자 흐름 검증 통과

하나라도 실패하면 `CHANGES_REQUESTED`로 판정하고 새 revision을 진행한다.

## 6. 구현 경계와 순서

1. 전역 셸, 사이드바, 상단바, 농장·작물 전환과 기존 v5 색상 토큰은 유지한다.
2. `08_흙날씨진단_UI_샘플.html`의 현재 대시보드 결과 영역을 단일 `#dashboard-workspace`로 교체한다.
3. `renderAnalysis`의 병렬 상태·행동 렌더링을 `renderDashboardWorkspace(analysis)`로 통합한다.
4. 기존 데이터 헬퍼는 재사용하되, 행동은 한 Todo 섹션에서만 렌더링한다.
5. 대시보드 전용 스타일을 한 파일로 격리하고 중복 선택자와 런타임 `<style>` 삽입을 제거한다.
6. 데스크톱과 모바일 실제 화면을 촬영한다.
7. 구현에 참여하지 않은 PM·QA가 독립 평가하고 5회 소비자 모의평가를 다시 수행한다.
8. 수용 조건을 모두 통과한 뒤에만 개선 보고서의 전후 화면을 갱신한다.

## 7. 역할 분리

- Developer: 실제 UI 구조 교체, 데이터 연결, 자동 테스트
- PM & QA: 실제 실행 화면 평가, 수용 기준 판정
- Consumer simulation: 고령 과수 농가, 초보 귀농인, 시설 재배자, 복합 작물 농가, 농업기술센터 상담자 관점

구현자는 자신의 결과를 최종 승인하지 않는다.

## 8. Revision 기록과 최종 판정

### Revision 1 — `CHANGES_REQUESTED`

- 기존 결과 영역을 단일 워크스페이스로 교체하고 대시보드 전용 CSS를 분리했다.
- 그러나 독립 PM 평가에서 정보 중복과 날짜 표현 문제가 남아 완료 게이트를 통과하지 못했다.

### Revision 2 — `CHANGES_REQUESTED`

- 상태와 자료 신뢰도를 분리하고 모바일 행동 우선 배치를 적용했다.
- PM 평균 `8.42`, 소비자 모의평가 평균 `8.48`로 기준에 미달했다.
- 같은 위험을 `오늘 할 일`과 `이번 주 주의`에 반복한 P1 결함이 확인됐다.

### Revision 3.2 — `PASS`

- 원인에 실제 날짜·기온·작물 기준을 함께 표시했다.
- 최우선 행동과 같은 위험은 주간 목록에서 제외했다.
- 연속된 같은 위험은 하나의 기간 카드로 묶고, 서로 다른 위험 지표는 분리했다.
- 즉시 확인형 작업과 실제로 기한이 지난 예방 작업을 구분했다.
- 재배 시기 미확인 작업의 이유에서 예보 위험 문장이 섞이지 않도록 분리했다.

최종 화면:

![최종 데스크톱](output/UI_FINAL_AFTER_DESKTOP.png)

![최종 모바일](output/UI_FINAL_AFTER_MOBILE.png)

## 9. 최종 검증 결과

### 독립 PM·QA

| 항목 | 점수 |
| --- | ---: |
| 정보 위계 | 8.8 |
| 시각 완성도 | 8.6 |
| 가독성 | 8.3 |
| 조작 직관성 | 8.6 |
| 일관성 | 8.8 |
| 상용 완성도 | 8.5 |
| **종합 평균** | **8.60** |

### SIMULATED_CONSUMER_REVIEW

| 관점 | 점수 |
| --- | ---: |
| 고령 과수 농가 | 8.4 |
| 초보 귀농인 | 8.6 |
| 시설재배 농가 | 8.5 |
| 복합작물 농가 | 8.7 |
| 농업기술센터 상담자 | 8.8 |
| **평균** | **8.60** |

### 농업·고령 사용자 독립 재감사

- 종합 `9.0/10`
- `원인 → 위험 → 할 일 → 기한 → 재확인` 구조 `9.2/10`
- 공공데이터와 필지 상태 구분 `9.4/10`
- 모바일 3초 이해 `9.2/10`
- P0 `0건`, P1 `0건`

### 실행 검증

- UI 테스트 `102/102` 통과
- 백엔드 테스트 `332/332` 통과
- 백엔드 정적 검사 `109개 파일` 통과
- 배포 빌드 통과
- 사과·배·오이·감자·상추 실제 전환 시 alert `0건`, 가로 overflow `0건`
- 실제 API 분석과 근거 제한형 챗봇 응답 흐름 통과

최종 판정은 `PASS`다. 모든 완료 게이트를 충족했으며 확인된 P0/P1 결함은 없다. 위 평가는 실제 사람이 아닌 명시된 소비자 관점의 모의평가를 포함한다.
