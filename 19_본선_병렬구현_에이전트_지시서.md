# 흙날씨 본선 병렬구현 에이전트 지시서

## 공통 지시

- 먼저 `backend-v3/AGENTS.md`와 `18_본선_병렬개발_공통계약.md`를 끝까지 읽는다.
- 다른 작업자와 같은 저장소에서 일한다. 지정된 소유 파일 외에는 수정하지 않고, 다른 작업자의 변경을 되돌리지 않는다.
- `.env`를 열거나 키를 출력하지 않는다.
- 기후·토양·사진·위성을 하나의 점수로 합치지 않는다. 결측값을 0으로 바꾸지 않는다.
- 구현 전에 실패 테스트를 만들고, 좁은 테스트부터 전체 테스트 순으로 검증한다.
- 완료 보고에는 변경 파일, 실행한 검사, 남은 위험, 커밋 SHA를 적는다.

## 역할 A — 공유 상태·배포 기반

소유 범위:

- `backend-v3/src/infrastructure/`의 신규 공유 저장 모듈
- 세션·멱등성·요청 제한의 공유 상태 전환
- Supabase 마이그레이션과 관련 테스트
- `backend-v3/server/config.js`와 배포 프리플라이트의 저장소 상태

완료 조건:

- 서버 인스턴스가 바뀌어도 세션/분석/멱등성/요청 제한이 일관된다.
- 공유 저장소 실제 원자 읽기·쓰기 검증 전에는 배포 `READY`를 반환하지 않는다.
- service-role/secret은 브라우저에 노출되지 않고, Data API 권한과 RLS가 최소 권한이다.

## 역할 B — 오늘 할 일

소유 범위:

- 신규 `action-plan` 도메인·응용 모듈과 전용 테스트
- 전용 UI 모듈 `ui-integration/action-plan.mjs` 및 테스트
- 기존 HTTP 라우터와 대시보드 셸은 직접 수정하지 않고 통합 방법을 보고한다.

완료 조건:

- 오늘/당분간 주의가 분리되고 완료·건너뜀·재확인 시점이 동작한다.
- 같은 규칙이 같은 날 중복 행동을 만들지 않는다.
- 가장 중요한 행동이 먼저 보이고, 근거와 실제 행동 문구가 함께 있다.

## 역할 C — 사진 기록·시즌 회고

소유 범위:

- 신규 `photo-season` 도메인·응용 모듈과 전용 테스트
- 전용 UI 모듈 `ui-integration/photo-season.mjs` 및 테스트
- 기존 HTTP 라우터와 대시보드 셸은 직접 수정하지 않고 통합 방법을 보고한다.

완료 조건:

- 원본/메타데이터/비교 결과가 분리되고 동의·삭제 정책이 적용된다.
- 과거 사진 비교는 관찰 가능한 변화만 말하고 병해를 확정하지 않는다.
- 시즌 종료 회고는 저장된 행동·위험·사진만 사용한다.

## 통합자 — 필지·위성·챗봇 승인

소유 범위:

- 필지 경계·위성 관측 어댑터와 지도/UI 통합
- 각 역할의 모듈을 실제 API·대시보드에 연결
- 챗봇 변경 제안/확인 도구
- 전체 회귀·실제 화면·실제 API·독립 감사

완료 조건:

- 위성은 촬영일·구름·해상도·공간범위와 한계를 항상 표시한다.
- 챗봇은 미리보기와 명시적 확인 전에는 투두를 변경하지 않는다.
- 실제 자연 사용자 흐름의 전후 스크린샷과 전체 검사 결과를 제출한다.
