# 흙날씨 Product-first Review Log

이 문서는 구현 revision별 실제 실행 증거, 독립 평가, 소비자 모의평가, 판정, commit SHA와 남은 위험을 기록한다. 제품 코드를 대체하는 테스트용 갤러리나 목업은 승인 증거로 사용하지 않는다.

## 2026-07-23 — Revision R0 BASELINE

### 판정

**`CHANGES_REQUESTED`**

Root 자체 평가 평균 5.8/10, 독립 PM & QA 평균 5.7/10으로 승인 기준 8.5/10을 충족하지 못했다. P0 1건과 P1 4건도 남아 있으므로 구현 전 기준선에서 승인할 수 없다.

### 실행 및 변경 상태

| 항목 | R0 기록 |
|---|---|
| 실제 실행 경로 | `http://127.0.0.1:4173` |
| 평가 대상 | 실제 앱의 자연스러운 온보딩→대시보드→현장 행동→근거→부차 화면 흐름 |
| 제품 소스 변경 | 없음. R0에서 source HTML은 변경하지 않음 |
| 콘솔 | 검사한 흐름에서 console log/error 0건 |
| Commit SHA | **`NONE`** — 저장소가 unborn 상태이며 `HEAD`가 없음 |
| 코드 수정 | 없음. R0는 캡처·평가·PRD 기준선 수립 단계 |

> “검사한 흐름에서 console log/error 0건”은 해당 흐름에서 관찰된 사실일 뿐 전체 브라우저·네트워크·모든 상태의 무오류를 뜻하지 않는다.

### 현재 실제 화면 증거

#### 핵심 화면 모음

- [현재 핵심 화면 A](product_first_baseline/00_current_core_screens_a.jpg)
- [현재 핵심 화면 B](product_first_baseline/00_current_core_screens_b.jpg)
- [현재 핵심 화면 C](product_first_baseline/00_current_core_screens_c.jpg)

#### 자연스러운 사용자 흐름

| 순서 | 화면 | 실제 캡처 |
|---:|---|---|
| 1 | 온보딩 시작 | [01_onboarding_start.jpg](product_first_baseline/01_onboarding_start.jpg) |
| 2 | 온보딩 검토 | [02_onboarding_review.jpg](product_first_baseline/02_onboarding_review.jpg) |
| 3 | 대시보드 첫 화면 | [03_dashboard_top.jpg](product_first_baseline/03_dashboard_top.jpg) |
| 4 | 현장 체크리스트 | [04_dashboard_checklist.jpg](product_first_baseline/04_dashboard_checklist.jpg) |
| 5 | 나의 분석 | [05_my_analyses.jpg](product_first_baseline/05_my_analyses.jpg) |
| 6 | 마이페이지 | [06_mypage.jpg](product_first_baseline/06_mypage.jpg) |
| 7 | 근거 상세 | [07_evidence_dialog.jpg](product_first_baseline/07_evidence_dialog.jpg) |
| 8 | 명령 팔레트 | [08_command_palette.jpg](product_first_baseline/08_command_palette.jpg) |
| 9 | 대시보드 중단 | [09_dashboard_middle.jpg](product_first_baseline/09_dashboard_middle.jpg) |
| 10 | 대시보드 하단 | [10_dashboard_bottom.jpg](product_first_baseline/10_dashboard_bottom.jpg) |

### 목표 화면 계약

- [목표 대시보드 시안 v2](product_first_baseline/12_target_dashboard_v2.png)

![목표 대시보드 시안 v2](product_first_baseline/12_target_dashboard_v2.png)

이 이미지는 구성·크기·위계에 대한 구현 계약이며 production asset이 아니다. 앱에 이미지로 복사하지 않고 실제 컴포넌트로 구현한다.

### 캡처 표면 불일치 기록

- 브라우저 런타임은 내부 레이아웃을 **1280×720**으로 보고했다.
- 저장된 온보딩 viewport 캡처는 **1041×720**이다.
- 저장된 주요 앱 viewport 캡처는 **1029×712**다.
- 저장된 캡처에서 오른쪽 영역과 CTA 잘림이 보인다.
- 이 불일치는 숨기거나 단순 캡처 오차로 승인하지 않는다. **실제 브라우저 표면과 캡처 표면 모두에서 재현·확인해야 하는 cross-surface 시각 결함**으로 기록한다.
- full-page 캡처에서 fixed overlay가 반복되어 보이는 현상은 캡처 도구 artifact다. 이는 제품 승인 또는 결함 해소 증거로 사용하지 않는다.

### Root 자체 평가와 독립 PM & QA 비교

Root 자체 평가는 독립 PM 결과 전에 확정했다. 독립 PM & QA는 구현자가 아닌 별도 역할로 실제 실행 화면을 평가했다.

| 평가 항목 | Root 자체 평가 | 독립 PM & QA | 승인 하한 |
|---|---:|---:|---:|
| 정보 위계 | 6.3 | 6.3 | 8.0 |
| 시각적 완성도 | 5.7 | 5.6 | 8.0 |
| 가독성 | 6.2 | 6.1 | 8.0 |
| 조작 직관성 | 5.5 | 5.2 | 8.0 |
| 일관성 | 6.0 | 6.0 | 8.0 |
| 상용 완성도 | 4.9 | 4.8 | 8.0 |
| **평균** | **5.8** | **5.7** | **8.5** |

두 평가 모두 실제 사람 평가가 아니다. 특히 독립 PM & QA는 독립 역할의 전문가 평가이며 실제 농업 종사자 사용자 테스트로 표현하지 않는다.

### 확인된 결함

| 심각도 | 건수 | 결함 | 사용자 영향 |
|---|---:|---|---|
| P0 | 1 | 온보딩 핵심 CTA가 캡처 표면에서 완전히 보이지 않거나 진행을 차단함 | 제품 시작 흐름을 완료할 수 없음 |
| P1 | 1 | 오른쪽 콘텐츠와 CTA의 가로 잘림 | 핵심 정보와 행동을 놓침 |
| P1 | 1 | 프로그램 포커스 대상과 fixed/sticky header 충돌 | 이동 후 사용자가 현재 위치를 이해하기 어려움 |
| P1 | 1 | 제목에 의미 없는 focus 사각형 노출 | 완성도가 떨어지고 포인터 사용자에게 혼란을 줌 |
| P1 | 1 | 근거 모달의 7열 표와 정보 과밀 | 이유·거리·기간을 빠르게 찾기 어려움 |

### 확인된 근본 원인

1. **데이터 산출물 중심 IA:** 사용자의 오늘 판단보다 점수·설명·표의 전시가 우선한다.
2. **하나의 최우선 결론 부재:** 여러 지표와 CTA가 비슷한 무게를 가져 3초 이해를 방해한다.
3. **실제 농지 객체의 약한 존재감:** 지역·작물·필지 상태보다 추상적인 분석 결과가 앞선다.
4. **CSS override 누적:** base + `v5` + `v5.1`과 상이한 breakpoint가 일관된 레이아웃을 방해한다.
5. **고정 사이드바 이후 가용 폭 미반영:** 콘텐츠 그리드가 실제 남은 폭을 보장하지 않아 잘림이 발생한다.
6. **포커스와 sticky header의 분리 설계:** 프로그램 이동, 보이는 위치, focus 표현이 하나의 규칙으로 동작하지 않는다.
7. **근거 모듈 적층:** 7열 표와 설명 모듈을 더하는 방식이 오히려 판단 근거 탐색을 느리게 한다.

### 보존할 강점

- 배수 길·물 고임·토양 굳음처럼 현장에서 수행할 수 있는 행동 문장.
- 필지 미등록 시 실제 필지 경계가 아님을 밝히는 정직성.
- 예보 격자 약 5km, 토양 대표지점 6.8km, 관측소 12.4km의 공간 거리 공개.

### R0 승인 게이트 결과

| 게이트 | 기준 | R0 결과 | 상태 |
|---|---|---|---|
| PM 평균 | 8.5 이상 | 5.7 | 실패 |
| PM 모든 항목 | 8.0 이상 | 최저 4.8 | 실패 |
| 소비자 모의평가 평균 | 8.5 이상 | R0 미실시 | 실패/미검증 |
| 소비자별 점수 | 8.0 미만 0 | R0 미실시 | 실패/미검증 |
| P0/P1/진행 차단 | 0 | P0 1, P1 4 | 실패 |
| 전체 자동 테스트 | 통과 | R0 구현 전, 전체 미실시 | 미검증 |
| 실제 화면 검증 | 전체 viewport·흐름 통과 | 데스크톱 일부만 확인 | 실패/미검증 |

### R0 남은 위험과 미검증 항목

- 390×844와 360×800에서의 실제 모바일 화면.
- 실제 60–70대 농업 종사자 대상 이해도·행동 성공률.
- 브라우저 viewport와 저장된 스크린샷 픽셀 크기의 명시적 parity.
- 서로 다른 환경에서의 네트워크 웹폰트 로딩.
- 모든 UI 상태와 브라우저에서의 콘솔·런타임 오류.
- R1 구현 이후 자동 테스트와 자연 흐름의 전체 회귀.

## 2026-07-25 — Revision R0.2 UI 실행 격차 기록

### 판정

**`CHANGES_REQUESTED`**

PRD의 문제 상황·핵심 사용자·AI-native 계약은 보강됐지만 실제 첫 화면과 결과 화면에는 충분히 반영되지 않았다. 문서가 좋아진 사실을 제품이 좋아진 증거로 사용하지 않는다.

### 문서와 실제 화면의 불일치

| 제품 계약 | R0.2 실제 화면 | 사용자 영향 | R1 조치 |
|---|---|---|---|
| 귀농 준비자·초보 농업인을 위한 서비스임을 3초 안에 이해 | 첫 제목이 `지금 어떤 상황인가요?`로 시작해 대상과 필요성이 보이지 않음 | 사용자가 “왜 이 서비스인가”를 이해하기 전에 입력을 요구받음 | 첫 화면 제목에 대상·사용 순간·효익을 함께 명시 |
| 현장 방문·임대·계약·파종 전에 사용 | 상황 카드가 사용 시점을 구체적으로 말하지 않음 | 언제 다시 이 서비스를 써야 하는지 기억하기 어려움 | 선택 카드에 `현장 방문·계약 전`, `오늘·1~3일 위험 확인`을 명시 |
| 사용자가 질문하지 않아도 놓친 위험과 결측을 먼저 안내 | `AI 정리` 라벨과 설명문은 있으나 AI가 먼저 한 일이 구조로 구분되지 않음 | 일반 대시보드에 AI 문구만 붙인 제품처럼 보임 | 결과 첫 화면을 `판단 / AI가 먼저 찾은 것 / 아직 모르는 것 / 첫 행동` 순서로 교체 |
| 서로 다른 축을 근거 없이 종합점수로 합치지 않음 | 정적 화면에 `62/100`, 기후 55%·토양 45% 합산이 남아 있음 | 거짓 정밀도와 문서-제품 모순 발생 | 종합점수를 제거하고 기후·토양·예보 상태를 분리 |
| 가장 중요한 행동 1개가 첫 결과의 중심 | 행동 목록과 별도 보고서 버튼이 함께 노출됨 | 초보자가 무엇부터 해야 하는지 다시 해석해야 함 | `primaryAction` 1개를 주 CTA로 올리고 나머지는 후속 확인으로 분리 |
| P1 알림 미구현 상태를 정직하게 표시 | `나중에 알림`, 기상 위험 알림 스위치가 실제 기능처럼 보임 | 작동하지 않는 기능에 대한 신뢰 손상 | 현재 세션의 `나중에 보기`로 축소하고 알림은 `준비 중` 비활성 처리 |
| 실제 결과를 별도 요청 없이 자동 설명 | 보고서를 사용자가 별도 버튼으로 요청해야 함 | 무엇을 물어봐야 할지 모르는 사용자가 핵심 설명을 놓침 | 분석 완료 직후 근거 제한형 보고서를 자동 요청하고 상태를 명시 |

### 근본 원인

1. 제품 정의를 PRD에 추가했지만 온보딩과 대시보드의 DOM 구조는 이전 산출물 중심 구조를 유지했다.
2. 정적 HTML과 백엔드 연결 뒤 동적 렌더러가 서로 다른 정보 위계를 사용했다.
3. `score` 관련 ID와 CSS를 호환성 목적으로 보존하면서 폐기하기로 한 종합점수 표현까지 남겼다.
4. AI-native를 “AI 라벨과 요약 문장”으로 표현하고, 선제 발견·핵심 결측·첫 행동이라는 사용 경험으로 구현하지 않았다.
5. 아직 없는 DB·스케줄러·알림 채널을 시연용 상호작용으로 남겨 제품 범위를 과장했다.

### R1 측정 가능한 수용 조건

- 온보딩 첫 viewport에서 `귀농 준비자 또는 초보 재배자`, `현장 방문·계약·파종 전`, `놓친 위험과 첫 행동을 먼저 안내`를 스크롤 없이 읽을 수 있다.
- 첫 단계의 주요 질문은 한 문장이고, 각 선택지는 `사용 순간 + 받는 결과`를 2줄 안에 설명한다.
- 결과 첫 viewport는 `현재 판단 1개`, `AI가 먼저 찾은 위험 1개`, `아직 모르는 핵심 정보`, `가장 먼저 할 행동 1개`를 보여준다.
- 정적 화면과 백엔드 동적 화면 어디에도 기후·토양을 55:45로 합친 종합점수를 표시하지 않는다.
- 기후·토양·예보는 별도 상태와 한계로 표시하며, `HOLD`일 때 결측을 임의 값으로 채우지 않는다.
- 분석 완료 뒤 사용자가 보고서 생성 버튼을 찾지 않아도 근거 제한형 설명 생성을 시작한다.
- 실제 알림 기능이 없는 상태에서는 알림 스위치를 활성 기능처럼 노출하지 않는다.
- 1280×720, 390×844, 360×800에서 페이지 수준 가로 overflow가 0이고, 주요 CTA의 보이는 영역이 잘리지 않는다.
- 키보드만으로 상황 선택→지역→작물→시기→확인 흐름을 진행할 수 있고, 보이는 focus가 고정 헤더에 가려지지 않는다.
- 실제 자연 흐름에서 console error 0, UI 통합 테스트·백엔드 회귀 테스트·정적 검사가 모두 통과해야 한다.

## 2026-07-25 — Revision R1 AI-native 정보 구조 교체

### 판정

**`CHANGES_REQUESTED`**

구현자 자체 평가와 소비자 관점 모의평가는 수치 기준을 넘었고 확인한 P0/P1 UI 결함은 0건이다. 다만 구현자와 독립된 PM & QA 재평가를 이번 revision에서 수행하지 않았고 전용 브라우저 console 이벤트 스트림도 사용할 수 없어, 구현자가 자신의 화면을 최종 승인하지 않는다.

### 변경 전후 실제 화면

| 구분 | Viewport | 실제 캡처 | 확인 내용 |
|---|---:|---|---|
| 변경 전 | 1280×720 | [R0.2 온보딩](product_first_baseline/r1/00_r0_2_before_onboarding.jpg) | 대상·사용 순간·AI-native 효익이 첫 화면에 없음 |
| 변경 후 | 1280×720 | [R1 온보딩](product_first_baseline/r1/01_r1_desktop_onboarding_1280x720.jpg) | 귀농 준비자·계약/파종 전·선제 발견·첫 행동을 첫 viewport에 표시 |
| 변경 후 | 1280×720 | [R1 대시보드](product_first_baseline/r1/02_r1_desktop_dashboard_1280x720.jpg) | 판단·선제 발견·결측·첫 행동과 축별 상태를 첫 viewport에 표시 |
| 변경 후 | 390×844 | [R1 모바일 온보딩](product_first_baseline/r1/03_r1_mobile_onboarding_390x844.jpg) | 핵심 약속·상황 선택·다음 버튼이 한 화면에서 보임 |
| 변경 후 | 390×844 | [R1 모바일 대시보드](product_first_baseline/r1/04_r1_mobile_dashboard_390x844.jpg) | 첫 행동 카드가 하단 내비게이션 위에 완전히 보임 |
| 변경 후 | 360×800 | [R1 좁은 모바일 대시보드](product_first_baseline/r1/05_r1_mobile_dashboard_360x800.jpg) | 페이지 가로 overflow 없이 핵심 행동 유지 |

### 구현 내용

- 온보딩 H1을 단순 상황 질문에서 `대상 + 사용 순간 + 효익`을 설명하는 제품 문장으로 교체했다.
- 상황 카드는 `현장 방문·임대·계약 전`과 `오늘~1–3일 위험 확인`이라는 구체적인 사용 순간을 설명한다.
- 결과 첫 화면을 `현재 판단 → AI가 먼저 찾은 것 → 가장 먼저 할 일 → 축별 준비 상태` 순서로 교체했다.
- 정적 62/100 종합점수와 기후 55%·토양 45% 합산 표현을 제거했다.
- 백엔드 동적 렌더러도 `primaryAction` 1개를 먼저 보여주고 나머지를 `그다음 확인`으로 분리한다.
- 기후·토양·예보를 별도 축으로 표시하며 결측을 임의 값으로 채우지 않는다는 문장을 함께 보여준다.
- 결정형 쉬운 설명은 분석 완료 뒤 별도 버튼 없이 자동 요청한다. 실패 시에만 재시도 제어를 노출한다.
- `나중에 알림`은 현재 화면의 `나중에 보기`로 축소했고, 실제 알림 설정은 `준비 중` 비활성 상태로 바꿨다.
- `SEASON_UNKNOWN` 같은 내부 제한 코드를 초보자용 한국어 설명으로 번역했다.

### Git 및 변경 범위

- 변경 구조: 온보딩 첫 단계와 대시보드 핵심 영역의 DOM·렌더러 교체. 색상만 바꾼 국소 패치가 아니다.
- Commit SHA: **`NONE`** — 사용자가 이번 변경의 commit을 요청하지 않아 working tree 상태로 유지했다.
- 비교 기준 parent HEAD: `13a1555ecea158b09b9dbc0dbef05745b5c97aed`.
- 주요 변경 파일:
  - `08_흙날씨진단_UI_샘플.html`
  - `ui-integration/backend-client.mjs`
  - `ui-integration/product-first-ui.test.mjs`
  - `PRODUCT_FIRST_PRD.md`
  - `PRODUCT_FIRST_REVIEW_LOG.md`
  - `product_first_baseline/r1/*`

### 실제 사용자 흐름 검증

`후보 농지를 알아보는 중 → 평창군 입력 → 주소 후보 직접 확인 → 감자 → 시기 잘 모름 → 조건 확인 → 분석`을 실제 실행 화면에서 완료했다.

| 검사 | 결과 | 실제 관찰 |
|---|---|---|
| 자동 쉬운 설명 | `PASS` | 별도 클릭 없이 `결정형 보고서` 상태 도달 |
| 1280×720 첫 행동 | `PASS` | 카드 bottom 702px ≤ viewport 720px |
| 390×844 첫 행동 | `PASS` | 카드 bottom 657px ≤ 하단 내비게이션 top 775px |
| 360×800 첫 행동 | `PASS` | 카드 bottom 657px ≤ 하단 내비게이션 top 731px |
| 390px 가로 overflow | `PASS` | document scrollWidth 390 = clientWidth 390 |
| 360px 가로 overflow | `PASS` | document scrollWidth 360 = clientWidth 360 |
| 390×844 온보딩 | `PASS` | 핵심 약속·상황 카드·다음 버튼이 viewport 안에 표시 |
| 360×800 온보딩 | `PASS` | document/대화상자 가로 overflow 0, 다음 버튼 완전 노출 |
| 샘플·HOLD 정직성 | `PASS` | 개발 샘플·의사결정 금지·판단 보류·필지 실측 아님을 유지 |
| 전용 console 이벤트 검사 | `NOT_RUN` | 선택된 브라우저에서 CDP console capability가 제공되지 않음 |

### 자동 검사

| 검사 | 결과 |
|---|---|
| `node --test ui-integration/*.test.mjs` | `PASS` — 18/18 |
| `node --check ui-integration/backend-client.mjs` | `PASS` |
| `npm test` (`backend-v3`) | `PASS` — 197/197 |
| `npm run check` (`backend-v3`) | `PASS` — 67개 파일 |
| `git diff --check` | `PASS` |

백엔드 전체 테스트의 첫 실행은 샌드박스의 localhost listen 제한으로 23개 HTTP 테스트가 `EPERM` 실패했다. 포트 사용이 허용된 동일 코드 재실행에서는 197/197이 모두 통과했다.

### Developer 자체 평가

> 구현자가 수행한 자체 평가이며 독립 PM & QA 평가가 아니다.

| 평가 항목 | 점수 / 10 | 근거 |
|---|---:|---|
| 정보 위계 | 9.0 | 대상·판단·발견·결측·첫 행동이 순서대로 보임 |
| 시각적 완성도 | 8.8 | 데스크톱과 모바일의 동일한 밀도·타이포·상태 카드 유지 |
| 가독성 | 8.8 | 전문코드 제거, 본문·CTA 크기와 대비 확보 |
| 조작 직관성 | 8.9 | 질문형 5단계와 자동 설명으로 탐색 부담 감소 |
| 일관성 | 8.7 | 정적 HTML과 백엔드 동적 화면이 같은 축별 계약 사용 |
| 상용 완성도 | 8.5 | 기능 과장 제거와 반응형 완성도 개선, 실제 데이터 준비는 별도 HOLD |
| **평균** | **8.78** | 독립 승인 점수가 아님 |

### SIMULATED_CONSUMER_REVIEW 5회

> 아래 평가는 실제 사람이 아닌 서로 다른 소비자 관점의 모의평가다.

| 관점 | 3초 이해 | 종합점수 / 10 | 주요 판단 |
|---|---|---:|---|
| 60–70대 농업 종사자 | 통과 | 8.5 | 큰 제목과 쉬운 행동은 좋지만 개발 샘플의 의미는 추가 설명이 필요 |
| 귀농 초보자 | 통과 | 9.1 | 무엇을 질문할지 몰라도 첫 행동을 받을 수 있음 |
| 경험 많은 농가 운영자 | 통과 | 8.2 | 축 분리와 근거 정직성은 좋지만 실제 필지·운영 데이터가 아직 부족 |
| 모바일 중심 사용자 | 통과 | 8.8 | 390/360px에서 선택과 첫 행동이 잘리지 않음 |
| 상용 SaaS 기대 사용자 | 통과 | 8.6 | 제품 인상과 상태 표현은 상용 수준에 가까우나 계정·저장·알림은 준비 중 |
| **평균** |  | **8.64** | 모든 모의 소비자 8.0 이상 |

### 승인 게이트

| 게이트 | 기준 | R1 결과 | 상태 |
|---|---|---|---|
| 독립 PM 평균 | 8.5 이상 | 미실시 | 실패/미검증 |
| 독립 PM 모든 항목 | 8.0 이상 | 미실시 | 실패/미검증 |
| 소비자 모의평가 평균 | 8.5 이상 | 8.64 | 통과 |
| 소비자별 점수 | 8.0 미만 0 | 최저 8.2 | 통과 |
| P0/P1/진행 차단 | 0 | 확인한 UI 흐름 0 | 통과 |
| 전체 자동 테스트 | 통과 | UI 18/18, backend 197/197, check 67 | 통과 |
| 실제 화면 검증 | 통과 | 1280/390/360 자연 흐름·overflow 통과 | 통과 |
| 전용 console 오류 검사 | 오류 0 | capability 미제공 | 미검증 |

### 남은 위험과 다음 평가 조건

- 독립 PM & QA가 같은 실제 화면과 자연 흐름을 다시 평가해야 한다.
- 실제 60–70대 농업 종사자와 귀농 초보자의 과업 성공률은 아직 측정하지 않았다.
- 현재 실행은 개발 샘플·운영 `HOLD`이므로 실제 공공데이터와 필지 판단의 정확성을 의미하지 않는다.
- DB·영구 저장·주기 분석·푸시 알림은 구현되지 않았다.
- 브라우저 console 이벤트를 읽을 수 있는 환경에서 초기 로드와 전체 흐름의 console error 0을 재확인해야 한다.

## 2026-07-25 — Revision R2 재배 중 농가 온보딩 교체

### 판정

`CHANGES_REQUESTED`

샘플 모드의 자연스러운 사용자 흐름과 자동 검사는 통과했다. 그러나 실제 위치 권한 허용 환경의 라이브 Kakao 역지오코딩과 사진 자동 생육 판독 모델은 아직 검증·연결되지 않았고, 구현자와 독립된 PM & QA 승인도 이번 revision에서 수행하지 않았다.

### 사용자 피드백과 근본 원인

변경 전 검토 화면은 지역 문자열을 표시했지만, 새로고침 뒤 메모리에 있던 위치 후보 토큰이 사라지면 제출 버튼은 비활성화됐다. 사용자는 지역을 완료했다고 보는데 실제 시스템은 미확인 상태인 이중 상태가 대시보드 진입을 막았다.

- 첫 질문이 후보지 탐색과 재배 중 상황을 다시 선택시켜 현재 농가의 사용 순간과 맞지 않았다.
- 단일 작물 라디오는 여러 작물을 함께 키우는 농가를 표현하지 못했다.
- 사과·배는 하나뿐인 `검수된 연간 기준`을 의미 없이 선택하게 했다.
- 생육단계는 텍스트 선택만 있었고 사진 기반 진단으로 확장할 안전한 입력 경계가 없었다.

### Git 및 변경 범위

- 기준 HEAD: `13a1555ecea158b09b9dbc0dbef05745b5c97aed`
- R2 commit: `NONE` — 사용자가 이번 revision의 commit을 요청하지 않아 작업 트리에만 반영
- 구조 교체: `상황 → 지역 → 단일 작물 → 시기 → 확인`을 `현재 위치 → 복수 작물 → 작물별 조건 → 사진/생육상태 → 확인`으로 교체
- 백엔드 추가: `POST /api/locations/current`, Kakao 좌표→법정동 후보 어댑터, 좌표 비노출 후보 토큰화
- 분석 오케스트레이션: 백엔드의 `1분석=1작물` 계약은 유지하고 프런트에서 작물별 독립 요청·결과 탭을 구성

### 변경 전후 실제 화면

| 구분 | Viewport/흐름 | 실제 화면 | 관찰 |
|---|---|---|---|
| 변경 전 | 모바일 검토 | [제출 차단 화면](product_first_baseline/r2/00_before_blocked_review.png) | 지역이 표시되지만 후보 토큰이 없어 분석 버튼 비활성 |
| 변경 후 | 1280×720 온보딩 | [현재 위치 시작](product_first_baseline/r2/01_r2_onboarding.jpg) | 재배 중 가정, 현재 위치 CTA, 직접 입력 대체 경로를 첫 화면에 표시 |
| 변경 후 | 1280×720 복수 작물 결과 | [작물 결과 전환](product_first_baseline/r2/02_r2_dashboard_multi_crop.jpg) | 사과·오이 결과 탭과 작물별 독립 판단 표시 |
| 변경 후 | 390×844 온보딩 | [모바일 현재 위치](product_first_baseline/r2/03b_r2_onboarding_mobile_390x844.png) | `scrollWidth=390`, 위치 CTA와 직접 입력이 가로 잘림 없이 표시 |
| 변경 후 | 390×844 검토 | [모바일 고정 분석 CTA](product_first_baseline/r2/04b_r2_review_mobile_sticky_cta_390x844.png) | 분석 CTA가 viewport 안에 고정되고 활성화됨 |
| 변경 후 | 390×844 대시보드 | [모바일 복수 작물 결과](product_first_baseline/r2/05_r2_dashboard_mobile_390x844.png) | 실제 분석 완료 후 가로 overflow 0 |

### 실행·검사 결과

| 검사 | 결과 |
|---|---|
| UI 계약·런타임 테스트 | `PASS` — 22/22 |
| backend-v3 전체 테스트 | `PASS` — 200/200 |
| backend-v3 프로젝트 검사 | `PASS` — 67개 파일 |
| `git diff --check` | `PASS` |
| 데스크톱 자연 흐름 | `PASS` — 수동 위치→사과+오이→작물별 조건→생육단계→분석→작물 탭 전환 |
| 390×844 자연 흐름 | `PASS` — 검토 `root/body scrollWidth=390`, CTA 화면 내 위치, 대시보드 진입 |
| 위치 권한 거부 대체 경로 | `PASS` — 권한 거부 문구 후 수동 주소 검색으로 진행 |
| 실제 위치 권한 허용 | `미검증` — 검증 브라우저가 위치 권한을 거부함 |
| 사진 자동 판독 | `NOT_IMPLEMENTED` — 로컬 미리보기·10MB 이미지 방어·수동 단계 확인만 제공 |

### Developer 자체 평가

> 구현자의 자체 평가이며 독립 PM & QA 승인이 아니다.

| 평가 항목 | 점수 / 10 | 근거 |
|---|---:|---|
| 정보 위계 | 9.1 | 첫 화면을 위치 한 행동으로 단순화하고 복수 작물 조건을 단계별 분리 |
| 시각적 완성도 | 8.8 | 데스크톱·모바일에서 동일한 제품 타이포와 상태 표현 유지 |
| 가독성 | 8.8 | 의미 없는 연간 선택 제거, 작물별 조건을 카드로 분리 |
| 조작 직관성 | 9.2 | 위치 권한 거부 대체 경로와 최종 고정 CTA로 진행 차단 제거 |
| 일관성 | 8.9 | 검토·제출이 동일한 후보 토큰 상태를 사용하고 작물별 결과 계약 유지 |
| 상용 완성도 | 8.5 | 복수 작물 흐름은 상용 수준에 가까우나 라이브 위치·사진 판독·DB는 미완료 |
| **평균** | **8.88** | 독립 승인 점수가 아님 |

### SIMULATED_CONSUMER_REVIEW 5회

> 실제 사람 평가가 아닌 서로 다른 소비자 관점의 모의평가다.

| 관점 | 종합점수 / 10 | 주요 판단 |
|---|---:|---|
| 60–70대 농업 종사자 | 8.7 | 현재 위치 버튼과 하단 고정 분석 CTA가 명확하며 직접 입력 대체 경로도 이해 가능 |
| 여러 작물을 키우는 소규모 농가 | 9.2 | 복수 선택과 작물별 환경·결과 전환이 실제 작업 단위와 맞음 |
| 귀농 직후 재배를 시작한 초보 | 8.9 | 상황을 다시 고르지 않고 필요한 위치·작물·단계만 물어 부담이 줄음 |
| 개인정보에 민감한 사용자 | 8.6 | 좌표 비표시 안내는 좋지만 실제 보관·삭제 정책은 서비스 약관으로 더 명시해야 함 |
| 모바일 중심 사용자 | 8.8 | 390px에서 가로 넘침이 없고 분석 버튼이 항상 보임 |
| **평균** | **8.84** | 최저 8.6, 8.0 미만 없음 |

### 남은 위험

- 라이브 Kakao 역지오코딩은 공식 계약에 맞춘 어댑터·테스트가 있지만 실제 사용자 키와 위치 권한 허용 단말에서 최종 검증하지 않았다.
- 브라우저 좌표를 서버에 전달하므로 운영 전 개인정보 처리방침, 보관하지 않는다는 운영 로그 검증, 전송구간 HTTPS가 필수다.
- 사진은 서버에 전송하지 않으며 생육단계를 판독하지 않는다. 검수된 이미지 데이터셋·비전 모델·오진 표현·동의·삭제 정책 없이는 자동 진단으로 승격하지 않는다.
- 복수 작물은 분석 요청과 결과가 분리되지만 DB가 없어 재접속 뒤 저장·알림·이력 비교는 불가능하다.
- 독립 PM & QA 재평가가 없으므로 전체 product-first 승인 게이트는 통과로 판정하지 않는다.

## 2026-07-25 — Revision R3 현재 날짜·7일 예보 시각화

### 판정

`CHANGES_REQUESTED`

계절 입력 제거, 현재 날짜 자동 적용, 실제 예보 배열의 시각화, 샘플 자연 흐름과 자동 검사는 통과했다. 그러나 실제 기상청 키를 사용한 단기·중기 예보 화면과 구현자에게 독립된 PM & QA 평가는 이번 revision에서 수행하지 않았으므로 최종 승인하지 않는다.

### 사용자 피드백과 제품 결정

- 현재 재배 중인 사용자에게 봄·여름·가을 작기를 다시 묻지 않는다.
- 현재 달은 기기 날짜에서 자동 적용하고 작물 차이는 재배 환경·생육 단계·검수 규칙으로 구분한다.
- 파종·정식일이 필요한 향후 기능은 계절 이름이 아니라 날짜를 선택적으로 받는다.
- 대시보드 첫 화면에서 상태 텍스트만 크게 보이던 구조를 7일 예보 그래프·날짜별 카드·오늘 할 일로 교체한다.
- 정적 이미지가 아니라 백엔드 예보 값에 연결된 SVG 선 그래프와 날씨 신호 아이콘을 사용한다.
- 샘플 값은 시각 흐름 검증에만 사용하며 `개발 샘플 예보`, `농업 의사결정 금지`를 유지한다.

### 근본 원인과 조치

1. `forecast.result.mergedDisplayDays`가 응답에 존재했지만 첫 viewport 렌더러는 모듈의 `state`만 표시했다.
2. 샘플 runtime이 단기 1일·중기 1일만 제공해 예측 UI가 작동하는지 시각적으로 확인하기 어려웠다.
3. 현재 재배 분석이 현재 날짜와 별개로 광범위한 작기 범위를 다시 입력받아 불필요한 사용자 결정을 만들었다.
4. 인라인 마법사와 백엔드 연결 모듈이 위치 완료를 서로 다르게 판단해 원시 문자열만으로 검토 단계까지 갈 수 있었다.

조치 후에는 현재 월을 시작·종료 월이 같은 `CUSTOM` 분석 범위로 전달하고, 위치 완료는 `candidateVerified` DOM 상태와 서버 후보 토큰을 함께 요구한다. 샘플 runtime은 단기 3일·중기 4일을 제공하며 대시보드는 최대 7일을 실제 응답 순서로 표시한다.

### 변경 후 실제 화면

| Viewport | 실제 화면 | 확인 내용 |
|---:|---|---|
| 기본 데스크톱 | [명사형 정보 구조](product_first_baseline/r3/03_r3_dashboard_factual_copy.png) | `농장 분석`, `7일 기온·강수 예보`, `우선 조치`로 발표·대화식 문구 제거 |
| 390×844 | [모바일 7일 예보](product_first_baseline/r3/02_r3_dashboard_mobile_390x844.png) | 문서 수평 overflow 0, 예보 카드 영역 내부 스크롤, 하단 내비게이션 유지 |

### 실행·검사 결과

| 검사 | 결과 |
|---|---|
| UI 전체 테스트 | `PASS` — 27/27 |
| backend-v3 전체 테스트 | `PASS` — 200/200 |
| backend-v3 프로젝트 검사 | `PASS` — 67개 파일 |
| JavaScript 구문 검사 | `PASS` — backend-client, api-contract, sample-runtime |
| `git diff --check` | `PASS` |
| 실제 자연 흐름 | `PASS` — 위치 후보 선택→감자→현재 날짜 자동→생육 중기→분석→7일 대시보드 |
| 위치 문자열 단독 진행 차단 | `PASS` — 후보 선택 전 다음 버튼 비활성 |
| 브라우저 console error | `PASS` — 검사 흐름에서 0건 |
| 390×844 수평 overflow | `PASS` — `root/body scrollWidth=390`, live outlook 폭 362px |
| 라이브 기상청 예보 | `미검증` — 현재 실행은 개발 샘플 runtime |

### Developer 자체 평가

> 구현자의 자체 평가이며 독립 PM & QA 승인이 아니다.

| 평가 항목 | 점수 / 10 | 근거 |
|---|---:|---|
| 정보 위계 | 9.2 | 첫 viewport에서 7일 변화와 오늘 행동을 바로 구분 |
| 시각적 완성도 | 9.0 | 실제 값 기반 SVG 선 그래프와 일별 카드가 데스크톱·모바일에 일관되게 표시 |
| 가독성 | 8.8 | 텍스트 상태를 기온 범위·날짜·강수확률로 분해 |
| 조작 직관성 | 9.1 | 계절 질문 제거, 오늘 날짜 자동 적용, 위치 후보 미확정 진행 차단 |
| 일관성 | 8.9 | 단기·중기 예보와 샘플/라이브 라벨을 동일한 구조로 표현 |
| 상용 완성도 | 8.6 | 첫 화면은 상용 제품 수준에 가까우나 라이브 데이터·DB·실제 알림은 미완료 |
| **평균** | **8.93** | 독립 승인 점수가 아님 |

### SIMULATED_CONSUMER_REVIEW 5회

> 실제 사람이 아닌 서로 다른 소비자 관점의 모의평가다.

| 관점 | 종합점수 / 10 | 주요 판단 |
|---|---:|---|
| 60–70대 농업 종사자 | 8.6 | 계절 선택이 사라져 쉽지만 모바일 예보 카드는 좌우 넘김 안내를 더 강화할 여지가 있음 |
| 귀농 초보자 | 9.1 | 앞으로 며칠의 온도·비와 오늘 행동을 한 화면에서 이해 가능 |
| 경험 많은 농가 운영자 | 8.5 | 데이터 출처 구분은 좋지만 필지 센서·실측·파종일 연동이 아직 없음 |
| 모바일 중심 사용자 | 8.8 | 390px에서 그래프와 4개 카드가 즉시 보이고 나머지는 카드 영역에서 스크롤 가능 |
| 상용 SaaS 기대 사용자 | 8.8 | 데이터 기반 시각 구조는 제품답지만 샘플 경고와 HOLD가 시연 완성도를 제한 |
| **평균** | **8.76** | 최저 8.5, 8.0 미만 없음 |

### 남은 위험

- 실제 기상청 단기·중기 응답의 일수·결측·발표시각 변화가 같은 시각 구조에서 올바르게 보이는지 라이브 키 환경 검증이 필요하다.
- 현재 달 자동 적용은 “지금의 가까운 위험”에는 적합하지만 생육일수·누적온도 기반 판단에는 파종·정식일이 추가로 필요하다.
- 복수 작물 선택 시 현재 생육단계는 아직 공통 입력이므로 작물마다 단계가 다른 농가는 다음 revision에서 작물별 단계 또는 사진별 연결이 필요하다.
- 7일 그래프는 온도 흐름을 보여주지만 강수량·풍속의 별도 추세 그래프와 위험 임계치 강조는 검수된 규칙에 연결한 뒤 추가해야 한다.
- 개발 샘플은 위험이 없다고 확인하거나 운영 `READY`로 승격되지 않는다.
- 독립 PM & QA 재평가가 없어 product-first 최종 승인 게이트는 통과하지 않았다.

## 다음 Revision 기록 템플릿

### Revision `[R?.?]` — `[YYYY-MM-DD]`

#### 판정

`APPROVED | CHANGES_REQUESTED`

#### 역할 독립성

- Developer: `[에이전트/역할]`
- 독립 PM & QA: `[에이전트/역할]`
- 구현자와 승인자가 동일하지 않음: `[예/아니오]`

#### Git 및 변경 범위

- Commit SHA: `[정확한 40자 SHA 또는 NONE + 사유]`
- 변경 파일: `[목록]`
- 목표 구조 교체/국소 변경: `[설명]`
- 같은 본질적 결함 반복 횟수: `[0/1/2+]`
- 2회 반복 시 구조 재설계 여부: `[해당 없음/실시]`

#### 실제 실행 환경

- 실행 URL: `[URL]`
- 브라우저/버전: `[값]`
- 실행 viewport: `1440×900 / 1280×800 / 1040×768 / 390×844 / 360×800`
- 브라우저 보고 viewport와 캡처 픽셀 parity: `[통과/실패 + 증거]`

#### 변경 전 실제 화면

| Viewport/흐름 | 캡처 경로 | 관찰 |
|---|---|---|
| `[값]` | `[경로]` | `[내용]` |

#### 변경 후 실제 화면

| Viewport/흐름 | 캡처 경로 | 관찰 |
|---|---|---|
| 1440×900 | `[경로]` | `[내용]` |
| 1280×800 | `[경로]` | `[내용]` |
| 1040×768 | `[경로]` | `[내용]` |
| 390×844 | `[경로]` | `[내용]` |
| 360×800 | `[경로]` | `[내용]` |

#### Developer 검사

| 검사 | 정확한 명령/절차 | 결과 | 실패·미실행 사유 |
|---|---|---|---|
| 자동 테스트 | `[값]` | `[통과/실패]` | `[값]` |
| 정적 검사 | `[값]` | `[통과/실패]` | `[값]` |
| 콘솔/런타임 | `[값]` | `[통과/실패]` | `[값]` |
| 자연 사용자 흐름 | `[값]` | `[통과/실패]` | `[값]` |
| 112.5% 텍스트 확대 | `[값]` | `[통과/실패]` | `[값]` |
| 키보드·포커스 | `[값]` | `[통과/실패]` | `[값]` |
| 수평 overflow | `[값]` | `[통과/실패]` | `[값]` |

#### 독립 PM & QA 평가

| 평가 항목 | 점수 / 10 | 근거 |
|---|---:|---|
| 정보 위계 | `[ ]` | `[ ]` |
| 시각적 완성도 | `[ ]` | `[ ]` |
| 가독성 | `[ ]` | `[ ]` |
| 조작 직관성 | `[ ]` | `[ ]` |
| 일관성 | `[ ]` | `[ ]` |
| 상용 완성도 | `[ ]` | `[ ]` |
| **평균** | **`[ ]`** | `[ ]` |

- 3초 안에 목적·대상·판단·다음 행동 파악: `[통과/실패]`
- P0: `[건수와 내용]`
- P1: `[건수와 내용]`
- 확인된 주요 결함: `[건수와 내용]`

#### SIMULATED_CONSUMER_REVIEW 5회

> 아래 평가는 실제 사람이 아닌 소비자 관점 모의평가로만 기록한다.

| 관점 | 3초 이해 | 종합점수 / 10 | 주요 불만 | 재사용 신뢰 |
|---|---|---:|---|---|
| 60–70대 농업 종사자 | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| 귀농 초보자 | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| 경험 많은 농가 운영자 | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| 모바일 중심 사용자 | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| 상용 SaaS 기대 사용자 | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| **평균** |  | **`[ ]`** |  |  |

#### 승인 게이트

| 게이트 | 기준 | 결과 | 상태 |
|---|---|---|---|
| PM 평균 | 8.5 이상 | `[ ]` | `[ ]` |
| PM 모든 항목 | 8.0 이상 | `[ ]` | `[ ]` |
| 소비자 모의평가 평균 | 8.5 이상 | `[ ]` | `[ ]` |
| 소비자별 점수 | 8.0 미만 0 | `[ ]` | `[ ]` |
| P0/P1/진행 차단 | 0 | `[ ]` | `[ ]` |
| 전체 자동 테스트 | 통과 | `[ ]` | `[ ]` |
| 실제 화면 검증 | 통과 | `[ ]` | `[ ]` |

#### 남은 위험

- `[검증하지 못한 환경·사용자·네트워크·데이터 위험]`

#### 변경 요청 원인 또는 승인 근거

- `[CHANGES_REQUESTED라면 실패 원인과 다음 revision 조치]`
- `[APPROVED라면 모든 게이트를 통과한 정확한 증거]`

## 사실과 미검증의 구분

### 확인된 사실

- R0 실제 실행 경로는 `http://127.0.0.1:4173`이다.
- 실제 자연 흐름의 R0 캡처 10개와 핵심 화면 모음 3개가 존재한다.
- 검사한 흐름에서 console log/error는 관찰되지 않았다.
- 브라우저 보고 내부 레이아웃 1280×720과 저장 캡처 크기 1041×720 또는 1029×712 사이에 차이가 있다.
- 캡처에는 오른쪽/CTA 잘림이 보인다.
- Root 평균은 5.8, 독립 PM & QA 평균은 5.7이다.
- P0 1건, P1 4건이 확인됐다.
- 저장소가 unborn 상태라 정확한 R0 commit SHA는 `NONE`이다.
- R0에서 제품 코드는 수정하지 않았다.

### 미검증 또는 승인 증거로 사용할 수 없는 항목

- 실제 모바일 화면 품질.
- 실제 60–70대 농업 종사자의 사용자 테스트 결과.
- 모든 브라우저·OS·네트워크에서의 폰트 로딩.
- 명시적 viewport-캡처 parity.
- full-page fixed overlay 중복 이미지는 캡처 artifact이므로 실제 제품 결함·해소 판단에 사용하지 않는다.
- 아직 수행하지 않은 자동 테스트, R1 실제 화면 검증, 독립 PM 재평가, 5회 소비자 모의평가.
