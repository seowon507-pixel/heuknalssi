# 흙날씨 UI–Backend v3 연결 가이드

## 연결 결과

기존 단일 HTML UI는 `backend-v3`의 실제 HTTP 계약에 연결된다.

```text
브라우저 http://localhost:3000
  ├─ 기존 UI HTML
  ├─ ui-integration/backend-client.mjs
  └─ /api/* 동일 출처 프록시
                    ↓
          backend-v3 http://127.0.0.1:3100
```

동일 출처 프록시를 사용하므로 브라우저가 `HttpOnly` 세션 쿠키를 정상
유지하고, `file://`의 `Origin: null` 문제를 피한다. 브라우저 코드는
`Cookie`나 `Origin` 헤더를 직접 만들지 않는다.

실제 연결 순서는 다음과 같다.

1. `GET /api/session`
2. `GET /api/health/preflight`
3. `POST /api/locations/current` 또는 `GET /api/locations?q=...`
4. 현재 위치 변환 후보를 확인하거나 검색 후보를 명시적으로 선택
5. 복수 작물을 작물별 독립 `POST /api/analyses` 요청으로 분리
6. 작물별 `GET /api/analyses/:id`
7. `POST /api/analyses/:id/report`
8. 완료될 때까지 `GET /api/analyses/:id`

분석 생성과 보고서 POST에는 메모리에 보관한 CSRF 토큰을 사용한다. 분석
생성에는 재시도 동안 유지되는 `Idempotency-Key`를 보낸다.

## 실행

Node.js 22 이상에서 프로젝트 루트 기준으로 실행한다.

### 개발 샘플 모드

```sh
node ui-integration/dev-server.mjs --sample
```

브라우저에서 <http://localhost:3000>을 연다.

이 모드는 세션·CSRF·위치 후보·분석·조회·보고서 전체 연결을 확인하기 위한
고정 개발 자료다. 모든 출처는 `deliveryState: "SAMPLE"`이며
`DO_NOT_USE_FOR_FARMING_DECISIONS` 표시를 갖는다. 실제 농업 판단에 사용하면
안 된다. backend-v3는 샘플 자료를 현재 자료나 `READY` 위험 판단으로
승격하지 않는다.

### 안전 기본 HOLD 모드

```sh
node ui-integration/dev-server.mjs --safe
```

검수 runtime과 외부 키를 전혀 사용하지 않는다. 위치 공급자도 닫혀 있으므로
UI에서 후보 없음과 운영 준비 `HOLD` 상태를 확인할 수 있다.

### 검수된 외부 runtime/키 모드

```sh
node \
  --env-file=/absolute/path/to/.env \
  ui-integration/dev-server.mjs --external
```

`--external`은 현재 셸 또는 명시한 env 파일의 backend 환경변수를 사용한다.
실제 키는 문서, HTML, JavaScript, `.env.example`, Git에 넣지 않는다.

- `DATA_GO_KR_SERVICE_KEY`가 있으면 기상청 단기·중기 어댑터와 검증된 parser
  계약을 활성화한다. 운영자가 `ENABLE_LIVE_KMA_SHORT=false` 또는
  `ENABLE_LIVE_KMA_MID=false`를 명시하면 그 설정을 우선한다.
- `KAKAO_REST_API_KEY`가 있으면 주소 후보 어댑터를 활성화한다. 키 존재는
  인증 성공을 뜻하지 않으므로 아래 live 검사를 통과해야 한다.
- 토양 API는 같은 공공데이터 키가 있어도 자동 활성화하지 않는다. 실제
  응답의 endpoint·필드·단위·구간 경계·면적·연도 계약이 검수돼야 한다.
- 검수 규칙과 위치 매핑을 조립할 때만
  `TRUSTED_BACKEND_RUNTIME_MODULE=/absolute/path/to/reviewed-runtime.mjs`를
  추가한다.
- `GOOGLE_AI_API_KEY`, `GEMINI_API_KEY`, `GOOGLE_API_KEY` 중 하나가 있으면
  농장 분석 도우미를 활성화한다. 키·주소·좌표·질문 원문은 브라우저 응답이나
  Google AI 입력에 포함하지 않고, 검증된 근거 ID 선택에만 사용한다.

외부 공급자를 안전하게 확인하려면 backend-v3에서 다음 검사를 실행한다.

```sh
node \
  --env-file=/absolute/path/to/.env \
  scripts/verify-live-providers.js
```

검사는 키를 출력하지 않고 공급자별 `configured`, `state`, 항목 수,
품질 플래그만 출력한다. 설정된 공급자 중 하나라도 `SUCCESS`가 아니면
종료 코드 1을 반환한다.

외부 배포를 `READY`로 만들려면 키 외에도 검수된 5개 작물 규칙, 최소 6개
지역 위치 매핑, 기후·관측·토양 계약이 필요하다.

## 화면 계약

- 위치 문자열만으로 확정하지 않고 서버 후보 토큰을 사용자가 선택한다.
- 현재 위치 좌표는 후보 변환 요청에만 사용하고 후보 응답·분석 결과·로그에
  반환하지 않는다.
- 기본 온보딩은 `ACTIVE_GROWING`이며 상황 선택을 다시 묻지 않는다.
- 사과·배의 작기는 클라이언트가 추측하거나 선택시키지 않고 서버의 단일
  검수 연간 프로필을 사용하도록 `season`을 생략한다.
- 현재 재배 중인 감자·오이·상추는 기기 날짜의 현재 월을 시작·종료 월이
  같은 `CUSTOM` 범위로 보내며 계절 이름을 다시 묻지 않는다. 월은 1~12
  정수만 허용한다.
- 오이·상추는 노지·시설흙·시설물만 확인하고 감자·사과·배는 노지로
  적용한다. 작기 차이는 현재 날짜와 생육단계로 구분한다.
- 여러 작물은 하나의 종합 요청으로 섞지 않고 작물 수만큼 독립 요청한다.
- 서버 enum에 없는 재배방식 “잘 모름”은 분석 전에 선택을 다시 요청한다.
- `LAND_SEARCH`에는 생육단계를 보내지 않는다.
- backend-v3에 없는 단일 종합점수, 55:45 가중치, 고정 18mm 예보, 목업
  근거를 실제 결과로 표시하지 않는다.
- 결과는 `state`, `conditionState`, `riskState`, `decision`, `actions`,
  모듈 상태, 출처, 한계를 그대로 보수적으로 표시한다.
- 대시보드 첫 화면은 `forecast.result.mergedDisplayDays`에서 최대 7일을
  읽어 최고기온 그래프, 날짜별 최저·최고기온, 강수확률, 단기/중기 공간
  수준으로 표시한다. 예보가 없으면 명시적 빈 상태를 표시한다.
- 샘플 runtime은 시각 흐름 검증을 위해 단기 3일·중기 4일의 고정 예보를
  제공하지만 항상 샘플로 표시하며 현재 위험 판단에 사용하지 않는다.
- 보고서는 자유형 AI 생성이 아니라 backend-v3의 결정론적 템플릿 결과다.
- 저장 동의가 있어도 현재 `persistenceState`가 `NOT_AVAILABLE`이면 저장됐다고
  표현하지 않는다.

## 검사

```sh
node --test ui-integration/*.test.mjs
cd backend-v3
npm run check
npm test
```

`npm test`는 로컬 HTTP 소켓을 사용하므로 실행 환경이 소켓 바인딩을
허용해야 한다.

실제 화면 검증은 데스크톱과 모바일 폭에서 다음 자연스러운 흐름으로 한다.

1. 서비스 연결 상태 확인
2. 현재 위치 또는 주소 검색으로 후보 확인
3. 복수 작물 선택
4. 작물별 재배환경·시기와 공통 생육단계 확인
5. 작물별 분석 생성과 결과 탭 전환
6. 모듈 상태·행동·출처·한계 확인
7. 보고서 생성과 완료 결과 확인
8. 콘솔 오류와 가로 넘침 확인

## 운영 전 남은 조건

개발 샘플 연결이 동작하는 것은 실제 공급자와 농업 규칙이 운영 준비됐다는
뜻이 아니다. 운영 배포 전 `/api/health/preflight`가 `READY`인지 확인하고,
`blockers`가 하나라도 남으면 UI에서도 운영 준비 보류로 유지한다.
