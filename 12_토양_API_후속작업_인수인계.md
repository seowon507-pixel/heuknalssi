# 토양 API 후속 작업 인수인계

## 1. 문서 목적

이 문서는 다음 개발자가 토양 API를 추가하거나 현재 연결을 보완할 때
제품의 안전 원칙과 기존 구조를 훼손하지 않도록 작업 범위와 검증 조건을
정리한 인수인계 문서다.

현재 제품은 공공데이터가 부족해도 값을 추측하지 않는다. 토양 자료가
없거나 의미가 다른 자료만 존재하면 `HOLD`, `PARTIAL`, `UNAVAILABLE` 중
정확한 상태를 반환하고 사용자가 다음에 확인할 행동을 안내한다.

## 2. 먼저 읽어야 하는 파일

작업 전에 아래 파일을 순서대로 읽는다.

1. `backend-v3/AGENTS.md`
2. `09_흙날씨진단_서비스로직_명세서.md`
3. `PRODUCT_FIRST_PRD.md`
4. `audit/PROJECT_AUDIT_2026-07-26.html`
5. `backend-v3/src/adapters/soil-v2.js`
6. `backend-v3/src/adapters/soil-field.js`
7. `backend-v3/src/domain/soil.js`
8. `backend-v3/src/application/services.js`
9. `backend-v3/runtime/reviewed-location-mappings.js`
10. 이 문서

## 3. 현재 구현과 연결 상태

### 3.1 농경지화학성 통계정보 V2

- 데이터셋: 공공데이터포털 `15144685`
- 역할: 법정동 단위 토양 pH 면적분포
- 현재 구현: `backend-v3/src/adapters/soil-v2.js`
- 지원 범위: `PH` 한 항목, 노지·시설·과수 토지이용별 구간 분포
- 중요한 한계: 지역 면적통계이며 선택한 필지에서 직접 측정한 pH가 아니다.
- 최근 실제 검증 상태: `UNSUPPORTED / LIVE_FEATURE_DISABLED`
- 다음 확인:
  - 해당 API의 활용신청 승인 여부
  - `DATA_GO_KR_SERVICE_KEY` 존재 여부
  - `ENABLE_LIVE_SOIL=true` 여부

### 3.2 토양도 기반 토양특성 상세정보 V3

- 데이터셋: 공공데이터포털 `15144225`
- 역할: 선택 필지의 토양도 기반 물리 특성
- 현재 구현: `backend-v3/src/adapters/soil-field.js`
- 현재 반환 항목:
  - 배수 코드
  - 유효 토심 코드
  - 표토 토성 코드
- 입력: 카카오 주소 결과에서 내부적으로 만든 19자리 PNU
- 중요한 한계:
  - 토양도 기반 자료이며 토양검정소에서 측정한 pH·EC 값이 아니다.
  - 코드 라벨이 공식 문서로 검수되기 전에는 임의의 한글 설명을 만들지 않는다.
- 최근 실제 검증 상태: `AUTH_ERROR`
- 다음 확인:
  - 해당 API의 별도 활용신청 승인 여부
  - 일반 인증키의 Decoding 값 사용 여부
  - `ENABLE_LIVE_SOIL_FIELD=true` 여부

### 3.3 실제 필지 토양검정 API

- 후보: 흙토람 `토양검정 API 신청`
- 목적: 실제 토양 시료의 pH, EC, 유기물, 유효인산 등 검정 결과 확보
- 현재 상태: 제품에 아직 연결되지 않음
- 주의:
  - 사용자가 선택한 필지와 검정 결과가 공식 식별자로 연결되는지 먼저 확인한다.
  - 공개 지역통계와 실제 필지 측정값을 같은 필드에 저장하지 않는다.
  - 사용자 동의나 접근 권한이 필요한 자료를 공개 API처럼 취급하지 않는다.
  - 검정일, 시료 위치, 단위, 검정기관이 없으면 현재 필지 측정값으로 사용하지 않는다.

## 4. 토양 데이터 구분 원칙

| 구분 | 의미 | 제품에서의 사용 | 금지 |
| --- | --- | --- | --- |
| 지역 화학성 통계 | 법정동 등 지역의 pH·EC 분포 또는 통계 | 지역 경향과 추가 검정 필요성 | 내 밭의 측정값처럼 표시 |
| 토양도 물리 특성 | PNU 기반 배수·토성·토심 등 지도 속성 | 배수·물리환경 참고 | 실험실 화학성 수치로 변환 |
| 필지 토양검정 | 실제 시료를 분석한 pH·EC 등 | 필지 화학성 판단의 가장 직접적인 근거 | 검정일·단위·필지 일치 확인 없이 사용 |
| 사용자 입력 | 사용자가 직접 입력한 측정값 | 입력 출처와 시각을 표시한 보조 근거 | 공공기관 측정값으로 위장 |

서로 다른 자료는 우선순위로 덮어쓰지 말고 의미가 다른 근거로 나란히
보관한다. 예를 들어 토양도 배수 코드는 실제 pH를 대체하지 않으며,
지역 pH 분포도 필지 토양검정값을 대체하지 않는다.

## 5. 반드시 지킬 프로그램 원칙

### 5.1 판단 안전성

- 기후·토양·관측·예보·SmartFarm을 합친 단일 종합점수를 만들지 않는다.
- 결측값은 `null`로 유지한다. 빈 문자열이나 결측을 `0`으로 바꾸지 않는다.
- 자료가 부족하면 `HOLD`, `PARTIAL`, `UNAVAILABLE`, `UNSUPPORTED`로 보류한다.
- 지역 통계만으로 내 밭이 안전하거나 위험하다고 확정하지 않는다.
- 검수되지 않은 코드 라벨, 기준 범위, 단위, 작물 민감도를 추측하지 않는다.
- 작물별 판단은 사과·배·오이·감자·상추의 검수된 규칙만 사용한다.
- 토양 정보가 행동으로 연결될 때는 `원인 → 위험 → 필요한 행동 → 재확인 시점`
  구조를 사용하되, 검수된 규칙에 없는 농약·비료·시비량 처방을 생성하지 않는다.

### 5.2 개인정보와 비밀정보

- `.env` 파일을 열거나 출력하거나 커밋하지 않는다.
- API 키를 URL, 로그, 오류 응답, 테스트 스냅샷에 남기지 않는다.
- 정확한 주소, 좌표, 19자리 PNU, 농가 ID를 LLM에 보내지 않는다.
- PNU는 요청과 응답의 필지 일치 검증에만 사용한 뒤 외부 응답에서 제거한다.
- 캐시 키에 PNU를 평문으로 넣지 않는다. 기존 HMAC 방식을 유지한다.
- 사용자 세션과 분석 소유권 검사를 우회하지 않는다.

### 5.3 외부 API 경계

- 공식 문서에서 확인한 HTTPS 호스트만 네트워크 허용목록에 추가한다.
- 공식 요청·응답 계약과 업데이트 날짜를 고정된 계약 버전으로 기록한다.
- 제공기관의 결과 코드, 무자료, 인증 실패, 스키마 변경을 구분한다.
- XML은 크기를 제한하고 `DOCTYPE`, `ENTITY`, 중첩 태그 삽입을 거부한다.
- 응답 행의 지역 코드 또는 PNU가 요청 대상과 일치하는지 확인한다.
- 단위와 구간 경계의 포함·제외 의미가 검수되지 않으면 판단에 사용하지 않는다.
- 재시도는 네트워크 오류·일시적 서버 오류·호출 제한처럼 재시도 가능한 경우로 제한한다.
- 출처명, 원문 URL, 공간 범위, 관측·발행·유효 시각, 어댑터 상태를
  `DataEnvelope`에 보존한다.

### 5.4 구조와 변경 범위

- 공급자 응답 파싱과 정규화는 `src/adapters`에 둔다.
- 결정론적 토양 판단은 `src/domain`에 둔다.
- 여러 자료의 호출과 조립은 `src/application`에 둔다.
- HTTP 입력 검증과 응답 변환은 `src/api` 또는 `server`에 둔다.
- 프론트엔드가 원문 공급자 코드나 내부 오류 코드를 해석하게 하지 않는다.
- 기존 헬퍼와 `DataEnvelope`, 캐시, 단일 실행, 실행 가드 구조를 재사용한다.
- 새 의존성은 기존 코드와 표준 기능으로 해결할 수 없을 때만 제안하고 승인 후 추가한다.

## 6. 권장 후속 작업 순서

### P0. 기존 토양 API 실제 연결 복구

1. 공공데이터포털에서 V2와 V3 각각 활용신청 상태를 확인한다.
2. 키 값을 출력하지 않고 사전점검 상태만 확인한다.
3. V2는 검수 지역 법정동 코드로 실제 응답을 확인한다.
4. V3는 정확한 주소에서 생성된 PNU로 실제 응답을 확인한다.
5. `AUTH_ERROR`, `NO_DATA`, `SCHEMA_CHANGED`를 혼동하지 않는다.
6. 실제 응답 상태만 기록하고 원문에 포함된 식별자는 보고서에서 제거한다.

### P1. 실제 필지 토양검정 자료 추가

1. 흙토람에서 토양검정 API 사용 자격과 인증 방식을 확인한다.
2. 필지 식별, 사용자 동의, 조회 가능 범위를 먼저 문서화한다.
3. 실제 제공 항목과 단위를 공식 명세로 고정한다.
4. 필지 일치, 검정일, 단위, 검정기관을 검증한다.
5. 지역 통계·토양도·필지 검정값을 서로 다른 출처와 공간 범위로 보관한다.
6. 검정값이 없으면 기존 `REQUEST_FIELD_SOIL_TEST` 행동을 유지한다.

### P2. 사용자 설명 개선

1. 배수·토성·토심 코드의 공식 라벨표를 검수한다.
2. 전문 용어를 쉬운 설명으로 바꾸되 원래 값과 단위를 보존한다.
3. 데이터가 없을 때는 백엔드 상세 오류 대신 사용자가 할 수 있는 다음 행동을 표시한다.
4. 토양 상태 카드에서 `지역 통계`, `필지 토양도`, `필지 검정`을 명확히 구분한다.

## 7. 수용 조건

아래 조건을 모두 만족해야 완료로 판단한다.

- V2와 V3의 실제 호출이 승인된 키로 각각 `SUCCESS` 또는 정당한 `NO_DATA`를 반환한다.
- 인증 실패를 샘플 데이터나 기본값으로 대체하지 않는다.
- 실제 필지 토양검정 API를 추가한 경우 필지·검정일·단위·기관 검증 테스트가 있다.
- 지역 통계가 필지 측정값으로 표시되는 경로가 없다.
- PNU·주소·좌표·API 키가 HTTP 응답, 로그, 테스트 결과, LLM 입력에 없다.
- 잘못된 PNU, 다른 필지 응답, 다중 행, 빈 값, 잘못된 단위, XML 공격 입력을 거부한다.
- 공급자 실패 시 다른 모듈 결과는 유지되고 토양 모듈만 안전하게 보류된다.
- 사용자 화면에서 자료 상태와 필요한 다음 행동을 이해할 수 있다.
- `npm run check`, `npm test`, `node --test ui-integration/*.test.mjs`가 모두 통과한다.
- 실제 자연 흐름에서 위치 선택 → 작물 선택 → 분석 → 토양 상태 확인을 검증한다.
- 최종 변경 검토에서 `.env`, 키, 원문 PNU, 임시 로그, 샘플 응답이 커밋에 포함되지 않는다.

## 8. 팀원이 AI에게 전달할 프롬프트

아래 내용을 그대로 복사한 뒤, 대괄호 부분만 실제 추가할 API 정보로 바꾼다.

```text
현재 저장소의 토양 API 후속 작업을 진행해 주세요.

작업 전에 반드시 아래 문서를 모두 읽고 지켜 주세요.
- backend-v3/AGENTS.md
- 09_흙날씨진단_서비스로직_명세서.md
- PRODUCT_FIRST_PRD.md
- audit/PROJECT_AUDIT_2026-07-26.html
- 12_토양_API_후속작업_인수인계.md

이번 목표:
1. 기존 농경지화학성 통계정보 V2와 토양특성 상세정보 V3의 실제 연결 상태를 진단합니다.
2. [추가할 토양 API 이름]([공식 문서 URL])을 기존 backend-v3 구조에 안전하게 연결합니다.
3. 지역 통계, 토양도 물리 특성, 실제 필지 토양검정값을 서로 다른 의미와 출처로 보존합니다.
4. 사과·배·오이·감자·상추 분석에서 검수된 근거만 사용하고, 결측 시 판단을 보류합니다.

절대 금지:
- .env 파일 열기, 출력, 복사, 커밋
- API 키, 주소, 좌표, 19자리 PNU, 농가 ID를 로그·응답·LLM에 노출
- 결측값을 0이나 평균값으로 채우기
- 지역 토양 통계를 내 밭의 실측값처럼 표시
- 기후·토양·예보·SmartFarm을 합친 종합점수 생성
- 검수되지 않은 코드 라벨, 단위, 기준값, 농약·비료·시비량 처방 생성
- 테스트를 통과시키기 위해 유효한 검증을 삭제하거나 약화

작업 방식:
1. 먼저 git 상태와 관련 코드·테스트·공식 API 명세를 읽고 현재 데이터 흐름을 설명해 주세요.
2. API 키 값은 보지 말고 기능별 READY/AUTH_ERROR/NO_DATA/UNSUPPORTED 상태만 확인해 주세요.
3. 공식 요청·응답 필드, 단위, 결과 코드, 공간 범위, 갱신 시각을 고정 계약으로 문서화해 주세요.
4. 공급자 파서는 src/adapters, 결정론적 판단은 src/domain, 조립은 src/application에 구현해 주세요.
5. DataEnvelope에 출처, 공간 범위, 시각, 상태, 품질 플래그를 보존해 주세요.
6. PNU는 요청·응답 일치 검증 후 제거하고 캐시에는 HMAC만 사용해 주세요.
7. 정상, 무자료, 인증 실패, 다른 필지, 스키마 변경, 결측, 이상치, XML 공격 입력 테스트를 추가해 주세요.
8. 사용자 출력은 확인된 사실과 원인 → 위험 → 필요한 행동 → 재확인 시점만 제공해 주세요.
9. 실제 API 호출 검증은 키나 식별자를 출력하지 말고 HTTP 상태, adapterState, sourceId만 보고해 주세요.
10. npm run check, npm test, node --test ui-integration/*.test.mjs와 실제 사용자 흐름을 검증해 주세요.

구현 전에 제출할 내용:
- 현재 토양 데이터 흐름
- 기존 연결 실패의 재현 결과와 원인
- 추가 API의 공식 계약 요약
- 변경할 파일과 테스트 계획

구현 후 제출할 내용:
- 변경 파일
- 지역 통계/토양도/필지 검정값이 구분되는 증거
- 실제 API의 안전한 상태 요약
- 전체 테스트 결과
- 남은 제한과 필요한 운영 설정

기존 개발자의 관련 없는 변경을 되돌리지 말고, 필요한 최소 범위만 수정해 주세요.
```

## 9. 현재 기준 검증 결과

- 백엔드 전체 테스트: `232/232` 통과
- UI 통합 테스트: `30/30` 통과
- 프로젝트 정적 검사: `80 files` 통과
- Google AI 분석 도우미 실제 호출: `GOOGLE_AI / SUCCESS`
- 주소 검색과 중기예보: 실제 연결 성공
- 단기예보: 실제 연결되었으나 반복 검증 중 호출 제한 발생
- ASOS와 토양 V3: 활용 권한 확인 필요
- 기후평년과 토양 V2: 활성화 설정 및 키 인식 상태 확인 필요
