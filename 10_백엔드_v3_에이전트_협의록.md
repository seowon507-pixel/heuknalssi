# 흙날씨.진단 Backend v3 에이전트 협의록

> 작성일: 2026-07-24  
> 목적: 백엔드 개선 과정의 제안·반론·결정·재평가 기록  
> 주의: 아래 참여자는 실제 사람이나 실제 채용 전문가가 아니라 독립 역할을 맡은 AI 서브에이전트 시뮬레이션이다. 내부 추론 전문이 아니라 검증 가능한 쟁점과 결정만 기록한다.

## 1. 참여 역할과 독립성

| 역할 | 에이전트 | 담당 | 개발 참여 |
|---|---|---|---|
| 통합 개발·조정 | 주 에이전트 | 명세 대조, 구현 통합, 독립 검증, 문서화 | 예 |
| 신규 도메인 개발자 | `domain_engine_dev` | 규칙, 기후·토양·예보, 작기, preflight | 예 |
| 신규 어댑터 개발자 | `adapter_dev` | Kakao·KMA·Soil, cache·retry·동시성 | 예 |
| 신규 API·보안 개발자 | `api_security_dev` | HTTP, 세션, CSRF, 소유권, 멱등성, 저장소 | 예 |
| 읽기 전용 구조 분석 | `rule_context_explorer` | 작기 입력공간과 matcher 구조 검토 | 아니오 |
| 기존 로직 평가자 | `backend_logic_reviewer` | 명세·도메인·결정론 평가 | **아니오** |
| 기존 통합 평가자 | `integration_reliability_reviewer` | 런타임·어댑터·HTTP·신뢰성 평가 | **아니오** |
| 기존 보안 평가자 | `security_test_reviewer` | 보안 경계·테스트·노출 평가 | **아니오** |

기존 평가자 세 명은 발견 사항만 제출했다. 수정은 신규 개발자 또는 주 에이전트가 담당했고, 수정 뒤에는 같은 평가자에게 다시 검증을 요청했다.

## 2. 초기 합의

| 발언 역할 | 제안 또는 우려 | 합의된 결정 |
|---|---|---|
| 로직 평가자 | v2의 기후·토양 합성점수와 결측 재가중은 근거 축을 왜곡한다. | 합성점수와 숨은 재가중을 폐기하고 각 축을 독립 상태로 유지한다. |
| 통합 평가자 | 실제 계약이 없는 API를 구현된 것처럼 취급하면 schema drift 때 잘못된 결론을 낸다. | 미검증 계약은 외부 호출 전 `HOLD/UNSUPPORTED`로 닫는다. |
| 보안 평가자 | 익명 서비스라도 세션 소유권, CSRF, rate limit, 요청 크기 제한이 필요하다. | 서명 세션과 서버측 소유권·Origin/CSRF·이중 rate limit을 적용한다. |
| 신규 개발자들 | 프론트엔드와 결합하면 로직 검증과 UI 상태가 섞인다. | 이번 범위는 독립 백엔드와 MD 문서로 제한하고 프론트엔드에 연결하지 않는다. |

## 3. Revision별 협의

### R1 — 실행 가능한 안전 경계

| 역할 | 핵심 의견 |
|---|---|
| 신규 도메인 개발자 | 기후·토양·예보를 순수 결정 함수로 분리하고 상태·근거·행동을 별도 산출해야 한다. |
| 신규 어댑터 개발자 | 모든 공급자 결과를 provenance와 시간 의미가 분리된 `DataEnvelope`로 정규화해야 한다. |
| 신규 API·보안 개발자 | 위치 후보 토큰과 분석 ID는 owner-bound opaque ID여야 한다. |
| 기존 평가자들 | 규칙 계약, 실패 격리, 저장소 포화, HTTP 보안이 충분하지 않다. |

결정: R1은 점수 68/62/60으로 `CHANGES_REQUESTED`. 기능 수보다 fail-closed 경계를 우선했다.

### R2 — 계약과 소유권 강화

| 쟁점 | 논의 | 결정 |
|---|---|---|
| 위치 후보 | 첫 번째 후보 자동선택은 광역행정구역을 필지로 오인할 수 있다. | 0·1·다건을 구분하고 사용자가 후보를 확인해야 분석 가능하게 한다. |
| 저장소 포화 | 활성 분석을 축출하면 소유자 조회가 예고 없이 사라진다. | 후보·분석·세션·멱등 저장은 기본 `reject`로 닫는다. |
| 공급자 실패 | 일부 응답이 살아 있어도 무위험을 확정하면 안 된다. | 부분 실패는 자료를 보존하되 `READY`와 무위험 결론을 차단한다. |
| Soil | 첫 행이나 평균을 대표값으로 쓰면 지역 면적통계 의미가 사라진다. | 구간·면적 배열만 허용하고 대표값 생성을 금지한다. |

결정: R2는 78/80/74로 개선됐지만 잔여 P1 때문에 `CHANGES_REQUESTED`.

### R3 — 동시성·실패 격리·보안

| 역할 | 제안 | 반영 |
|---|---|---|
| 통합 평가자 | 같은 요청을 합쳐도 한 사용자의 취소가 다른 사용자의 호출을 중단하면 안 된다. | waiter 취소를 격리하고 마지막 waiter가 떠날 때만 업스트림을 취소한다. |
| 통합 평가자 | 공급자 장애 때 무제한 동시 호출·재시도는 장애를 키운다. | 동시성·대기열·circuit·deadline·bounded retry를 추가한다. |
| 보안 평가자 | 전달 헤더를 무조건 신뢰하면 rate limit을 우회할 수 있다. | 직접 연결이 기본이며 명시적 proxy CIDR만 신뢰한다. |
| 보안 평가자 | JSON 표현만 다른 같은 요청이 멱등성을 우회할 수 있다. | 정규화된 도메인 요청을 의미론적으로 해시한다. |

결정: R3은 88/92/84. 구조는 크게 개선됐지만 잔여 주요 결함이 있어 계속 수정했다.

### R4 — 런타임 조립과 readiness

| 쟁점 | 합의 |
|---|---|
| 운영 조립 | 공식 `npm start`에서 운영자 소유 신뢰 런타임 모듈을 명시적으로 주입한다. |
| 미설정 자산 | 임의 fixture ID나 flag만으로 준비 완료를 만들지 않는다. |
| preflight | 규칙·지역 매핑·어댑터 계약과 실행 가능한 컨텍스트를 모두 확인한다. |
| 정보노출 | preflight는 안전한 상태·사유만 반환하고 규칙 ID·임계값·가중치·source URL을 반환하지 않는다. |

결정: 로직 93, 통합 95. 보안 평가는 중단되어 점수로 만들지 않았고, 컨텍스트 P1 때문에 승인하지 않았다.

### R5 — 작기 입력공간

기존 로직 평가자는 높은 평균점수와 무관하게 다음 P1을 제기했다.

- Soil을 season profile에 묶으면 `UNKNOWN`에서 명세와 달리 비활성화된다.
- `CUSTOM` 대표 한 달만 검사하면 나머지 월의 규칙 누락을 놓친다.
- 시설의 `CUSTOM`, `UNKNOWN`, `NOT_APPLICABLE`을 같은 적용 모듈로 검증해야 한다.

읽기 전용 `rule_context_explorer`의 제안:

1. Soil과 Forecast는 작기와 독립적으로 활성화한다.
2. Climate만 작기 profile과 CUSTOM 월 집합에 의존한다.
3. CUSTOM은 12개 단일월 검사로 모든 연속·연도순환 범위를 안전하게 커버한다.
4. 외부 preflight 응답은 12개 내부 검사를 월 커버리지 요약 한 건으로 축약한다.
5. Soil 충돌 키에서 season profile·기간·단위 차이로 중복 계상을 허용하지 않는다.

결정: R5는 95/98/96이었지만 P1 1건 때문에 `CHANGES_REQUESTED`.

### R6 — 작기 구조 교체

신규 도메인 개발자가 다음을 구현했다.

- 공통 요청 컨텍스트 matcher
- Soil·Forecast 작기 독립 활성화
- Climate의 CUSTOM 월 필터
- CUSTOM 12개 단일월 preflight
- 노지 `UNKNOWN`의 `EXPECTED_PARTIAL`
- 시설 3종 작기의 동일 적용 모듈
- Soil 중복 계상 충돌 차단
- reviewed profile 수와 무관한 bounded preflight 요약

검증 결과:

- 기존 R5 작기 P1은 해소됐다.
- 188/188 테스트와 61파일 정적 검사를 통과했다.
- 로직 평가자는 reviewed profile 8개와 500개에서 preflight 크기가 각각 16,848B와 16,850B여서 고정 크기 요약임을 확인했다.

그러나 같은 로직 평가자가 새 P1을 재현했다.

| 조건 | preflight | 실제 Forecast |
|---|---|---|
| Forecast 규칙 `stage:"UNSPECIFIED"` + baseline 요청 | 활성 규칙으로 계산 | `ANY`가 아니므로 제외 |

그 결과 완전 구성처럼 보이는 preflight가 실제로 실행되지 않는 Forecast 규칙을 승인할 수 있었다.

결정: R6 점수 96/98/97, 평균 97.0이지만 P1 때문에 `CHANGES_REQUESTED`.

### R7 — preflight와 실제 실행의 단일 stage 계약

같은 본질의 matcher 불일치가 반복됐으므로 preflight에 예외 조건을 추가하는 국소 패치를 하지 않았다.

| 역할 | 제안 또는 검증 |
|---|---|
| 로직 평가자 | registry와 Forecast 실행이 동일 matcher를 사용하고 활성 규칙 수가 같아야 한다. |
| 신규 도메인 개발자 | 공통 `ruleStageMatchesRequestContext`를 만들고 Forecast의 중복 로컬 matcher를 제거한다. |
| 주 에이전트 | Climate·Soil의 기존 semantics를 보존하고 `ANY/UNSPECIFIED/FLOWERING` 동등성 테스트를 요구한다. |
| 통합 평가자 | preflight 표본도 실제 요청 정규화와 같은 registry resolve 경로를 쓰는지 확인한다. |
| 보안 평가자 | 변경 후 preflight가 내부 규칙 상세를 새로 노출하지 않는지 확인한다. |

최종 동작:

| 요청 stage | Forecast 활성 규칙 |
|---|---|
| `UNSPECIFIED` | `ANY`만 |
| `FLOWERING` | `ANY` + `FLOWERING` |
| 다른 구체 stage | `ANY` + 동일 stage |

Climate·Soil의 기존 `UNSPECIFIED` 정확 일치 의미는 유지했다.

## 4. Revision 7 독립 재평가 대화 요약

### 기존 백엔드 로직 평가자

- R6의 잘못된 preflight 재현이 R7에서 `ready:false`, `completeCropCount:0`, Forecast `HOLD`로 바뀐 것을 확인했다.
- registry와 실제 Forecast의 활성 규칙 수를 baseline, `ANY`, `FLOWERING`에서 직접 대조했다.
- R6의 UNKNOWN·시설 3작기·CUSTOM 12월·11→2·Soil 충돌 회귀가 없음을 확인했다.
- 판정: **99/100, P0 0, P1 0, P2 1, 내부 `READY`**.

### 기존 통합·신뢰성 평가자

- registry, 실제 Forecast, 애플리케이션, preflight가 같은 정규화·resolve 경로를 사용하는지 확인했다.
- KMA 부분 실패, Soil contract drift 0-fetch, SingleFlight 취소 격리, batch rollback, 저장소 fail-closed 회귀가 없음을 확인했다.
- 실제 `npm start`와 preflight HTTP 200을 다시 확인했다.
- 판정: **98/100, P0 0, P1 0, P2 2, 내부 `READY`**.

### 기존 보안·테스트 평가자

- 세션·CSRF·소유권 비열거·proxy CIDR·멱등성·rate limit·본문 제한·최소 로그가 유지됨을 확인했다.
- preflight에 규칙 ID·임계값·가중치·출처 URL이 추가되지 않았음을 확인했다.
- 판정: **97/100, P0 0, P1 0, P2 0, 내부 `READY`**.

세 평가자 모두 코드와 문서를 수정하지 않았다.

## 5. 최종 합의

| 질문 | 합의 |
|---|---|
| 내부 도메인·API 코드가 다음 단계로 넘어갈 수 있는가? | 예, `READY` |
| 확인된 P0/P1이 남아 있는가? | 아니오 |
| 자동 테스트가 전부 통과했는가? | 예, 191/191 |
| 프론트엔드와 연결했는가? | 아니오 |
| 기존 v2를 수정했는가? | 아니오 |
| 실제 운영 배포가 가능한가? | 아니오, 외부 검수 자산 전까지 `HOLD` |
| 높은 점수만으로 외부 자산 부재를 무시했는가? | 아니오 |

## 6. 남은 이견이 아닌 추적 항목

평가자들은 다음을 현재 P0/P1 결함이 아닌 후속 부채로 분류했다.

- `FAILED_INPUT` 생명주기의 감사 가능한 기록
- 공식 `npm start`·신뢰 런타임 모듈의 자동 child-process E2E
- 다작물·시설·부분실패 전체 HTTP E2E 매트릭스
- 다중 인스턴스 전 공유 저장소·분산 rate limit
- 실규칙·6지역 매핑·기후/ASOS/Soil/Kakao/KMA 실제 계약 검수

## 7. 실행 증거

| 항목 | 결과 |
|---|---|
| 전체 테스트 | 191/191 |
| 정적 검사 | 61파일 |
| 커버리지 | line 89.55%, branch 77.29%, function 91.57% |
| 실제 HTTP E2E | 1/1 |
| 공식 실행 | 정상 listen |
| preflight | HTTP 200, `ready:false`, `serviceState:"HOLD"` |
| 프론트 파일 혼입 | 0 |
| 대표 secret 패턴 | 0 |
| Commit SHA | `NONE` — 저장소에 `HEAD` 없음 |

