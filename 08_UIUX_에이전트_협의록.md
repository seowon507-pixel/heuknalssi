# 흙날씨 진단 UI·UX v3 전문가 협의록

> 문서 목적: 사용자 문제정의와 추가 요구를 기준으로 네 UI·UX 전문 역할이 제안·반론·합의를 거쳐 만든 v3 프로토타입의 정보구조, 시각체계, 구현 범위와 검증 증거를 기록한다.  
> 최상위 문제정의: [사용자 제공 메인 문제정의서](https://lean-mahogany-686.notion.site/1-39b09c484bff80b6a7e3c41842def27a)  
> 대상 프로토타입: [08_흙날씨진단_UI_샘플.html](./08_흙날씨진단_UI_샘플.html)  
> 작성 기준일: 2026-07-23  
> 문서 상태: v3 구현 및 정적·브라우저·E2E 검수 완료

이 문서는 내부 사고과정을 전재하지 않는다. 검토 가능한 제안, 반론, 결정, 구현 사실과 검증 결과만 기록한다. 화면의 지역·기후·토양·예보·참고지수는 모두 **체험용 fixture**이며 실제 영농 또는 농지 매입 판단에 사용할 수 없다.

---

## 1. 기준과 문제 정의

### 1.1 메인 문제정의서에서 지켜야 할 최소 기준

- 초보 귀농인이 희망지역과 희망작물을 입력한다.
- 공공 토양·기상 자료를 작물 조건과 비교해 적합성과 위험을 분석한다.
- 현재 조건뿐 아니라 시계열 관측과 예보를 이용해 위험을 선제적으로 알린다.
- 지원 작물은 사과·배·오이·감자·상추 5종이다.
- 변수별 가중치, 적정범위 이탈정도, 작물 민감도, 시계열 예보, 결측·이상치 방어를 포함한다.
- 결과는 직관적인 지수 또는 대시보드로 제공하고, LLM은 전문용어를 쉬운 말로 바꾼다.

### 1.2 사용자 추가 요구

- 기존 시안의 Dribbble식 검정·라임·대형 카드 조합에서 느껴지는 “AI가 만든 화면” 인상을 제거한다.
- 한국 농업 종사자의 연령대를 고려해 처음부터 쉬운 질문으로 필요한 정보를 받는다.
- 대시보드에서 필요한 정보와 다음 행동을 한눈에 확인하게 한다.
- 사용자가 직접 해석하기 전에 서비스가 자료를 정리하고 우선순위를 제시한다.
- 데스크톱 웹과 모바일 웹에서 같은 업무를 수행할 수 있어야 한다.
- 메인 문제정의서는 최소 기준이며, 반복사용·접근성·설명가능성까지 제품 수준으로 보완한다.

### 1.3 v3 성공 조건

1. 첫 사용자가 전문용어 없이 최대 5단계 안에 분석 조건을 완성한다.
2. 농지를 알아보는 사람과 이미 재배 중인 사람에게 같은 계산 결과를 다른 행동 우선순위로 제시한다.
3. 대시보드, 저장한 분석, 개인·화면설정이 명확한 세 메뉴로 분리된다.
4. 참고지수는 검증 가능한 규칙으로 계산되며 성공확률처럼 보이지 않는다.
5. 결측·지연·오류·오프라인·계산보류가 정상 화면과 구별된다.
6. 모바일 320px 폭과 큰 글씨 설정에서도 주요 선택과 행동이 잘리지 않는다.

## 2. 참여 구조

| 참여 역할 | 책임 | 최종 기여 |
|---|---|---|
| 전문가 1 — 고령 농업인 온보딩 | 질문 순서, 쉬운 표현, 모름·주소 불명 분기, 48px 조작 | 상황부터 묻는 5단계 입력과 단계별 검증 |
| 전문가 2 — 제품·대시보드·소비자 반복사용 | 메뉴 구조, 저장·재방문, 준비 중/재배 중 업무 차이 | `대시보드 / 내 분석 / 마이페이지`, 저장과 농장 전환 |
| 전문가 3 — 인간적 시각언어·접근성 | AI 클리셰 제거, 토큰, 정보밀도, 글자·대비·반응형 | 흰색 업무화면, 18px 본문, 큰 글씨·고대비 설정 |
| 전문가 4 — 프로토타입 통합·구현 | 합의안을 HTML/CSS/vanilla JS 상태모델로 통합 | 온보딩, 결과, 저장, 할 일, 데이터 상태, 근거 대화상자 |
| 지원 검수 | 정적 구조, 브라우저 레이아웃, E2E·포커스 회귀검사 | 세 뷰포트와 주요 분기·상태 검증 |

네 전문가는 각 영역을 독립 검토한 뒤 제품정의·접근성·과학적 정직성을 함께 만족하는 안만 공동 결정으로 채택했다.

## 3. v2에서 폐기한 방향

v2의 지도 중심 Dribbble 대시보드와 검정·형광 라임 시각체계는 사용자 피드백 및 핵심 사용 시나리오와 맞지 않아 v3 기준에서 폐기했다.

- 대형 검정 명령 패널과 형광 라임 강조
- 영문 eyebrow와 대문자 `SAMPLE MODE`
- 22~30px의 과도한 카드 반경과 큰 확산 그림자
- 첫 화면 대부분을 차지하는 가짜 필지 SVG 지도
- 아이콘을 어두운 둥근 상자에 넣는 반복 패턴
- 9~12px 초소형 레이블과 과도한 제목 크기 대비
- `개요 / 근거 / 자료`를 제품 전체 메뉴처럼 사용한 구조
- 조건 입력을 결과 화면 옆 drawer에만 숨긴 흐름
- 실제로 저장하거나 다시 방문할 이유가 없는 일회성 분석

v3의 목표는 “멋있어 보이는 AI 대시보드”가 아니라 **초보 귀농인과 고령 농업인이 이해하고 다시 사용할 수 있는 한국형 농업 의사결정 도구**다.

## 4. 핵심 이견과 최종 합의

### 4.1 세 번째 메뉴: 오늘 할 일, 내 농장, 내 분석

**제안**

- `오늘 할 일`: 재배 중 사용자의 반복사용과 현장 행동에 유리하다.
- `내 농장`: 실제 농업인의 필지 관리에는 명확하지만 토지를 아직 소유하지 않은 귀농 준비자를 배제할 수 있다.
- `내 분석`: 관심 지역·작물 저장, 비교, 변화 확인, 실제 농장 전환을 모두 수용할 수 있다.

**반론**

- `내 분석`은 이름만으로 즉시 행동을 떠올리기 어렵다.
- `오늘 할 일`을 독립 메뉴로 두면 준비 중 사용자에게 빈 화면이 될 수 있다.

**결정**

- 전역 메뉴는 `대시보드 / 내 분석 / 마이페이지`로 확정했다.
- 오늘 할 일은 재배 중 사용자에게 대시보드 최상단 업무 모듈로 노출한다.
- 저장된 관심지역은 `내 분석`에서 실제 재배가 시작되면 농장으로 전환한다.
- 이 구조는 토지 미소유 귀농 준비자와 재배 중 농업인을 같은 제품 안에서 연결한다.

### 4.2 숫자점수 회피와 과제의 적합도 요구

**제안**

- 숫자 하나는 성공확률·토지가치처럼 오해될 수 있으므로 문장 상태만 제시하자는 의견이 있었다.
- 문제정의서의 직관적 적합도 요구와 비교 가능성을 위해 수치가 필요하다는 의견도 있었다.

**결정**

- 이름을 `재배환경 참고지수`로 제한한다.
- 단일 숫자 옆에 `추정 범위`, `자료 상태`, `기후/토양 분해`, `주요 감점 이유`를 함께 표시한다.
- 근거 대화상자에서 관측값, 공식 범위 예시, 이탈, 민감도, 가중치, 감점을 행별로 공개한다.
- `수확량·성공확률·안전등급·토지가치가 아니다`라는 비확률 고지를 결과 가까이에 둔다.
- 핵심 자료가 없으면 임의 보간하지 않고 점수를 `—`로 바꿔 계산을 보류한다.

### 4.3 지도 우선과 판단 우선

**제안**

- 공간정보 서비스의 인상을 위해 지도를 첫 화면 중심에 두자는 안이 있었다.
- 등록되지 않은 가상 지도는 실제 필지 분석처럼 오해되고, 첫 행동을 아래로 미룬다는 반론이 있었다.

**결정**

- 지도·위성은 대시보드 하단의 보조 관찰 기능으로 이동한다.
- 필지가 없으면 실제 지도처럼 꾸미지 않고 `.map-empty`에 `등록된 필지가 없습니다`를 표시한다.
- 위성은 생육 확정판정이 아니라 필지 등록 후 현장기록과 비교하는 변화 관찰 기능으로 정의한다.

### 4.4 LLM의 계산 권한

**제안**

- 자연어 모델이 자료를 해석하고 점수까지 만들면 빠르게 다양한 작물을 지원할 수 있다는 의견이 있었다.
- 동일 입력의 재현성, 산식 검증, 환각·누락값 추정 위험 때문에 계산 권한을 주면 안 된다는 반론이 있었다.

**결정**

- 점수는 공식 범위 예시, 작물 민감도, 변수 가중치, 이탈거리로 구성된 규칙엔진이 계산한다.
- LLM은 검증된 구조 결과만 받아 쉬운 한국어 요약과 행동 설명으로 번역한다.
- LLM은 점수 계산, 공식 범위 변경, 누락값 추정 또는 실제 적합성 확정을 하지 않는다.

### 4.5 준비 중과 재배 중

**결정**

- 두 상황은 같은 지역·작물 규칙과 같은 참고지수 계산을 사용한다.
- `농지를 알아보는 중`은 장기 환경과 현장 방문 확인을 우선한다.
- `이미 농사 중`은 1~3일 예보와 오늘 할 일을 먼저 보여준다.
- 예보를 장기 참고지수에 섞지 않고 가까운 위험으로 별도 표시한다.

## 5. 역할별 제안·반론·결정

### 5.1 전문가 1 — 고령 농업인 온보딩

**제안**

- 서비스가 먼저 `농지를 알아보는 중 / 이미 농사 중`을 묻고 이후 문구와 행동 순서를 바꾼다.
- 한 화면에 한 질문만 두고 단계는 `1 / 5`처럼 읽을 수 있게 표시한다.
- 상세 주소가 아니라 시·군·읍·면 정도만 요구한다.
- 어려운 질문에는 `주소를 잘 모르겠어요`, `잘 모름`을 동등한 선택으로 제공한다.
- 마지막 단계에서 모든 입력을 검토하고 항목별로 바로 수정하게 한다.

**반론**

- 작물·환경·시기·생육단계를 모두 받으면 단계가 길어질 수 있다.
- 오이·상추에만 환경 질문이 필요하고, 재배 중 사용자에게만 생육단계가 필요하다.

**결정**

- 최대 5단계를 유지하되 조건부 질문을 같은 단계 안에서 펼친다.
- 순서는 `상황 → 지역 → 작물·환경 → 시기·단계 → 확인`이다.
- 빈 지역 입력으로 다음 단계에 갈 수 없으며, 주소 불명 분기를 선택하면 넓은 지역 자료의 한계를 설명한다.
- 버튼·입력은 기본 52px, 보조 조작도 48px 이상으로 한다.

### 5.2 전문가 2 — 제품·대시보드·반복사용

**제안**

- 대시보드는 `현재 판단 → 오늘 할 일 또는 첫 행동 → 가까운 위험 → 참고지수와 이유 → 자료상태 → 쉬운 설명 → 상세근거` 순으로 구성한다.
- 저장 동의는 선택이며 기본값을 꺼 둔다.
- 저장한 분석은 지역·작물·상황·최근 확인·지수를 한 카드에서 비교한다.
- 실제 재배가 시작되면 저장한 분석을 농장으로 전환해 오늘 할 일을 활성화한다.

**반론**

- 자동 저장은 편하지만 위치·관심지역 정보에 대한 동의가 필요하다.
- 모든 사용자에게 오늘 할 일을 보여주면 준비 중 사용자에게 실제 생육 경고처럼 보일 수 있다.

**결정**

- `#save-consent`를 선택·기본 꺼짐으로 두고 저장하지 않아도 분석할 수 있다.
- 준비 중에는 현장 방문의 첫 행동만, 재배 중에는 `#today-task-panel`을 노출한다.
- 작업은 완료·나중에 알림·현장과 다름 상태를 지원하고 같은 세션의 UI에 반영한다.

### 5.3 전문가 3 — 인간적 시각언어·접근성

**제안**

- 흰색 업무 화면, 얇은 경계선, 자연스러운 녹색 한 가지를 주색으로 사용한다.
- 큰 검정 패널, 형광색, 영문 장식문구, 유리효과, 가짜 지도를 사용하지 않는다.
- 기본 글자를 18px로 올리고 모든 선택·버튼을 고령 사용자 기준으로 설계한다.
- 결과를 독립된 둥근 카드로 계속 나누지 말고 패널 내부 행과 구분선으로 묶는다.
- 마이페이지에 큰 글씨와 고대비 설정을 제공한다.

**반론**

- 정보밀도를 높이면서 글자와 터치영역을 키우면 첫 화면 길이가 늘어날 수 있다.
- 점수·위험·출처를 모두 같은 강도로 표시하면 판단 우선순위가 약해진다.

**결정**

- 제목보다 본문·수치·행동을 우선하고, 12px 반경 이하와 기본 무그림자를 사용한다.
- 주의는 옅은 주황 배경과 좌측 선, 정보는 파랑, 정상은 초록으로 의미를 분리한다.
- 모바일에서는 장식이 아니라 정보 순서를 재배치하고, 하단 메뉴를 사용한다.

### 5.4 전문가 4 — 프로토타입 통합·구현

**제안**

- 외부 프레임워크 없이 단일 HTML에서 화면·상태·포커스 흐름을 검증한다.
- 온보딩 입력이 결과 문구, 규칙표, 저장 카드, 오늘 할 일에 같은 상태로 전달되게 한다.
- 정상 데이터만 보여주지 않고 일부 부족·지연·오류·오프라인·계산보류를 직접 전환해 검수한다.
- 근거는 네이티브 `<dialog>`와 표로 제공하고 닫은 뒤 호출 지점으로 포커스를 돌린다.

**반론**

- 체험 fixture가 실제 분석처럼 보이지 않도록 모든 결과와 산식의 예시 성격을 명확히 해야 한다.
- 단일 파일 상태는 새로고침 뒤 영구 저장을 보장하지 않는다.

**결정**

- 모든 결과에 체험용 표기와 비확률 고지를 둔다.
- 서버 저장으로 표현하지 않고 현재 페이지 세션 안의 상호작용임을 마이페이지 개인정보 설명에 밝힌다.
- 실제 API가 없을 때 성공한 것처럼 가장하지 않고 결측·오류 상태를 독립적으로 시연한다.

## 6. 최종 정보구조와 사용자 시나리오

### 6.1 메뉴 구조

| 메뉴 | 대상 업무 | 핵심 내용 |
|---|---|---|
| 대시보드 | 현재 결과와 우선 행동 확인 | 조건, 현재 판단, 오늘 할 일/첫 행동, 예보 위험, 참고지수, 자료상태, 쉬운 설명, 근거, 위성 빈상태 |
| 내 분석 | 관심지역 저장과 변화·농장 전환 | 저장 카드, 최근 변화, 결과 보기, 실제 재배로 전환 |
| 마이페이지 | 개인정보·알림·화면 편의 | 기본 지역, 글자 크기, 고대비, 위험 알림, 개인정보 원칙 |

데스크톱은 `.sidebar`, 모바일은 `.bottom-nav`를 사용하며 같은 `.nav-button[data-view]` 상태로 `#view-dashboard`, `#view-analyses`, `#view-mypage`를 전환한다.

### 6.2 첫 사용자 흐름

1. `#onboarding`이 첫 진입에 열리고 현재 상황을 묻는다.
2. 지역을 직접 입력하거나 `#region-unknown`으로 넓은 지역 기준을 선택한다.
3. 사과·배·오이·감자·상추 중 하나를 선택한다. 오이·상추에는 노지·시설흙·시설물·잘 모름 환경 분기가 추가된다.
4. 작물별 재배시기를 선택한다. 재배 중이면 현재 생육단계를 추가로 묻는다.
5. 확인 화면에서 상황·지역·작물·환경·시기·단계를 검토하고 선택적으로 내 분석 저장에 동의한다.
6. 체험 분석을 실행하면 대시보드 제목으로 이동해 결과와 우선 행동을 확인한다.

### 6.3 준비 중 사용자

- `#dashboard-mode-copy`가 농지를 알아보는 사람의 확인 순서를 안내한다.
- 가까운 예보를 실제 생육 위험으로 단정하지 않는다.
- `#first-action`은 현장 방문 때 물 고임과 배수로를 확인하라고 안내한다.
- 저장한 관심지역은 내 분석에서 다시 보거나 실제 농장으로 전환할 수 있다.

### 6.4 재배 중 사용자

- 동일한 규칙 계산 결과를 사용하되 `#today-task-panel`이 먼저 나타난다.
- `#drainage-task`에서 완료·나중에 알림·현장과 다름을 기록한다.
- `#risk-context`와 `#first-action`이 가까운 예보와 현재 생육단계에 맞는 행동으로 바뀐다.

## 7. 현재 구현과 selector 증거

| 기능 | 구현 selector 또는 요소 | 동작 |
|---|---|---|
| 데스크톱·모바일 메뉴 | `.sidebar`, `.bottom-nav`, `.nav-button[data-view]` | 3개 페이지 전환과 `aria-current` 동기화 |
| 5단계 온보딩 | `#onboarding`, `.wizard-step[data-step="1"…"5"]` | 단계 검증, 이전·다음, 진행률, 확인·수정 |
| 상황 분기 | `input[name="situation"]` | planning/growing에 따라 대시보드 우선순위 변경 |
| 지역 직접입력·모름 | `#region-input`, `#region-unknown`, `#use-broad-region` | 빈 입력 차단, 넓은 지역 사용과 한계 안내 |
| 5종 작물 | `input[name="crop"]` | apple/pear/cucumber/potato/lettuce 규칙 전환 |
| 재배환경 분기 | `#cultivation-group`, `input[name="cultivation"]` | 오이·상추의 노지·시설·잘 모름 처리 |
| 시기·생육단계 | `#season-options`, `#growth-group` | 작물별 시기, 재배 중 생육단계 입력 |
| 결과 지수 | `#score-value`, `#score-range`, `#score-state` | 참고지수·추정범위·자료상태 표시 |
| 오늘 할 일 상태 | `#today-task-panel`, `#drainage-task`, `[data-task-action]` | 완료·알림·현장차이 상태 반영 |
| 저장·농장 전환 | `#save-consent`, `[data-analysis-action]` | 저장 카드 추가, 결과 보기, 실제 재배 전환 |
| 데이터 방어상태 | `#data-state-details`, `[data-data-state]` | ready/partial/stale/error/offline/hold 시연 |
| 쉬운 설명 | `#ai-summary`, `.plain-steps` | 구조화 결과의 쉬운 문장과 행동 단계 |
| 계산·근거 | `#evidence-dialog`, `.calc-table`, `#calculation-rule-copy` | 6개 변수, 산식, 시계열, 결측·이상치, 한계 공개 |
| 화면 편의 | `input[name="font-size-setting"]`, `#contrast-setting` | 기본/큰 글씨와 고대비 전환 |
| 위성 보조기능 | `#satellite-title`, `.map-empty` | 필지 미등록 빈상태와 오해방지 설명 |
| 접근성 상태 안내 | `#live-region`, `:focus-visible`, `.skip-link` | 상태 변화 알림, 포커스 표시, 본문 건너뛰기 |

프로토타입은 외부 CSS, JavaScript, 이미지, 폰트 또는 네트워크 요청 없이 동작하는 단일 HTML이다.

## 8. 최종 시각·접근성 체계

### 8.1 시각 토큰

```css
--bg: #F4F6F2;
--surface: #FFFFFF;
--text: #202520;
--muted: #5D665E;
--border: #D8DDD7;
--primary: #2F6B3B;
--radius: 8px;
--radius-lg: 12px;
```

- 일반 패널은 그림자 없이 1px 경계선으로 구분한다.
- 장식적 그라데이션, 유리효과, 형광 라임, 대형 검정 패널을 사용하지 않는다.
- 영문 eyebrow와 임시 아이콘 로고를 사용하지 않는다.
- 기본 본문은 18px이며 큰 글씨 설정은 20.16px다.
- 기본 버튼·입력은 52px, 보조 조작도 48px 이상이다.
- 지도는 실제 필지가 있을 때만 연결할 후순위 기능이며 현재는 빈상태다.

### 8.2 대표 색 대비

| 전경 / 배경 | 대비비 | 일반 텍스트 WCAG AA |
|---|---:|---|
| `#202520` / `#F4F6F2` | 14.34:1 | 통과 |
| `#5D665E` / `#FFFFFF` | 5.95:1 | 통과 |
| `#FFFFFF` / `#2F6B3B` | 6.39:1 | 통과 |
| `#9A5A00` / `#FFF2D4` | 4.93:1 | 통과 |
| `#A53932` / `#FBE9E7` | 5.54:1 | 통과 |

### 8.3 고령 사용자 기준

- 한 화면 한 질문, 쉬운 예시, `잘 모름` 선택을 제공한다.
- 전문용어만 단독으로 제시하지 않고 관측값의 의미와 다음 행동을 함께 쓴다.
- 색에만 의존하지 않고 상태명, 경계선과 설명을 함께 사용한다.
- 가로 드래그나 hover 전용 조작 없이 키보드와 터치로 동일 업무를 수행한다.
- 마이페이지에서 큰 글씨와 고대비를 즉시 전환한다.
- 모바일 320px에서 단일 열 선택과 균등한 이전·다음 버튼을 사용한다.

## 9. 계산·LLM·데이터 방어 원칙

### 9.1 참고지수

- 공식 적정범위 예시에서 벗어난 거리, 작물 민감도와 변수 가중치로 감점을 계산한다.
- 기후와 토양 분야 안에서 정규화한 뒤 분야 가중치를 적용한다.
- 결과에는 점수만 두지 않고 추정범위, 자료 충족도, 주요 이유를 함께 표시한다.
- 단기예보는 변동성이 크므로 장기 참고지수에 합산하지 않고 향후 10일 위험으로 분리한다.

### 9.2 결측·이상치

- 결측값을 임의 평균이나 LLM 추정치로 채우지 않는다.
- 자료가 일부 부족하면 충족도를 낮추고 추정범위를 넓힌다.
- 핵심 토양자료가 없으면 계산을 보류하고 점수를 `—`로 표시한다.
- 센서 허용범위와 이웃 관측을 크게 벗어난 이상치는 계산에서 제외하고 자료상태에 표시한다.

### 9.3 LLM

- 입력: 검증된 구조화 결과, 자료상태, 계산 이유, 사용자의 준비/재배 상황.
- 출력: 쉬운 요약, 왜 확인해야 하는지, 첫 행동과 확인 순서.
- 금지: 점수 계산, 누락값 보간, 공식 범위 생성·변경, 성공확률 또는 토지가치 단정.

## 10. 검증 증거

### 10.1 정적 검사

| 항목 | 결과 |
|---|---:|
| 파일 크기 | 99,866 bytes |
| 줄 수 | 1,473 lines |
| JavaScript 문법 | 통과 |
| 전체 ID | 102개 |
| 중복 ID | 0개 |
| 존재하지 않는 ARIA 참조 대상 | 0개 |
| button | 34개 |
| 접근 가능한 이름 없는 button | 0개 |
| 외부 의존성 | 0개 |

### 10.2 브라우저 레이아웃·접근성

| 검사항목 | 1440×900 | 390×844 | 320×800 |
|---|---:|---:|---:|
| 가로 overflow | 0 | 0 | 0 |
| 현재 보이는 조작 중 48px 미만 | 0 | 0 | 0 |
| 내비게이션 | 좌측 sidebar | 하단 menu | 하단 menu |
| 기본 글자 | 18px | 18px | 18px |
| 큰 글씨 설정 | 20.16px | 20.16px | 20.16px |

- 320×800 큰 글씨에서도 온보딩 선택지와 footer의 이전·다음 버튼이 잘리지 않았다.
- 모바일 `농지를 알아보는 중` 390×844 화면에서 headline status는 y=303, bottom=340이었다.
- 같은 화면의 첫 행동은 bottom=697로 844px 첫 뷰포트 안에 들어왔다.
- 데스크톱 sidebar와 모바일 bottom navigation 전환을 실제 렌더링에서 확인했다.

### 10.3 E2E 시나리오

- [x] planning → 지역 직접입력 → 감자 → 봄 감자 → 확인 → 저장 → 참고지수 62 결과
- [x] 저장 동의 후 내 분석 카드 1개에서 2개로 증가
- [x] 저장한 분석을 실제 재배 중으로 전환하면 오늘 할 일이 보이고 첫 행동 문구가 변경
- [x] 배수로 작업 완료 상태가 작업 UI와 남은 개수에 반영
- [x] `계산 보류`에서 점수 `—`, 자료 충족도 45%, 토양자료 없음 상태 표시
- [x] 근거 대화상자에 변수 6행 표시, 닫은 뒤 호출 버튼으로 포커스 복귀
- [x] `Escape`로 대화상자가 닫히지 않던 회귀를 발견해 수정 후 재검증
- [x] 마이페이지 큰 글씨와 고대비 전환
- [x] 주소를 모르는 분기에서 평창군 넓은 지역으로 진행하고 한계 안내
- [x] 오이 → 노지 → 참고지수 64, 오이 규칙 레이블, 근거 6행, 가중치 합 100%
- [x] 브라우저 console error 0개

검증은 프로토타입의 상호작용과 표시 일관성을 확인한 것이며, fixture의 농업 과학적 타당성을 승인한 것은 아니다.

## 11. 남은 한계와 다음 검증

- 모든 수치와 작물 규칙은 UI 검수용 체험 fixture다.
- 실제 기상청·흙토람·농사로 API, LLM, 로그인, 서버 저장, 주소검색, 필지·위성 서비스는 연결되지 않았다.
- 작물별 적정범위·민감도·가중치·행동 문구는 농업 전문가 검증과 버전관리가 필요하다.
- `내 분석` 저장은 현재 페이지 안의 시연 상태이며 영구저장이 아니다.
- 실제 60세 이상 농업인 대상 사용성 테스트를 하지 않았다.
- 200% 브라우저 확대와 느린 네트워크·저사양 기기 성능 검증을 하지 않았다.
- 실제 서비스 전환 시 API 실패, 재시도, 캐시·자료 최신성, 개인정보 동의와 위치정보 보관정책을 서버 동작과 함께 검증해야 한다.
- 이 화면의 결과는 실제 영농, 파종·방제·시비, 농지 매입, 투자 또는 재배 성공 판단에 사용할 수 없다.

## 12. 최종 산출물

- UI 프로토타입: [08_흙날씨진단_UI_샘플.html](./08_흙날씨진단_UI_샘플.html)
- UI·UX v3 협의록: [08_UIUX_에이전트_협의록.md](./08_UIUX_에이전트_협의록.md)
- 제품·데이터 기준선: [07_흙날씨진단_100점_최종기획서.md](./07_흙날씨진단_100점_최종기획서.md)

최종 합의의 우선순위는 **쉬운 최초입력 → 현재 판단과 첫 행동 → 저장·반복사용 → 검증 가능한 참고지수 → 결측·오류 방어 → 고령 사용자 접근성**이다. 시각적 장식이나 AI 인상은 이 우선순위를 앞설 수 없다.

## 13. 가시성 v4 재설계

### 13.1 사용자 피드백과 재진단

사용자는 데이터 관련 부분을 제외하고 UI·UX의 가시성이 매우 좋지 않다고 평가했다. 이에 따라 이번 재검토는 데이터의 정확성이나 계산 구조가 아니라 화면의 구분, 시선 흐름, 조작 우선순위와 읽기 편의성만을 대상으로 했다.

기존 v3는 본문 글자와 배경의 대비는 충분했지만, 다음과 같이 화면 요소 사이의 경계가 약했다.

| 기존 요소 | 대비 |
|---|---:|
| 흰색 패널 / 페이지 배경 | 1.09:1 |
| 패널 테두리 / 패널 | 1.38:1 |
| 입력 컨트롤 경계 / 입력 배경 | 2.10:1 |
| 선택 카드 경계 / 카드 배경 | 1.85:1 |

또한 CTA와 제목·본문의 강조 수준이 평준화되어 무엇을 먼저 읽고 눌러야 하는지 즉시 구분하기 어려웠다. 소비자 관점에서 결과 대시보드의 기존 가시성 평가는 **68/100**이었다.

### 13.2 변경 범위와 불변 범위

- **불변:** 데이터, 문구, 계산, JavaScript 동작, 정보구조(IA).
- **수정:** 색, 경계, 타이포그래피, 공간, CTA, 내비게이션, 온보딩의 시각적 표현.
- **금지:** gradient, glass, neon 표현을 사용하지 않는다.

### 13.3 실제 v4 구현

| 영역 | v4 구현 |
|---|---|
| 페이지와 패널 | 페이지 배경 `#ECEFEA`, 패널 경계 `#B9C1B9`로 면 분리 강화 |
| 입력 컨트롤 | 경계색 `#7B877D` 적용 |
| 카드 공간 | 주요 카드 사이 간격 22px |
| 데스크톱 내비게이션 | 흙·잎 계열 단색 사이드바, 활성 메뉴는 크림색 바탕과 토양색 4px 표시 |
| 정보 흐름 | `decision-flow`로 요약 → 오늘 할 일 → 위험 영역을 연속된 판단 순서로 연결 |
| CTA 위계 | `근거 보기`는 quiet, `조건 바꾸기`는 secondary로 낮춤. 재배 중 작업은 `완료`만 primary, `나중에 알림`은 secondary, `현장과 다름`은 quiet로 분리 |
| 글자 위계 | h1 36~38px, h2 26px, h3 20px, 본문 18px |
| 온보딩 | 선택 카드 최소 높이 92px, 진행 막대 너비 180px |
| 반응형 | 520px 이하에서 1열 구성 |
| 모바일 내비게이션 | 높이 70px 이상, 활성 메뉴 상단 4px 표시 |
| 키보드 포커스 | 어두운 사이드바에서 흰색 3px outline, 2px offset, `#1E2E23` 5px 외곽 링 적용 |

### 13.4 검증 결과

#### 정적 검증

| 항목 | 결과 |
|---|---:|
| JavaScript 문법 | 통과 |
| 전체 ID | 102개 |
| 중복 ID | 0개 |
| ARIA / label `for` 대상 누락 | 0개 |
| script SHA | `b05dc3fe...` — v3와 동일 |
| visible text SHA | `3179ce7a...` — v3와 동일 |
| gradient 사용 | 0개 |
| `backdrop-filter` 사용 | 0개 |

script와 visible text의 해시가 동일하므로 이번 수정이 데이터·문구·계산·동작을 변경하지 않은 시각 계층 재설계임을 확인했다.

#### 시각 재평가

| 항목 | 대비 |
|---|---:|
| 컨트롤 경계 | 3.75:1 |
| 주요 CTA | 7.11:1 |
| 사이드바 기본 글자 | 8.97:1 |
| 사이드바 활성 글자 | 10.50:1 |
| 모바일 비활성 내비게이션 | 7.76:1 |
| 모바일 활성 내비게이션 | 8.33:1 |

가시성 재평가는 **90/100**이며, 해당 시각 평가 범위에서는 P0 문제가 없었다.

#### 고령 사용자 UX 재감사와 최종 보완

초기 v4에 대한 고령 사용자 관점 재감사 점수는 **86/100**이었다. 이 재감사에서 다음 P0 두 건을 추가로 확인했다.

| P0 발견사항 | 최종 보완 | 결과 |
|---|---|---|
| 상단 `조건 바꾸기`와 작업 CTA가 모두 강하게 보여 핵심 행동의 우선순위가 불명확함 | `조건 바꾸기`를 secondary로 낮추고, 재배 중 작업은 `완료`만 primary, `나중에 알림`은 secondary, `현장과 다름`은 quiet로 분리 | 해소 |
| 어두운 사이드바에서 키보드 포커스 표시가 충분히 분리되지 않음 | 흰색 3px outline, 2px offset과 `#1E2E23` 5px 외곽 링을 함께 적용 | 해소 |

두 보완의 실제 구현을 확인했으며, 고령 사용자 UX 재감사에서 발견된 **P0 2건은 모두 해소**됐다. 앞의 90/100은 시각 가시성 평가이고 86/100은 고령 사용자 행동 우선순위·키보드 접근성을 포함한 보완 전 재감사 점수이므로 서로 다른 평가 축과 시점의 결과다. 두 패치 적용 후 고령 사용자 UX를 다시 평가한 최종 점수는 **91/100**이며, 최종 P0 잔여 건수는 0개다. 다만 이 평가는 전문가 휴리스틱 재평가이며 실제 고령 농업인을 모집한 사용성 테스트 결과는 아니다.

### 13.5 검증 한계

v3에서 기록한 1440×900, 390×844, 320×800 브라우저 레이아웃·상호작용 검증은 기존 브라우저 검증 결과다. 이번 v4에서는 정적 구조, 문법, 해시, 색상 대비와 금지 표현 여부를 검증했다. 다만 이 턴의 로컬 `file://` URL 제어 제한으로 v4 화면의 브라우저 렌더링 재검증은 수행하지 못했다. 따라서 최종 배포 전 실제 브라우저에서 데스크톱·모바일 레이아웃, 포커스 표시, 텍스트 확대와 하단 내비게이션의 겹침 여부를 다시 확인해야 한다.

## 14. 상용 SaaS v5.1 전면 재설계

### 14.1 재설계 배경과 범위

사용자는 v4에 대해 **바뀐 것이 거의 없어 보이고 정부 앱 같으며, 실제 전문 서비스 앱 기준에 미치지 못하고 폰트와 글자 품질도 낮다**고 평가했다. 따라서 v5는 기존 화면에 색이나 테두리를 더하는 수준이 아니라, 결과를 읽는 순서와 제품의 조작 구조를 상용 SaaS 방식으로 다시 조직하는 전면 재설계로 진행했다.

이번 범위는 데이터 정확도, 계산값 또는 농업 판단 규칙을 바꾸는 작업이 아니다.

- **불변:** 작물·지역·점수·위험·자료상태·출처·계산·문구를 포함한 데이터 블록과 기존 제품 사실.
- **변경:** app shell, 화면 위계, 타이포그래피, 내비게이션, CTA, 정보 밀도, 온보딩, 반응형 표현과 상호작용 진입점.
- **원칙:** 데이터 의미는 유지하고, 사용자가 `현재 판단 → 점수 → 지금 할 일 → 근거`를 더 빠르게 찾게 한다.

### 14.2 전문가 역할과 합의

네 가지 핵심 전문가 역할이 서로 다른 축에서 화면을 검토했다. 고령 사용자 UX는 최종 단계에서 이 네 역할의 결론을 다시 교차 검증하는 별도 재감사 축으로 적용했다.

| 역할 | 주요 조언 |
|---|---|
| 상용 SaaS 시각 감사 | 긴 세로 카드 보고서를 버리고 quiet chrome, 안정된 grid, 제한된 surface와 명확한 visual weight를 사용한다. |
| 한국어 타이포그래피·브랜드 | 실행 환경에 없는 폰트명을 선언하는 것으로 끝내지 말고 실제 로딩되는 한국어 variable font를 사용한다. 굵기 800·900에 의존하지 않고 크기·행간·간격으로 위계를 만든다. |
| SaaS UX 아키텍처 | sidebar, 현재 분석 context, topbar와 command shell을 만들고 hero+score, metric strip, action workspace, progressive disclosure 순서로 재구성한다. |
| 소비자·경쟁 서비스 비교 | Linear·Ramp·Stripe·OneSoil처럼 첫 화면에서 현재 상태와 다음 행동이 연결되어야 하며, 지도나 위성은 실제 공간 데이터가 있을 때만 보여야 한다. |

고령 사용자 UX 재감사에서는 글자 크기뿐 아니라 첫 행동의 명확성, 모바일 터치 크기, 키보드 포커스, 화면 진입 직후 제목 전달과 숨은 콘텐츠 여부를 점검했다.

### 14.3 참고 기준과 적용 원칙

다음 서비스는 시각을 복제하기 위한 템플릿이 아니라 제품 구조와 정보 위계의 참고 기준으로만 사용했다.

- [Linear UI redesign](https://linear.app/now/how-we-redesigned-the-linear-ui): quiet chrome과 중요도에 따라 얻어지는 시각적 무게.
- [Linear latest design refresh](https://linear.app/now/behind-the-latest-design-refresh): 조밀한 제품 화면에서의 일관된 시각 언어.
- [Stripe Dashboard basics](https://docs.stripe.com/dashboard/basics): 안정된 dashboard shell과 지속되는 context.
- [Ramp products](https://ramp.com/products): 상태 설명보다 처리할 행동을 먼저 제시하는 action-first 구조.
- [OneSoil platform](https://onesoil.ai/en/platform): 실제 필지와 연결되는 field monitoring의 공간적 맥락.
- [SUIT](https://github.com/sun-typeface/SUIT): 한국어 UI에 맞는 variable font와 일관된 숫자·본문 표현.

참고 서비스의 브랜드 장식이나 데이터를 가져오지 않았으며, 흙날씨 진단의 사용자·기능·데이터 한계를 우선했다.

### 14.4 v4 초기 상용 시각 감사

v4의 상용 제품 시각 점수는 **52/100**이었다.

| 원인 | 관찰 결과 |
|---|---|
| 정부형 내비게이션 인상 | 약 232~244px의 큰 좌측 sidebar와 넓은 메뉴 버튼이 제품 조작 shell보다 공공 포털 메뉴처럼 보였다. |
| 세로 카드 보고서 | 요약, 위험, 점수, 출처, AI, 근거와 위성이 같은 카드 문법으로 계속 이어져 결과 보고서를 스크롤하는 경험이 됐다. |
| 과도하게 큰 기본 밀도 | 18px 기본 글자와 52px 컨트롤이 거의 모든 요소에 동일하게 적용되어 핵심 행동과 보조 조작의 밀도 차이가 사라졌다. |
| 제품 언어 부족 | 브랜드 자산, 일관된 아이콘, 현재 분석 context, 최근 항목, 사용자 영역과 명령형 shell이 부족했다. |
| 폰트 품질 불확실 | `Pretendard`를 font stack에 적었지만 실제 환경에 설치되지 않으면 시스템 fallback이 사용되어 한국어 폭·굵기·숫자 정렬이 환경마다 달라졌다. |

이에 따라 [SUIT Variable](https://github.com/sun-typeface/SUIT)의 공식 CDN을 적용했다. 이는 특정 기기에 설치된 폰트에 의존하지 않고 한국어 본문, 숫자와 UI 레이블을 하나의 family로 일관되게 표시하기 위한 선택이다. CDN 의존성은 별도 한계로 남긴다.

### 14.5 v5 제품 구조 구현

#### App shell과 내비게이션

- 어두운 quiet app shell을 사용해 작업 영역과 내비게이션을 분리했다.
- sidebar에 현재 context, 최근 분석, 사용자 상태를 넣고 topbar에 현재 화면과 주요 조작을 배치했다.
- 체험 데이터라는 사실을 숨기지 않도록 test mode를 지속적으로 표시했다.
- 데스크톱은 제품 내 탐색을 유지하고, 모바일은 하단 내비게이션으로 핵심 세 화면을 연결했다.

#### Dashboard

- 최대 1280px 작업 영역에서 첫 overview를 **8:4 비율의 hero+score**로 구성했다.
- hero에는 현재 판단과 다음 행동을, score에는 참고지수·추정범위·자료상태를 배치했다.
- 기후, 토양, 자료 충족도와 향후 위험은 하나의 metric strip으로 묶었다.
- 오늘 할 일과 가까운 위험은 action workspace로 연결했다.
- AI 설명, 점수를 낮춘 이유, 계산·출처·한계는 insight와 evidence의 progressive disclosure 구조로 분리했다.
- 실제 지도 대신 자료 지점 간 거리만 설명하는 honest spatial diagram을 사용했다.

#### Onboarding과 반응형

- 데스크톱 온보딩은 **38:62 split layout**으로 구성했다. 왼쪽은 진행 단계와 입력 이유, 오른쪽은 실제 form과 검토 내용이다.
- 기존 5단계, `잘 모름` 분기, 검증과 저장 동의 기본값은 유지했다.
- 모바일은 한 열 흐름과 하단 내비게이션을 사용하고, 데스크톱 shell을 단순 축소하지 않았다.
- gradient, glass, neon과 실제 데이터가 없는 fake map은 사용하지 않았다.

### 14.6 v5 1차 감사와 발견사항

v5 1차 결과는 상용 시각 **81/100**, 소비자·경쟁 서비스 관점 **83/100**이었다. 정부형 카드 보고서에서는 벗어났지만 다음 P0·P1 문제가 남았다.

- 첫 행동이 화면에 보이지만 실제 checklist를 여는 조작이 아닌 정적 문장에 가까웠다.
- score가 여전히 큰 시각 무게를 가져 사용자의 다음 행동보다 먼저 읽혔다.
- 제품 전체를 빠르게 탐색하는 command shell이 없었다.
- 아이콘 언어가 충분히 일관되지 않았다.
- 일부 metric·insight에 카드 반복이 남았다.
- 모바일에서 일부 터치 영역이 목표보다 작았다.
- 필지 데이터가 없다는 정직함은 있었지만, 공간 정보를 이해할 anchor가 약했다.

### 14.7 발견된 JavaScript 회귀와 수정

새 metric DOM을 추가하면서 기존 `setMeter`가 값 요소를 `nextElementSibling`로 찾는 구조적 가정을 그대로 사용했다. DOM 순서가 달라지면 대상이 없거나 잘못된 노드를 가리켜 `TypeError`가 발생할 수 있는 회귀였다.

- meter 값 요소에 고유 ID를 부여했다.
- `setMeter`가 ID를 통해 명시적으로 대상을 찾도록 변경했다.
- 대상이 없을 때 중단하는 null guard를 추가했다.
- 수정 후 JavaScript 문법과 literal ID selector를 다시 검사했다.
- 데이터 블록 hash는 그대로 유지되어 계산·fixture가 바뀌지 않았음을 확인했다.

### 14.8 v5.1 최종 보완

#### 행동과 점수 위계

- 첫 행동을 실제 primary checklist CTA로 만들었다.
- CTA는 `aria-expanded`와 `aria-controls`로 펼침 상태와 대상을 연결하고, 펼친 뒤 checklist로 focus가 이동한다.
- score 크기를 46~52px로 낮춰 행동보다 점수가 과도하게 지배하지 않도록 했다.

#### 공간 anchor와 정보 밀도

- 첫 viewport에 `필지 미등록` 상태와 5km, 6.8km, 12.4km의 자료 거리 anchor를 함께 표시했다.
- 공간 도식에 **실제 필지 경계가 아님**을 명시했다.
- metric과 insight의 불필요한 중첩 테두리를 제거해 flattened structure로 정리했다.

#### 아이콘·명령·모바일

- 7종 아이콘을 17px, 1.7-stroke의 CSS mask 체계로 통일했다.
- 데스크톱에서 실제로 동작하는 `Cmd/Ctrl+K` command palette를 추가했다.
- 320px에서는 topbar 내용을 축약해 핵심 context와 조작이 겹치지 않도록 했다.
- 모바일 보조 조작은 최소 44px, 주요 조작은 48px 이상으로 조정했다.

#### 마지막 cascade P0

일반 모바일 버튼을 44px로 지정했지만 `.task-actions .button`의 42px 규칙이 더 높은 specificity를 가져 작업 버튼에 남는 문제가 발견됐다. 이를 stylesheet 후순위의 동일 specificity 규칙으로 48px로 덮어 최종 해결했다.

- 작업 action: 48px.
- data state와 topbar 조작: 44px.
- 모바일 bottom navigation 레이블: 13px.
- 공간 도식 title: 14px, row: 13px.
- 온보딩을 처음 열 때 단계 제목으로 focus를 이동한다.
- 온보딩 활성 여부와 skip-link의 inert 상태를 동기화했다.

### 14.9 최종 전문가 평가

| 평가 축 | 최종 점수 | P0 |
|---|---:|---:|
| 상용 SaaS visual | 88/100 | 0개 |
| 소비자·경쟁 서비스 비교 | 90/100 | 0개 |
| UI-only 고령 사용자 UX | 92/100 | 0개 |

세 점수는 전문가 휴리스틱 평가이며 실제 소비자 또는 고령 농업인을 모집한 사용성 테스트 결과가 아니다.

### 14.10 최종 독립 정적 검증

| 항목 | 결과 |
|---|---:|
| 파일 크기 | 151,216 bytes |
| 줄 수 | 2,061 lines |
| JavaScript 문법 | 통과 |
| 전체 ID | 123개 |
| 중복 ID | 0개 |
| 존재하지 않는 ARIA 참조 | 0개 |
| 존재하지 않는 literal ID selector | 0개 |
| 데이터 블록 SHA-256 | `371e6a5210fbc73256a2f66f5c2d1ff6b94af84fd3fe1648fa5332fd5bff1e50` |
| HTML tag balance | 전체 0 |
| `font-weight: 800/900` | 0개 |
| gradient | 0개 |
| `backdrop-filter` | 0개 |

버튼의 접근 가능한 이름과 320px 위험 항목은 구현자 정적 감사에서 통과했다. 이는 실제 브라우저 렌더링 또는 상호작용 자동화 통과를 뜻하지 않는다.

### 14.11 한계와 남은 P1

- 이번 turn에는 실제 browser pixel/render, console, overflow와 상호작용 자동화를 수행하지 않았다.
- 실제 고령 농업인 또는 귀농 준비자를 모집한 사용성 테스트를 하지 않았다.
- SUIT 공식 CDN은 네트워크 연결에 의존한다. 출시 전 self-host font와 fallback 동작 검증이 필요하다.
- v4, v5, v5.1 CSS override가 계층으로 남아 있어 향후 하나의 design token·component layer로 통합하는 것이 바람직하다.
- 실제 필지가 등록되기 전에는 fake map 또는 satellite 화면을 표시하지 않는다.
- 남은 P1은 CSS consolidation과 font self-hosting, 더 세밀한 icon semantics, 모바일 command parity, 재진입 onboarding의 취소 경로, 핵심 보조문자 14px 확대와 큰 글씨 설정 진입 개선이다.

### 14.12 결론

v5.1은 흙날씨 진단을 정부형 결과 페이지에서 **출시 가능한 상용 MVP 수준의 제품 shell과 행동 중심 dashboard**로 전환했다. 다만 정적 완성도와 전문가 점수는 실제 제품 검증을 대신하지 않는다. 실제 사용자 테스트, 브라우저·기기 검증, font 자가 호스팅과 실제 필지·위성 연동은 다음 단계다.
