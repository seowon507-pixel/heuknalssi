# 흙날씨 업데이트 계획 (본선 대비)

작성 2026-07-31 · 기준 브랜치 `v01`

이 문서는 "무엇을 만들지"가 아니라 **"각 항목을 하려면 무엇이 먼저 필요한지"**를
정리한다. 코드에서 실제로 확인한 현재 상태만 근거로 쓴다.

---

## 1. 지금 실제 상태 (2026-07-31 확인)

### 동작하는 것

| 항목 | 상태 | 확인 방법 |
|---|---|---|
| 사전점검 | `READY`, blockers 0건 | 로컬 `/api/health/preflight` |
| 카카오 주소 | READY | 실호출 |
| 기상청 기후평년 | READY | 내장 1991–2020, 219지점 |
| 기상청 ASOS 최근관측 | READY | 실호출 (서울 108 정상) |
| 기상청 단기·중기예보 | READY | 실호출 |
| 농경지화학성 V2 | READY | 실호출 |
| 토양특성 V3 · 토양검정 V2 | READY (optional) | preflight |
| 농장 분석 도우미 | READY | Google AI 선택형 |
| 검수 지역 | **33곳** (서울 25 + 8) | `reviewed-location-mappings.js` |
| 작물 | **5종** 사과·배·오이·감자·상추 | `reviewed-crop-rules.js` |
| 규칙 | 103개 | preflight `ruleRegistry` |

감사보고서(`audit/PROJECT_REAUDIT_2026-07-26.md`)에서 FAIL이던 기후평년·ASOS
권한 문제는 **모두 해소됐다.** 그때 BLOCKED였던 백테스트도 이제 실행 가능하다.

### 막혀 있는 것

| 항목 | 값 | 의미 |
|---|---|---|
| `capabilities.persistence` | **`NOT_AVAILABLE`** | 분석·세션이 메모리에만 있음. 서버 재시작·서버리스 인스턴스 전환에 소실 |
| `capabilities.satellite` | `DISABLED` | 플래그를 켜도 `UNSUPPORTED`로만 보고 |
| `capabilities.report` | `DETERMINISTIC_TEMPLATE` | 자유 생성 없음 |
| `capabilities.deviceBackup` | `CONFIGURED_UNVERIFIED` | Supabase 스키마·RLS·RPC 마이그레이션이 저장소에 없어 UI 비활성 |

**`persistence: NOT_AVAILABLE`이 이번 업데이트의 최대 병목이다.**
아래 항목 중 2·3·B·C·E가 전부 여기에 막혀 있다.

### 백업 브랜치에 있는 미반영 자산

`backup/team-main-before-overwrite` (태그 `team-main-1f5328c`)

- `infrastructure/web-push.js` — VAPID·RFC8291을 `node:crypto`만으로 구현, 의존성 0
- `application/plain-report.js` — 쉬운 말 리포트 (숫자 대조·금지어·분량 3중 검증)
- `domain/suitability.js` — 백엔드 생육 적합도
- `scripts/build-icons.mjs`, `png.mjs`, `generate-vapid-keys.mjs`, 아이콘 4종

**이 4개는 새로 만들지 말고 가져오는 쪽이 빠르다.**

---

## 2. 요청한 업데이트 항목

### ① 인공위성 데이터

**현재** `ENABLE_SATELLITE`를 켜도 `server/config.js:184`에서 `UNSUPPORTED`로만
보고한다. 다만 `domain/request.js:106-111`에 "노지 재배만 가능", "확정된 필지
필요" 가드가 이미 설계돼 있어 뼈대는 있다.

**필요한 것**

1. Copernicus Data Space Ecosystem 가입 → OAuth2 client credentials
2. Statistics API 어댑터 1개 (`adapters/satellite-ndvi.js`)
   폴리곤 + 기간을 주면 NDVI 평균을 JSON 숫자로 반환한다. 영상 처리 불필요
3. 계약 버전 상수 + `.env.example` 플래그 + preflight 노출
4. 구름 판정(SCL 밴드)으로 유효 장면 없으면 `HOLD` 처리
5. UI: NDVI 시계열 패널 (`#satellite-title`이 이미 있으나 `hidden` 처리 중 —
   `backend-client.mjs:3321`)

**주의**

- **필지 경계가 없다.** 토양특성 V3가 경계를 주지 않으므로 주소 좌표 주변
  버퍼 원으로 계산하고 "내 필지 정확값이 아님"을 명시해야 한다
- **한국 여름은 구름이 많다.** 5일 재방문 × 장마 = 유효 장면 없는 날이 잦다.
  `HOLD`가 뜨는 건 이 앱의 일관성이지만, **시연 당일 NDVI가 안 뜰 위험**이
  있으므로 발표 데모는 사전 확보한 실제 응답으로 준비할 것

**선행조건** 없음 (독립 실행 가능) · **난이도** 중 · **권장 순서** 마지막

---

### ② 투두리스트 (현 기능 개선)

**현재** `TODO_STORAGE_KEY = "heuknalssi.todo.v1"`로 localStorage에 **오늘 할 일
한 줄만** 저장해 알림 문구로 쓴다 (`backend-client.mjs:3870`). 리스트도, 완료
체크도, 이력도 없다.

**좋은 소식: 데이터 모델이 이미 완성돼 있다.**
`domain/actions.js`의 랭킹 결과가 그대로 투두 스키마다.

- `dueWindow`: `NOW` / `1_TO_3_DAYS` / `BEFORE_DECISION` / `4_TO_10_DAYS`
- `severity`: `WARNING` / `CAUTION` / `INFO`
- `guidance.reason`, `guidance.recheck`, `triggerIds`, 출처 URL

**필요한 것**

1. `projectDisplayActions(…, 3)`로 3개만 자르는 대신 전체 랭킹을 기한별
   그룹으로 렌더
2. 완료 체크 + 완료 시각 저장 → **영속화 필요**
3. 항목마다 근거 칩(왜 해야 하는지 + 출처 링크) — 데이터는 이미 있음
4. 완료 이력 조회 API

**차별점** 다른 앱 투두에는 근거가 없다. "원인 → 위험 → 행동 → 재확인"이
붙은 투두는 이 프로젝트만 만들 수 있다.

**선행조건** 영속화(3-1) · **난이도** 하 · **권장 순서 1위 (ROI 최고)**

---

### ③ 사진 저장 · 비교 분석 · 시즌 정리

**현재** `#growth-photo` 입력이 있으나 미리보기만 하고 버린다
(`ui-shell.mjs:17-21`). 저장·전송하지 않는다고 화면에 명시돼 있다.

**설계 경고 — 사진으로 진단하지 말 것**

사진을 비전 LLM에 넣어 "잎이 노랗네요, 질소 부족입니다"를 하는 순간, 이
프로젝트가 높은 평가를 받는 이유(검증 안 된 건 단정하지 않고 `HOLD` 표시)를
스스로 부순다. `guarantees.freeFormLlm: false`가 preflight에 공개돼 있다.
심사위원은 반드시 "그 판단 근거가 뭐죠?"를 묻는다.

**대신: 사진은 기록, 비교 분석은 데이터**

1. 사진 저장 시 그날의 스냅샷 동시 기록
   — 생육 적합도 점수, 기온·강수, 완료한 작업, 발생한 위험
2. 타임라인 = 사진 슬라이더 + 점수 꺾은선 + 위험 마커 + 작업 완료 마커
3. "악화" 판정은 **점수·위험 이력**으로 하고 사진은 옆에 놓아 눈으로 확인
4. 정량 지표를 사진에서 뽑고 싶으면 **ExG 초록 피복률**
   (`2G-R-B`, canvas에서 결정론적 계산, ML 없음) — "AI가 추측"이 아니라
   "픽셀을 셌습니다"라서 이 프로젝트 철학과 맞는다
5. 시즌 종료 리포트: 기간 요약 + 완료 작업 수 + 위험 대응 이력 + 사진 타임랩스

**필요한 것** 이미지 저장소(Supabase Storage 등) · 영속화 · 타임라인 UI

**선행조건** 영속화(3-1) · **난이도** 중 · **권장 순서** 3위

---

### ④ 챗봇 업그레이드

**현재 구조** (`application/assistant.js`)

분석 결과에서 **최대 24개 검증 항목 카탈로그**를 만들고 → Gemini는 **ID만
고른다** → 고정 문구를 제목별로 이어붙여 출력. **자유 생성이 아예 없다.**
환각은 구조적으로 불가능하지만 읽으면 기계 같다.

**업그레이드 경로 (권장 순서)**

**(c) 음성 입출력 — 가장 먼저**
타깃이 초보·고령 사용자인데 타이핑을 시키고 있다. Web Speech API
(`SpeechRecognition` ko-KR + `SpeechSynthesis`). **라이브러리 추가 0, 비용 0.**
감사보고서의 "실제 초보·고령 사용자 평가 — NOT RUN" 항목과도 직결.

**(a) 제약된 생성**
선택된 항목을 Gemini가 자연스러운 한국어로 다시 쓰게 하되, 출력의 모든
숫자·날짜·고유명사가 선택 항목 안에 존재하는지 후검증하고 하나라도 새로
생기면 폐기 후 현재 방식으로 폴백.
→ **`plain-report.js`가 이미 이 검증을 구현해 뒀다. 백업 브랜치에서 가져와
챗봇에 재사용하면 된다.** 새로 만들지 말 것.
→ 시연에서 "없는 숫자를 만들게 유도했더니 거부하고 폴백합니다"를 보여주면
그 자리에서 점수가 갈린다.

**(b) 카탈로그 확장**
지금은 현재 분석 결과만 들어간다. 여기에 작물 규칙 출처 · 투두 완료 이력 ·
사진/NDVI 타임라인 · 백테스트 정확도를 추가하면 **"지난주보다 나빠진 거야?"
"왜 그렇게 판단했어?"** 같은 실제 농가 질문에 답할 수 있게 된다.

**(d) 근거 칩 인라인** — `itemIds`가 이미 반환되므로 UI만 붙이면 됨

**선행조건** (c)(a)(d) 없음 / (b) 영속화 · **난이도** 하~중 · **권장 순서** 4위

---

## 3. 추가 제안

### A. 예보 정확도 배지 ★ 최우선 — **구현 완료 (v01)**

실행해 보니 상황이 감사 기록과 달랐다. 사실만 적는다.

- 감사 때 `INTERNAL_ERROR`였던 원인은 권한이 아니라 **API 제약**이었다.
  `getVilageFcst`는 `resultCode 10 "최근 3일 간의 자료만 제공합니다"`를
  돌려준다. 1년 전 발행분은 애초에 조회되지 않는다.
- 기상청 API허브의 단기예보 과거자료는 `403 "활용신청이 필요한 API 입니다"`.
  **활용신청 한 건이면 과거 1년치가 한 번에 열린다.**
- ASOS 일자료는 **전날까지** 제공된다. 발행 예보는 **D-0·D-1**만 조회된다.

그래서 승인 없이도 굴러가도록 **누적 원장** 방식으로 만들었다.

- `scripts/collect-forecast-ledger.mjs` — 매일 발행분을 리드타임별로 적고,
  유효일에 ASOS 실측이 나오면 채운다. 결측은 보간하지 않는다.
- `scripts/build-forecast-accuracy.mjs` — 검수된 백테스트 엔진으로
  MAE·RMSE·bias·Brier를 계산해 `ui-integration/forecast-accuracy.json` 생성.
  리드타임을 섞지 않고, 표본 10쌍 미만이면 `PARTIAL`로 표시한다.
- `.github/workflows/forecast-ledger.yml` — 매일 09:10 KST 자동 수집·커밋.
  **하루 거르면 그날 발행분은 되찾을 수 없으므로 사람 손에 맡기지 않는다.**
- 화면: 예보 카드에 배지. 표본이 기준 미만이면 숫자를 확정된 정확도처럼
  쓰지 않고 "정확도 검증 중 · 현재 N쌍" 으로만 표시한다.

첫 수집 실측(2026-07-31): 8개 지점 · 예보-실측 **8쌍** ·
최고기온 **MAE 1.41℃** (bias +1.21, 리드타임 0일).

**남은 일** — 표본이 쌓이기를 기다리거나, API허브 활용신청으로 과거분을
한 번에 채우는 것. 후자가 훨씬 빠르다.

### A-1. (참고) 원래 계획했던 내용

`application/backtest.js`에 MAE·RMSE·bias·강수확률 Brier score가 **이미 구현돼
있고 단위 테스트도 통과한다.** 감사 시점에는 ASOS 권한이 없어 BLOCKED였지만
**지금은 ASOS가 READY라 바로 실행 가능하다.**

**필요한 것**
1. `scripts/verify-backtests.js`를 최근 3개월 구간으로 실행해 지표 산출
2. 결과를 정적 JSON으로 저장 (매 요청 재계산 불필요)
3. 예보 카드 옆에 배지 노출 — "이 지역 최고기온 예보 MAE 1.4℃"

**하루면 된다. 이런 지표를 가진 해커톤 팀은 사실상 없다.**

**선행조건** 없음 · **난이도** 하 · **권장 순서 2위**

---

### B. 위험 예보 푸시 알림

**현재** 알림은 앱이 켜져 있어야만 뜬다 (`showNotificationNow`, SW postMessage).

실제 가치는 **저온·폭염·호우가 예보에 잡히면 전날 저녁에 먼저 알려주는 것**.
심사위원의 "왜 이 앱을 다시 열죠?"에 대한 유일한 답이다.

**필요한 것**
1. `web-push.js`를 백업 브랜치에서 가져오기 (**이미 구현 완료, 의존성 0**)
2. VAPID 키 생성 (`generate-vapid-keys.mjs`도 백업에 있음) → Vercel 환경변수
3. 구독 정보 저장 → **영속화 필요**
4. `vercel.json`에 cron 추가 → 예보 점검 후 위험 시 발송

**선행조건** 영속화(3-1) · **난이도** 중(대부분 이미 구현됨) · **권장 순서** 5위

---

### C. 영농일지 내보내기

투두 완료 이력 + 사진 + 그날 기상 근거를 영농일지 양식(PDF/CSV)으로 출력.
GAP 인증·직불금 서류에 실제로 쓰인다.

**②·③을 만들면 거의 공짜로 나오는 결과물**인데 "실질적 쓸모"를 한 방에
증명한다. 심사에서 "실제 행정 서식과 연결"은 강한 카드다.

**선행조건** ②③ · **난이도** 하 · **권장 순서** 6위

---

### D. 지역 분포 속 내 위치 시각화

`crop-condition-score.mjs`가 이미 지역 pH 분포 대비 `fitRatio` /
`uncertainRatio` / `outsideRatio`를 계산 중이고 농경지화학성 V2는 READY다.
**데이터는 이미 흐르고 있고 UI만 없다.** 분포 그래프에 내 위치 점만 찍으면 끝.

**선행조건** 없음 · **난이도** 하 · **권장 순서** 곁다리로 아무 때나

---

### E. 오프라인 열람

밭에는 신호가 안 터진다. 마지막 분석을 IndexedDB에 캐시. PWA는 이미 있다.

**선행조건** 없음 · **난이도** 하

---

### F. 실제 사용자 5명 테스트

감사보고서의 **유일한 NOT RUN 항목**. 하루면 된다.
발표에 "60~70대 5명 중 4명이 도움 없이 완료" 한 줄 넣는 것이, 기능 하나 더
넣는 것보다 본선에서 세다.

**선행조건** 없음 · **난이도** 하 · **권장** 기능 마감 후 즉시

---

### 하지 말 것

**피해액·수확량 예측.** 추정 금지 원칙(`guarantees`)과 정면충돌하고
심사위원이 근거를 물으면 무너진다.

---

## 4. 선행조건: 영속화 (3-1)

`capabilities.persistence: NOT_AVAILABLE`이 ②③B(C)E를 전부 막고 있다.

**필요한 것**

1. Supabase 스키마 마이그레이션을 **저장소에 포함** — 지금은 없어서
   `deviceBackup`이 `CONFIGURED_UNVERIFIED`이고 UI에서 비활성 상태
2. 테이블: `analyses`, `todo_items`, `todo_completions`, `growth_photos`,
   `push_subscriptions`, `seasons`
3. RLS 정책 + 익명 세션 키 설계
4. Supabase Storage 버킷 (사진)
5. 서버리스 세션을 메모리에서 공유 저장소로 교체
   (`14_팀_변경분_비교_검토.md`의 "배포 전 필수 작업"에 이미 지적돼 있음)

**이것 하나가 열리면 ②③B C E가 동시에 가능해진다.**

---

## 5. 권장 순서

| 순위 | 항목 | 선행조건 | 난이도 |
|---|---|---|---|
| 0 | 백업 브랜치에서 4개 자산 회수 (web-push · plain-report · suitability · 아이콘) | 없음 | 하 |
| 0 | 작물 5종 → 10~12종 (벼·고추·마늘·양파·배추·토마토·딸기) | 없음 | 중(데이터 입력) |
| 1 | **A. 예보 정확도 배지** | 없음 | 하 |
| 2 | **영속화 (3-1)** | 없음 | 중 |
| 3 | ② 투두리스트 정식화 | 영속화 | 하 |
| 4 | ③ 사진 타임라인 + 시즌 리포트 | 영속화 | 중 |
| 5 | ④ 챗봇 (c 음성 → a 제약생성 → d 근거칩 → b 카탈로그) | (b만 영속화) | 하~중 |
| 6 | B. 푸시 알림 | 영속화 | 중 |
| 7 | C. 영농일지 내보내기 | ②③ | 하 |
| 8 | ① 위성 NDVI | 없음 | 중 |
| 상시 | D 분포 시각화 · E 오프라인 · F 사용자 테스트 | 없음 | 하 |

### 왜 이 순서인가

- **작물 확장이 0순위인 이유**: 심사위원은 자기 작물을 넣는다. 지금 5종에는
  벼가 없다. 한국 농업 해커톤에서 이건 치명적이다. 지역은 서울 25구를
  추가해 33곳이 됐지만 작물은 그대로다
- **A가 1순위인 이유**: 이미 구현된 엔진 + 이제 열린 ASOS = 하루 작업으로
  다른 팀이 못 가진 지표를 얻는다
- **영속화가 2순위인 이유**: 나머지 대부분의 잠금을 한 번에 푼다
- **위성이 마지막인 이유**: 가치는 높지만 시연 당일 구름 위험이 있고,
  ③ 타임라인 위에 한 줄 얹는 형태가 가장 자연스럽다

---

## 6. 이번 브랜치(`v01`)에서 한 일

### 서울특별시 25개 자치구 추가 (검수 지역 8곳 → 33곳)

전부 **실제 API 호출로 검증**했고 추측한 값은 없다.

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 법정동코드 25개 | 카카오 주소 API `b_code` 전수 조회 | 25/25 확인 |
| 중기기온 구역 `11B10101` | `getMidTa` 실호출 | `NORMAL_SERVICE` |
| 중기육상 구역 `11B00000` | `getMidLandFcst` 실호출 | `NORMAL_SERVICE` |
| ASOS 관측지점 서울(108) | `getWthrDataList` 실호출 | 정상 (평균 29.0℃) |
| 기후평년 지점 108 | 내장 데이터셋 | 12개월 전부 존재 |
| 농경지화학성 V2 | 25개 구 전수 조회 | 25/25 농경지 면적 > 0 |

**설계 메모**: 조회 로직(`location-keys.js:304`)이 10자리 법정동코드의 앞
5자리로 `<5자리>00000` 후보를 만들므로, 서울은 자치구 단위 코드를 키로 쓰면
기존 로직 변경 없이 동작한다. (경기 광역시 계열에서 발생하는 4자리 접두어
문제와 달리 서울 자치구는 5자리 규칙에 그대로 맞는다.)

**로컬 실서버 확인** — 강동구/상추, 서초구/감자, 노원구/사과, 종로구/오이
4건 모두 분석 성공. 기후 `READY`, 예보 `READY`, 토양 `READY`~`PARTIAL`로
대조군(안동)과 동일 수준.

### 검증

- UI 테스트 62 통과 · 백엔드 테스트 270 통과 · 프로젝트 검사 통과
- Vercel 정적 빌드 통과
- 사전점검 `READY`, blockers 0건, 검수 지역 33곳 (p0 33)
